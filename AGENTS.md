# Master-Agent Handoff Doc

This file briefs an orchestrator agent (a "master bot") on what ResearchOS is, how the codebase is organized, and the working conventions established over a long collaboration on it. Read this end-to-end before suggesting work, spawning sub-agents, or committing. For deeper architecture (data flow, FSA wrapper internals, store layout), read `ARCHITECTURE.md` next.

> **Inheriting a live session?** Read the latest dated session handoff first for exactly where work stands and what is in flight. NOTE: several sessions run in parallel; there are FOUR recent, INDEPENDENT handoffs from 2026-06-12, read whichever matches your task. (A) **`~/Desktop/HandoffForAgents/2026-06-12_ui-explorers-density-and-checkins.md`** (Methods/Experiments/Notes explorer redesigns + method fork-creation + density pass; the Check-ins revamp ALL FOUR PHASES now BUILT on local main [group check-ins + D4 real-task sync, the full IDP, seed templates/compact/onboarding, mentorship-tree/skip-level/rotation/committee/roster-IDP-status]; the demo-page floating-button fixes BUILT; and the global-nav slimming arc where the drag-to-customize mockup is APPROVED but the REAL BUILD is NOT STARTED [the inheritor's main task]; all on LOCAL main, not pushed, none browser-verified by the orchestrator). **CONTINUED by `~/Desktop/HandoffForAgents/2026-06-12_ui-polish-nav-icons-folderconnect.md`** (the global-nav build is now DONE + browser-verified; plus a full icon de-collision audit with 10 new glyphs + the one-glyph-per-meaning rule, the folder-connect dropzone-first redesign, and the demo-mode migrate-to-solo gate fix). **FURTHER CONTINUED by `~/Desktop/HandoffForAgents/2026-06-12_pi-experience-and-lab-funnel.md`** (finished Check-ins verification incl the D4 cross-user fix + expanded the demo to an 8-person lab; THEN the big new arc: PI Mode = redesign the whole lab-head experience "show the lab not the self" [proposal `docs/proposals/pi-mode-ia.md` + APPROVED review mockup `docs/mockups/2026-06-12-pi-mode-ia.html`, only RS-3/calendar rejected, UI NOT BUILT yet], and the lab-account funnel which is ~90% already built [create/invite/join/billing all wired behind `LAB_TIER_ENABLED`] with the 2 real gaps now CLOSED [PI-initiated invite flow `0857acffd`, real billing display `334aa158b`]; a controlled Chrome-agent test harness `docs/testing/lab-funnel-controlled-harness.md` was RUNNING at pause [relay :8787 + app :3002 on isolated worktree `~/Desktop/ROS-funnel-test`, billing-sim seeded -- TEAR DOWN when done]; surfaced + fixed a real Neon-HTTP DDL bug `74e9a4aa3`; KEY open thread = the two parallel lab systems, crypto/folder membership vs billing roster, need converging into one atomic enroll). (B) **`docs/orchestrator-handoffs/2026-06-13-embeds-phase7-complete-and-transclusion-crash.md`** (the markdown + ResearchOS embed system; ALL Phase 7 now built [P7-4 external/literature embeds + P7-6 mobile/a11y added], Preview now renders embeds, plus Grant-requested embed-UX [click no longer edits markdown + Edit-markdown button + Pin->Freeze rename + auto-newline-on-insert] and fixes [picker dup-key, public-method deep-link, editor setState-loop trap]; ONE CRITICAL OPEN BUG = a transclusion-only "Maximum update depth" crash on typing/opening `![[ ]]` [reproduces 100%, needs the full stack trace; inert-chip stopgap offered]. Prior embeds handoffs: `2026-06-12-embeds-phase6-7-handoff.md`, `2026-06-12-embeds-phase5-6-handoff.md`, `2026-06-12-markdown-embeds-handoff.md`). (C) **`docs/orchestrator-handoffs/2026-06-12-marketing-ai-demo-handoff.md`** (marketing redesign + cross-link IA split + AI surfacing + demo polish; on LOCAL main; THREE sub-bots were in flight at pause, see that doc to pull/verify/merge or re-dispatch). (D) **`~/Desktop/HandoffForAgents/2026-06-12_datahub-v2-and-wrangling.md`** (Data Hub v2 deeper phases change-params/right-click-CRUD/plot-code/subcolumn-formats/transforms/exclude-value ALL merged on local main, the data-wrangling TransformOp engine phases 1/1b/2 merged, ONE feature [lineage-aware code export] committed-but-unmerged on branch `worktree-agent-a312a152f5da0accc`, BeakerAI coordination, an open transform-icon decision). Prior: **`~/Desktop/HandoffForAgents/2026-06-10_regression-dogfood-and-login-redesign.md`**. Older: **`docs/HANDOFF_2026-06-04_orchestrator.md`**.

> **CURRENT STATE (2026-06-13, the Mobile UI redesign lane, RN REBUILD Phase 1 + most of Phase 2 MERGED to main).** The `mobile/` companion redesign is now BUILDING in code, not just designed, and is MERGED to local main + EMULATOR-VERIFIED. SHIPPED: Geist + Geist Mono app-wide, FLAT canvas, the floating 4-tab nav (Home/Notebook/Methods/Inventory) + center ＋ Capture with calc/timers/wiki in a Home hub, the new Home hub screen, all 4 tab roots migrated, hub-tool headers (calc/timers/wiki), core components on the new tokens (Button/Card/ScreenHeader/ThemedText + new `FloatingTabBar`), and the pushed screens which INHERITED the redesign via the shared components and were emulator-verified (Settings, Notifications, Pair w/ BeakerBot mascot, Quick note, Add purchase, Reorder, Lab calculators, Method-detail). Polish: Notebook live-pulse badge, calc-custom title de-dup, splash-deps fix. REMAINING (not pure restyle, need data or net-new): the 10 METHOD READERS (need published method fixture data on a paired device to verify; build per-type renderers to 03b), the SCAN FLOW + SMART-MATCH (camera + the net-new barcode-lookup feature + Vercel proxy), the ACTIVE-EXPERIMENTS BAND (companion lane's HELD branch, keep `recordSnapshotGeneratedAt` wiring), and minor capture-tile polish. SELF-VERIFY HARNESS proven: worktree metro :8082 -> emulator (`emulator-5554`) + `mobile/scripts/device-shot.sh` + deep-link nav (`adb shell am start -d researchos://<route>`); Grant's Samsung untouched on :8081. **CAUTION for parallel lanes: coordinate before large `mobile/` changes.** Build branch `mobile-redesign-foundation` (merged). Plan/status: `docs/proposals/2026-06-13-mobile-ui-contract-plan.md`. Design contract (docs): front door `docs/mockups/mobile-contract/00-index.html`. The TWO approved in-flight features (recipe check-as-added; SMART PACKAGE SCAN = own-data match + GS1/barcode parse + external lookup) remain to build.

> **CURRENT STATE (2026-06-13, the Billing build-out lane, payer cascade LANDED + an open auth decision recorded).** On LOCAL main, gate-verified. Full briefing: **`docs/orchestrator-handoffs/2026-06-13-billing-buildout-handoff.md`**. LANDED: the **org-tier payer cascade** (`4a1ac1264`), a non-gated resolution layer walking member -> lab -> dept -> institution to decide who pays; pure resolution, no UI surface, sits below the tier features. With all four org tiers (Member/Lab/Dept/Institution) now built and dark behind `DEPT_TIER_ENABLED` / `INSTITUTION_TIER_ENABLED`, the only remaining tier work is Phase 3 billing (self-serve plan builder wired to a Stripe RECURRING PROCUREMENT INVOICE), still GATED on Grant's Stripe products + the WI sales-tax question, same gate as lab go-live. **OPEN, UNBUILT DESIGN DECISION (dept/inst portal auth, decided this session, none built):** one mandatory root OAuth recovery account (the un-loseable key, exactly one) + a resettable shared ACCESS CODE as the everyday door (hand around, lab password manager, reset on departure); DROP the multi-OAuth-with-approval idea (later option = in-portal allowlist where being-inside-is-the-approval). Code stored hashed + shown once + rate-limited (reuse the sharing-identity show-once recovery-code prior art); consider gating highest-stakes actions (change plan/billing, rotate recovery account) to the root OAuth and leaving the code for roster/invite/usage; total loss of both = support-mediated out-of-band ownership verification, optional one-time backup codes at creation. Slots into `/department` + `/institution` entry when built. Memory: `project_dept_institution_tier`.

> **CURRENT STATE (2026-06-13, the Notifications / companion-push lane, FULLY CLOSED, phone buzz now witnessed on a real device).** All on LOCAL main, gate-verified (tsc 0, icon-guard clean). Every companion + notification thread from this session is built, merged, and witnessed, including the literal phone buzz on Grant's paired Samsung (the last open item, confirmed live 2026-06-13). SHIPPED. (1) PHONE PUSH P1/P2/2.5/P3 fully built, the Cloudflare DO relay deployed, the one-command test harness green 10/10 (`npm run test:notify`, `e3d1bf2f9` + `ad5650df0`; P3a sender-triggered shift-alert `64a16b1c5`, P2.5 sender-triggered email `27cd2b6e3`, P3b scheduled calendar-reminder DO alarm `69ba28a66`). Phone-push P1 is the laptop-open wake-and-fetch buzz with a generic content-free payload (see memory `project_notification_preferences`); it stays INERT until Grant redeploys the relay and cuts an EAS dev build. (2) EMAIL inbox-verified, LAPTOP pop-up verified. (3) The DEMO-SYNC BUG is FIXED (`1a5ddf3aa`, demo companion pairing + relay sync now survive a refresh) which was the last thing blocking the buzz test. (4) COMPANION toggles surfaced in main Settings (`e472ca9f1`), phone notification routing mirrored in the Companion Settings tab (`20383a758`), pairing-card live freshness + tap-to-sync (`c03661d34` + `6f3058642`), single connection-cue consolidation (`901624ca1` + `eb8e40287`, unified TabHeader `231023b18`), the notifications Settings section now uses a bell not a warning triangle (`aeb74c783`). (5) A permanent "Send a test notification" button in Settings (`7d68126af`). (6) The no-account Connect gate is now a free-account conversion moment with the six-capability screen in the `status === "none"` branch, dev buttons demoted (`56934ca16`, merged `1a07c124c`; this was a cross-lane chip from the Companion-connect conversion lane, verified on main and that lane closed). THE PHONE BUZZ IS NOW WITNESSED (2026-06-13): Grant ran Settings -> Notifications -> "Send test" on his paired Samsung and the real push landed (the "Notifications" channel showing "Reminder / Test notification / just now"). End-to-end path confirmed LIVE on a real device (createEventReminder -> headless watcher -> relay publish -> phone buzz). THE LANE IS FULLY CLOSED, every item built, merged, and verified including the literal phone buzz. Memory: `project_notification_preferences`.

> **CURRENT STATE (2026-06-13, Data Hub /wiki/stats screenshots, the Data v4 session).** All on LOCAL main, UNPUSHED, NOT browser-verified by Grant (pages render 200 on his :3000 + every shot eyeballed during capture). 3 commits: `fdf6ba17f` + `3a41d4fa6` + `8357cf250`. SHIPPED: all 9 `/wiki/stats` explainers now embed a real Data Hub result screenshot (the BeakerBot interpretation box + the result table), closing the last optional-polish item from the Data Hub v2 COMPLETE arc. 10 new `frontend/public/wiki/screenshots/datahub-stats-*.png` captured via the existing Playwright harness `scripts/capture-wiki-screenshots.mjs` by DEEP-LINKING to a saved fixture analysis (`/datahub?doc=<id>&analysis=<id>` + `&wikiCapture=1`; `page.tsx` reads both params on first load, `ResultsSheet` recomputes live so a `resultCache:null` spec renders fine). 5 explainers were already covered by the 7 existing fixture analyses (effect-sizes/anova/linear+multiple-regression/dose-response); the other 5 (roc-auc/outliers/survival/contingency/repeated-measures) needed NEW fixture specs in `wiki-capture-fixture.ts`: a `rocCurve` on table 5 + FOUR new datahub tables 8-11 (Grubbs column w/ a planted 11.8 outlier, two-arm survival, 2x2 contingency, within-subject repeated-measures) + bumped the `datahub` counter 3->11. Capture ran against the leftover Data-v2 `:3001` rig, which is now FULLY TORN DOWN (Preview server stopped, worktree `/tmp/ros-datahub-testserver` removed, branch `datahub-testserver2` deleted, port free, launch.json entry removed). To re-capture: start a DATAHUB-enabled non-prod server, then `WIKI_CAPTURE_BASE_URL=http://localhost:<port> WIKI_ONLY=datahub-stats node scripts/capture-wiki-screenshots.mjs`. REMAINING Data Hub items are all OPTIONAL non-blocking polish (recipes + info-sheet browser-verify, figure-style-in-recipes, info-constants-in-analyses, ROUT-if-asked); the suite is otherwise done. Memory: `project_datahub_v2_stats.md` updated.

> **CURRENT STATE (2026-06-13, phylo published-tree reproduction framework BUILT, the phylogenetics lane).** On LOCAL main, UNPUSHED, gate-verified in an isolated worktree (tsc 0, transparency+phylo vitest 732 pass / the new published gate SKIPS until a run lands, icon-guard 0); merge `8cf39d8db` (commit `b0001fd43`). Full briefing: **`docs/orchestrator-handoffs/2026-06-12-phylogenetics-handoff.md`** (the "Published-tree reproduction" section now reads FRAMEWORK BUILT). The second half of the phylo transparency story (ggtree domain = our render matches ggtree; this = the Builder's GENERATED recipe, run offline on a real paper's input, recovers that paper's published tree, scored by Robinson-Foulds + percent clades recovered). SHIPPED: `lib/transparency/datasets/phylo-published.ts` (cases + readiness + `comparePublishedCase` consuming `lib/phylo/rf.ts` + per-case RF `tolerance`), `datasets/phylo-published/<case>/` verbatim sources, the `phylo-published` transparency domain in `run.ts` + `published-tree` oracle, the `phylo-published` visual + `PhyloPublished.tsx` (side-by-side trees via the baselined `renderTreeSvg`), `phylo-published.gate.test.ts` (skips until `anyCaseReady()`), and `scripts/run-phylo-published-case.sh` (offline run helper). CASE DATA: **HPV58 = fully sourced + READY** (90 GenBank complete genomes via efetch, headers matched to the seed tree's tip labels; published tree reuses `HPV58_NWK`); **turtle** = alignment + 29-gene partition sourced verbatim (IQ-TREE workshop), published tree is a TreeBASE sourcing TODO (`publishedNewick` null, inactive); **firefly opsin** = stubbed for Grant's Dryad download (Dryad blocks scripts). TO ACTIVATE: Grant runs `frontend/scripts/run-phylo-published-case.sh hpv58` offline + commits the result; each case lights up its /transparency section + the gate independently when its files land. **UPDATE 2026-06-13 (commit `6a5940aee`): HPV58 ACTIVATED + the gate is now SUPPORT-AWARE.** Grant ran HPV58 (IQ-TREE 2.2.0); it recovers EVERY well-supported published clade but raw normalized RF is 0.287 (all 25 differing clades are support < 70, near-identical-genome low-support churn). So the gate now implements Grant's locked decision #1 = a case passes when it misses NO published clade with bootstrap support >= 70 (Hillis & Bull 1993; Grant picked 70 over 80 as the literature-backed cutoff), NOT a raw-RF threshold. `rf.ts` reports `missingFromOursSupport`; `reproductionVerdict()`+`WELL_SUPPORTED_CUTOFF=70` in `phylo-published.ts`; page shows raw RF + percent recovered + every differing branch with support. tsc clean / 472 vitest pass incl the live HPV58 gate / icon-guard 0. The `-T AUTO` footgun is also ADDRESSED (commit `df758cd6b`): default stays AUTO but the generated recipe warns + suggests `-T 4` on small alignments (reaches the wizard + BeakerBot's generate_tree). The `/phylo` WIKI PAGE is also DONE (commit `8b805a3f0`, `wiki/features/phylo`, wired into nav+search; 6 screenshots pending capture via `?wikiCapture=1`, they render as pending placeholders meanwhile). **UPDATE 2026-06-13 (commit `dc63e55d4`): TURTLE DROPPED -> CRAUGASTOR supermatrix case.** The Chiari turtle tree is not deposited as a verbatim Newick anywhere (figure only), so it could not be an RF case; replaced with a real partitioned supermatrix from TreeBASE S10103 = Streicher et al. 2009 Craugastor frog complex (47 taxa x 1658 bp, 4 partitions, matrix + Fig. 2 tree from the same submission, labels match; format-conversions only). Also landed the deferred recipe.ts `have:"alignment"` supermatrix branch (skips AMAS -> `iqtree2 -s input_alignment.fasta -p partitions.nex`), the shape BeakerAI's fixture harness now asserts (they repointed turtle->craugastor; suite green). **UPDATE 2026-06-13 (Grant ran craugastor, commit `6f86e0879`): CRAUGASTOR ACTIVATED + support-less gate is RECOVERY mode.** It recovered 33/34 published clades (97.1%), resolving 11 polytomy nodes; the Fig. 2 tree has no support AND polytomies, so the support-less gate passes when the fraction of published clades recovered >= `recoveryFloor` (0.9), NOT symmetric RF (which unfairly counted our resolving the published polytomies as 11 "extra" clades, normalized RF 0.1364). Verdict has two modes SUPPORT (hpv58) / RECOVERY (craugastor); case declares `supportCutoff`/`recoveryFloor`; gate mode-agnostic. **BOTH hpv58 + craugastor LIVE + passing** (505 tests, tsc 0, icon-guard 0). STILL OPEN (published-repro): only the opsin Dryad files + the over-conserved smoke FASTA. BeakerAI's generate_tree contract pinned (catalog-miss fallback = nearest+factual-note, raw-FASTA single-locus binding, supermatrix have:alignment shape landed). **SEPARATE ARC NOW IN FLIGHT: ggtree-class viz.** Proposal `docs/proposals/2026-06-13-phylo-ggtree-class-viz.md` (Data Hub is our ggplot2; unlock = tree drives a panel's row axis; Phases 0-3). Phase 0 (continuous scales + value heatmaps + legends + heatmap UI + circular rings) MERGED `331793664`. **Phase 1 MERGED `3468fec20` (recovered re-merge of sub-bot `37ddbf880`): LAYERS control model + aligned-panel framework** = the figure is now an ordered `panels[]` stack (additive, back-compat, old figures project to default layers), new `TipAxis` (layout.ts) + pure `panel-render.ts` framework (column/ring aligned tip-for-tip), Phase 0 tracks consolidated onto it, geoms heat(matrix)/bars/dots/box/labels/points/strip/clade/support, `PhyloLayers.tsx` UI (reorderable layer list + per-layer inspector + searchable categorized Add menu + Start-from templates) replaces the toggle wall, ggtree export walks panels[]. Gate: tsc 0 / 785 tests / icon-guard 0. A fix pass `c5fea245a` (sub-bot) cleared 4 browser-test defects (reorder setState-in-render, categorical color/legend for un-baked columns, tip-decoration legends + toggle, UX scroll/template-ID/labels-default/circular-clip), 794 tests. **Phase 1 + fixes BROWSER-VERIFIED by Grant 2026-06-13 (Claude-in-Chrome, all 4 fixes PASS + regression sweep PASS + console clean through drag).** Known minor: a template-apply render flicker (correct end state) to watch in Phase 2; an InboxBadge `isReady` crash the test saw was a transient stale-build artifact (no live `isReady` in source, tsc clean), not a real bug. Control-model decided via the approved layers mockup `docs/mockups/2026-06-13-phylo-control-model.html` (research `docs/research/2026-06-13-phylo-control-model-ui.md`). **Phase 2 MERGED `0ccaacb91` (atomic; sub-bot, gate tsc 0 / 809 tests / icon-guard 0): Data Hub-class statistical panels aligned to tips** = violin (per-tip kernel density), point+error (lollipop, sd/sem, value+`errorColumn` or replicate mean/sd), scatter (jittered replicate points), each a ring/column against the TipAxis with a niceTicks value axis + legend. Reuses Data Hub primitives READ-ONLY (quantileSorted/niceTicks/palettes); `lib/datahub/` untouched (design refinement = render phylo-side, do NOT inject into the Data Hub renderer; cross-lane clean). Additive optional `AlignedPanel.errorColumn` + violin/point/scatter kinds. Spec `docs/proposals/2026-06-13-phylo-phase2-build-spec.md`. Phase 2 browser test: Violin PASS (renders ring+column, value-axis toggle, console clean); Point+Scatter pending a continued Chrome run (first run rate-limited). **Phase 3 MERGED `0398c90c7` (atomic; sub-bot, gate tsc 0 / 837 tests / icon-guard 0): msaplot alignment track + multi-panel/legend polish + template-flicker fix.** msa geom = aligned-FASTA import (reuses tip-label join) -> nucleotide/AA residue matrix ring/column with column binning for wide alignments (HPV58 90x7884 -> 564 bins, 90/90 matched), residue legend, `msaplot()` ggtree export; alignment carried as in-memory `RenderSpec.msaTrack`/`FigureInputs.alignment` (no sidecar/panels[] change). Legends columnize at 4+ panels; per-panel titles; wider panel gap. Flicker fixed (controlled-select snapback -> `appliedTemplate` state). Spec `docs/proposals/2026-06-13-phylo-phase3-build-spec.md`. KNOWN POLISH QUEUED (from the Phase 2 browser test, batch after Point/Scatter results): (1) circular distribution panels (violin/point/scatter) show only a guide ring, no numeric tick labels or value legend (rectangular shows the axis) -- add a value scale key in circular; (2) responsive single-column collapse + the demo floating buttons overlap/clip the Layers+inspector panel at some widths. DEMO RESEED still HELD (Grant confirms; edits committed seed). HANDBOOK still parked (final step). HANDBOOK = a wiki treedata-book is a PARKED TODO for a final step (`docs/proposals/2026-06-13-phylo-handbook-plan.md`, curated attributed example data, do it LAST). DEV TEST CORPUS at `~/Desktop/ggtree-testdata/` is LOCAL-ONLY, never commit/push. **SHARED-TREE LESSON (cost me a sweep this session): on the contended main checkout NEVER leave a turn-length gap between `git merge --no-commit` and `git commit` — a concurrent session's commit resets the index and your later commit no-ops ("no changes added"); chain merge + foreign-bleed-check + commit in ONE bash call, or use atomic `git merge --no-ff <ref> -m ...`. If swept, the branch's commits survive as objects (re-merge by SHA even after the branch is deleted).** **UPDATE 2026-06-13 (Tree Studio v3 UNIFIED RAIL + cross-page rail dedup; all LOCAL main, browser-verified in demo).** The two QUEUED polish items are FIXED: circular distribution panels now emit a numeric scale-key legend (`renderValueScaleLegend` + `distributionDomain` in `panel-render.ts`, collected in `render.ts` `collectPanelLegends`) and the responsive/demo-button clipping is gone (commit `156f9e1f0`; later SUPERSEDED at the source by the shared split shell below). Point+Scatter browser-verified (adds/binds/whisker/jitter, both layouts, console clean); one deferred finding = on Point+error with an explicit `errorColumn` the SD/SEM selector is a no-op (error taken verbatim), so that control should collapse to From-column/None in that mode. **THEN the v3 redesign Grant approved (interactive mockup `docs/mockups/2026-06-13-phylo-v3-unified-rail.html`, 8 decisions):** Tree Studio's left is now the COLLECTION RAIL recycled from Sequences/Chemistry (`PhyloCollectionRail.tsx`, commit `bad914dcf`) and its right is a TABBED ACTION RAIL reusing `components/sequences/SequenceOperationsRail.tsx` (tabs Layers/Setup/Export/Code; the old Tree/Metadata/Alignment left controls moved into Setup); the Phylo Hub saved-trees grid is RETIRED (trees live only in the rail); `/phylo` studio view is full-height (commit `6c7823e06`). BeakerAI shipped PDF-reproduce Output 4 `match_figure_style` against the stable `PhyloFigureSpec` + `phyloApi` + `?doc=` contract (NOT PhyloStudio internals); hydration seam re-verified clean post-refactor. **THEN Data Hub got the same shell** (commit `c9ba74bf5`): its fixed-232px rail is now resizable + collapse-to-focus + has a type-to-filter, family-tree nesting untouched (mockup `docs/mockups/2026-06-13-datahub-unified-rail.html`). **THEN the dedup (commit `286cd8e93`): ONE shared `components/SplitShell.tsx` (`useSplitShell` hook + `SplitDivider` + `RailReopenButton`) now backs ALL FOUR data-page rails (sequences, chemistry, datahub, phylo).** CANONICAL going forward: a new data-page rail uses `useSplitShell(<per-page localStorage key>)` + `<SplitDivider>`/`<RailReopenButton>` -- do NOT re-copy the divider/persist plumbing; the pure clamp math stays in `lib/sequences/split-layout.ts` (unit-tested). The hook persists on drag-end/keyboard-commit, NOT a `[width]` effect (that pattern carries a React-StrictMode restore bug, now fixed for all four). Sequences keeps its bespoke edge-tab re-open pill + sky-400 divider + modal-aware Escape-exit-focus, wired to `shell.setCollapsed`. All gate-green (tsc 0, 199 phylo/datahub/split tests) + four pages browser-verified. RELAY NOTE (Grant 2026-06-13): inter-lane messages now go via the CCD `send_message` tool (not printed for manual relay), still To:/From: + signed. **UPDATE 2026-06-13 (ggtree COVERAGE EPIC Waves 1+2 SHIPPED + plot artboard WISH; all LOCAL main, UNPUSHED, tsc 0 / ~199 phylo tests green at each commit; full briefing `docs/handoffs/2026-06-13-phylo-coverage-unified-rails-artboard.md`, memory `project_phylo_ggtree_coverage` + `project_plot_artboard`).** Grant's directive: broaden Tree Studio toward the FULL ggtree/ggtreeExtra/aplot surface, make every toggle EXIST (render + figure-spec via the loose `AlignedPanel.options` seam + the PhyloLayers inspector + ggtree export); the settings-UI organization is a DEFERRED separate audit (Grant), not a blocker. **Wave 1 (`187042fab`)** = tip-label options (font size, boxed `geom_label`, color-by-trait, italic default on). **Wave 2 COMPLETE** (all clade-level, built on a new MRCA model): branch coloring by trait (`aes(color=)`, paints monophyletic clades, `4304662d8`); the headline **MRCA finder + multiple clade highlights** (`6537580d7`, `mrca(root, tipNames)` in `layout.ts`, name the tips and the clade root resolves so you never hunt a node on a big tree, both layouts incl circular which was previously deferred); clade labels / brackets (`geom_cladelab`, `18690c84c`); collapse-to-triangle (`geom_collapse`, reshapes the tree pre-layout, rect fan + circular wedge, `8fe26b845`); rotate/flip (`rotateNode` + a Setup-tab "Rotate a clade by members" using MRCA, `deb5a8448`). All clade features share ONE `CladeInspector` (color/label/style/collapse/members-by-tip-name). DATA-SHAPE FLAGGED (additive, back-compat): `PhyloFigureSpec.branchColorColumn?`; all other clade state lives on `options` (no new on-disk field). CHROME-VERIFY TAIL (Grant drives, one item not self-verified): add a Clade-highlight layer, pick 2+ members WITH A REAL MOUSE (a synthetic select keypress does not commit), confirm MRCA highlights the clade in both layouts, then Style->Bracket / Collapse->triangle / Setup-tab branch-color + rotate-a-clade, and check the Code tab reflects each toggle. BACKLOG = Wave 1B (tip-label align leader-lines, node/root points, scale-bar toggle), Wave 3 (slanted/fan/unrooted/inward-circular layouts), Wave 4; plus a small UX cleanup (Point+error with an explicit `errorColumn` makes the SD/SEM selector a no-op, it should collapse to From-column/None in that mode). **NEW WISH, design-stage, NOT built: a plot ARTBOARD / page frame** = an Illustrator-style publication-page canvas behind ANY plot so the figure is sized in REAL units inside a chosen paper (the artboard IS the export dimension, so the exported SVG is publication-exact instead of today's fixed 620x460 px abstract canvas), SHARED across Tree Studio + Data Hub + any figure surface (build it once as a shared component like the SplitShell dedup). Interactive mockup `docs/mockups/2026-06-13-plot-artboard-page-frame.html` (paper presets incl journal column widths, orientation, rulers, room/overflow feedback, export-dims readout, 5 decisions to approve, the crux being the real-unit scaling model). Awaiting Grant's review of the 5 decisions; likely the next BUILD since it serves both pages at once.**

> **CURRENT STATE (2026-06-13, markdown embeds: Phase 7 complete + embed-UX pass + transclusion crash RESOLVED, the embeds lane DONE).** UPDATE (2026-06-13 takeover session): the "ONE CRITICAL OPEN BUG" described below is RESOLVED, it was a STALE-BUILD GHOST. A Claude-in-Chrome capture forced the live `TransclusionEmbed` mount in a LORO-bound editor (`LORO_PILOT_ENABLED=true`, handle opens for demo notes) with a non-cyclic section on a freshly rebuilt server, and saw NO "Maximum update depth" loop, the exact CM6-widget+Loro path blamed below (so if it were a live bug, forcing the path reproduces it; it did not). The genuine latent loop trap was already fixed in `f3d11822b`; the old "reproduces 100%" reading was a stale/concurrent dev build. A temporary inert-chip stopgap + `?liveTransclude=1` dev harness + a seeded `/demo` repro note were built then FULLY REVERTED to baseline (commit `b017866c0`; the 4 commits `b923f93ca`/`99d0d73a4`/`7f6696873`/`b017866c0` net to byte-identical-to-handoff MINUS the crash). Live editor transclusion is the intended default again. tsc 0, 219 embed/cm/transclusion/fixture suites green, LOCAL main UNPUSHED. ONLY residual = a belt-and-suspenders real-folder transclusion sanity check; if clean the whole markdown-embeds lane is CLOSED. Full writeup: the RESOLUTION banner at the top of the handoff doc. The original open-bug text follows. All on LOCAL main, UNPUSHED, gate-verified (tsc 0, embed/cm/references suites green). Verify with `git merge-base --is-ancestor b017866c0 main`. Full briefing: **`docs/orchestrator-handoffs/2026-06-13-embeds-phase7-complete-and-transclusion-crash.md`**. SHIPPED: Phase 7 FINISHED (P7-4 external/literature embeds [DOI/PubMed cite cards via Europe PMC, PubChem-CID + bare-SMILES structure cards, URL link preview; new `lib/embeds/external-embeds.ts` lane] + P7-6 mobile/a11y pass); **Preview now renders embeds** (was plain ReactMarkdown -> now the embed-aware `RenderedMarkdown`, `a39ed6325`; this was the real cause of an early "embeds don't render" report -- they had tested Preview mode). Grant-requested embed-UX: **clicking an embed (incl the molecule SVG) no longer edits the markdown** (`b450d82cf`, ignoreEvent + mousedown preventDefault on non-controls) + an **"Edit markdown" button** + **Pin->Freeze rename** (display copy only, persisted `#ros=pin` fragment unchanged) + **block embeds auto-drop onto their own line on insert** (`e2e739eb0`, render as a card immediately). Fixes: picker duplicate-key crash (`da5278d71`, private vs public method id collision), public-method references now resolve to the public method (`98f2697f8`, `public:` id prefix, closes `task_5632a884`), and an editor broken-image-scan setState-loop trap (`f3d11822b`, closes `task_84b7adfc`). CONFIRMED by Grant on :3000: click-safety, Freeze rename, `## heading` persistence. **ONE CRITICAL OPEN BUG**: a TRANSCLUSION-only "Maximum update depth exceeded" crash -- typing `![[Note#Heading]]` OR opening a saved note with a `[H](/notes/<id>#ros=transclude&section=H)` link loops in `NoteDetailPopup.updateEntryContent` via the `InlineMarkdownEditor` updateListener (`~:642`). The normalize itself works (the link generates + persists correctly); the RENDER path exposes a loop now that transclusions reach the rendered state in the CM6 editor for the first time. `TransclusionEmbed` is clean; the loop is the CM6-embed-widget/Loro interaction, not pinnable statically + not headlessly reproducible. NEXT STEP = capture the FULL stack trace (reproduces 100% on opening the note; prompt in the handoff) then fix at source; an inert-chip stopgap (render transclude as a plain chip in the editor, live section only in Preview) was offered to kill the crash meanwhile. STILL OPEN chip `task_46c70900` (SMILES-direct RDKit import for the structure-card Add-to-library stub). LESSONS reaffirmed: COW-worktree node_modules silently no-ops jest-dom matchers (HIDES real test failures -- use a SYMLINK or run on real main); a stale dev build made the same crash look gone-then-back (hard-refresh before trusting a verify); re-run tsc AFTER writing tests; commit IMMEDIATELY after `git merge --no-commit` + the `--cached` foreign-bleed check (a concurrent commit absorbed one staged merge again this session).

> **CURRENT STATE (2026-06-12 late, demo-video recording + editing + placement, the demo-recorder lane).** Full briefing: **`docs/orchestrator-handoffs/2026-06-12-demo-recording-edit-and-placement.md`**. All code on LOCAL main (gate-verified tsc 0). The SEVEN demo clips (`frontend/src/lib/demo-video/scripts.ts`: chemistry, datahub, sequences, purchases, sequencesNcbi, chemistryGliotoxin, checkins) are ALL recorded by Grant and edited; the trimmed videos live in `~/Desktop/FinalRecords/*-trimmed.mp4` (NOT in the repo). GRANT'S PICKS: gliotoxin (not caffeine) for the chemistry slot; BOTH sequence clips (editor/Gibson + guided-NCBI) are keepers. REMAINING = video placement (NOT started): the welcome page (`WelcomePage.tsx`) references videos by **Vercel Blob URL**, so shipping = (1) Grant uploads the trimmed mp4s + posters to Blob, (2) next session wires the `<video src>`/poster URLs + decides the lineup (2nd seq clip placement open). **MUST PUSH: `d071e4fc7`** fixes the prod build that FAILED on commit 79e2aa4 (AI-helper privacy guard rejected the seeded `remy` persona; added remy/nia/theo/ivy to `ALLOWED_FIXTURE_OWNERS`) — push local main + redeploy. This session also shipped many record-time fixes (literature explorer now the default full-page inline view; nav flattened + routes/literature prewarmed in record mode; BeakerSearch new-bar-shown/old-pill-hidden in record mode; NCBI over-cap Datasets escape; pEGFP-N1 enriched to 15 features + 3 primers + a completed Gibson in the seq clip; purchases stage+misc seeding for alex+morgan; datahub clip runs the guided wizard + shows the BeakerBot verdict; companion Maestro flows in `mobile/.maestro/`). STANDING HAZARD: the demo fixture generator is drifted from its two committed outputs — NEVER run a full `generate-demo-data.mjs` regen (it wipes nia/theo/ivy + binary PNGs), edit all three places surgically; keep `build-ai-helper`'s allowlist synced with the demo roster. Two Chrome-test prompts in `docs/test-prompts/2026-06-12-*`.

> **CURRENT STATE (2026-06-12, marketing brand-polish + privacy hardening + OAuth/ORCID + mobile, a DIFFERENT parallel session).** All on LOCAL main, UNPUSHED, gate-verified (tsc 0, icon-guard clean, targeted vitest green); NOT browser-verified by the orchestrator (Grant runs his own `:3000`). SHIPPED: ORCID lab-create now recovers via email-OTP instead of stalling (`12330eee6`); `/thanks` open-source credits are now hyperlinks to each project (`a02d2d0a0`); public marketing pages readable on ANY browser + mobile (the unsupported front door renders `WelcomePage` in a read-only mode instead of the dead-end `BrowserNotSupported`, added the missing viewport meta, `MarketingNav` got a mobile menu; `580dc2030`); off-brand button normalization on chemistry/sequences/datahub (raw `bg-sky-*`/`bg-indigo-*` + the diagonal brand gradient -> canonical `bg-brand-action`, 31 files, `c824539ad`); the **check-ins + mentoring** welcome feature section with a multi-level mentorship-tree visual built from divs + scoped styled-jsx, no inline SVG (`4e8276e2e`). PRIVACY (the repo is PUBLIC): removed two pushed docs that named the owner's bank/brokerage/ACH (`edbf26970`), added a no-business-info rule to this file (`38a4373af`) + a `privacy-guard` pre-commit hook (`f0c861b97`, blocks EIN/SSN + a gitignored `.privacy-denylist`, wired into `install-git-hooks.sh`). Business/financial/personal/legal-strategy info now lives ONLY in `~/Documents/ResearchOS_LLC/`, never in-repo; the full cleanup + remaining doc-scrub + git-history-purge plan is `~/Documents/ResearchOS_LLC/repo-privacy-cleanup-plan.md`. BRAND: locked the rainbow rule (pastel `--brand-rainbow` for decoration, vivid `--brand-rainbow-vivid` for gradient TEXT and as the dark-mode rainbow; never a generic red/pink rainbow), recorded in `docs/branding/brand-rulebook.html`. IN FLIGHT (mockup-review-first, awaiting Grant): a 10/10 pricing trust-band redesign (`docs/mockups/2026-06-12-pricing-trust-band-redesign.html`, bento + animated mechanic graphics) and an `/about` redesign (`docs/mockups/2026-06-12-about-redesign.html`, founder story humanized via the `ai-humanizer` skill + Grant's real portrait from gnick18.github.io, still needs a candid lab shot). APPROVED + build-queued: the **brand-unification rollout** ("equal polish on every public page") = Foundation (extract shared `Kicker`/`RainbowFrame`/`FeatureRow` from `WelcomePage`/`AIPage` into `components/marketing/`), then mechanical polish passes on /about, /terms, /privacy, /transparency, /thanks, /open-source, and the /pricing hero, per the page-polish audit (welcome 9.5 is the bar; /about + legal pages lowest). Parked mid-rollout 2026-06-12. NEW MEMORY this session: `feedback_no_business_info_in_public_repo`, `feedback_brand_button_tokens`, `feedback_brand_pastel_rainbow`, `project_orcid_lab_email_fix`, `project_public_pages_mobile_anybrowser`.

> **CURRENT STATE (2026-06-12, Data Hub v2: THE ENTIRE ARC IS COMPLETE, the Data v2 session).** On `origin/main` (sibling pushes swept it up). Full briefing: **`~/Desktop/HandoffForAgents/2026-06-12_datahub-v2-COMPLETE-handoff.md`**. The Data Hub v2 deeper-phase list is DONE. (1) STATS ENGINE, all 4 themes (Theme 1 estimation, Theme 2 dose-response+regression, Theme 3 survival+repeated-measures = Cox PH `coxRegression`/Gehan-Breslow-Wilcoxon/RM-ANOVA/linear mixed model, Theme 4 diagnostics = Grubbs/ROC+AUC/QQ+residual+ROC plots), every stat validated vs scipy/statsmodels/lifelines/pingouin/sklearn with a `/transparency` pin + show-code (the hard gate; `datahub-stats.gate.test.ts` enforces `comparisons.length === STAT_PINS.length`), HUMAN-VERIFIED, plus 7 QA fixes re-verified. ROUT DEFERRED (GraphPad-specific, Grant has no Prism). (2) THREE NEW TABLE TYPES, all HUMAN-VERIFIED 22/22: `contingency` (chi-square/Yates/Fisher/OR/RR), `nested` (nested t-test reusing the mixed-model engine + nested one-way ANOVA), `partsOfWhole` (pie/donut/stacked, the only descriptive gate-exempt type). (3) `info` Info sheets (markdown documentation + constants in the rail, additive `DataHubDocContent.info`, no analysis). (4) BeakerBot interpretation box (`9fac5ba73`, every result gets BeakerBot's plain-English read + a "For your numbers:" worked example + a per-type "Learn more" link). (5) `/wiki/stats` (`e674ddf60`, a hub + 9 plain-English explainers, the BeakerBot Learn-more links re-pointed there). (6) Saved analysis recipes (`8061eb052`, "Analysis recipes": save an analysis type+params, replay on a new table). BeakerAI relay FULLY CLEARED (they own `src/lib/ai/tools/*`, consume our read-only engine). LEFT (all optional polish): human-verify recipes + info sheets, richer wiki screenshots, figure-style-in-recipes, info constants usable in analyses, ROUT if a user asks. GOTCHAS: shared root is a contended TREADMILL (commit can say "no changes" because a sibling absorbed your staged merge, verify with `git merge-base --is-ancestor <sha> HEAD`; extract foreign-staged files with `git reset --soft HEAD~1`+`git restore --staged`); sub-bots stall/throttle but commit early so salvage from the worktree; `public/demo-data/.../datahub/*.loro` UNTRACKED (git tracks only `.json`, sidecar rebuilds). The `:3001` human-verify rig may still be live (`/tmp/ros-datahub-testserver` + `~/Desktop/researchos-datahub-test`); tear down per the handoff.

> **CURRENT STATE (2026-06-12, UI polish: global-nav build + icon audit + folder-connect + demo-gate, the UI-explorers session continued).** On `origin/main` (a sibling session pushed the shared checkout) EXCEPT the last commit `093ef2776` (demo-gate fix, local-only). Full briefing: **`~/Desktop/HandoffForAgents/2026-06-12_ui-polish-nav-icons-folderconnect.md`**. SHIPPED: the **global-nav slimming build** (`b1f7fe1f2`, slim header + More overflow + drag-customize `UserSettings.navLayout` inline/More split; new `AppNavBar.tsx` + pure `nav-layout.ts`; pointer-drag + responsive auto-overflow) — DONE and BROWSER-VERIFIED by Grant via the Claude-in-Chrome extension (persistence + tinted-header both PASS, plus a headless verifier pass, 0 bugs). A full **icon de-collision audit** (66 used glyphs / 437 usages) splitting overloaded glyphs under the new ONE-GLYPH-PER-MEANING rule: 10 new glyphs `labTree`/`storageNested`/`calculator`/`move`/`lineage`/`resize`/`library`/`reference`/`results`/`growth` (`de8a3006f`+`656a1a160`+`fffba1626`), reuse fixes (hourglass-for-stale, eyeOff-for-IDP-privacy, layer-for-IDP-plan, chart-for-Results), rule recorded in this file + the `registry.tsx` header (`cde26dcb8`+`1d74ba409`). The **folder-connect dropzone redesign** (`f211cd810`, drag-and-drop is now the hero of `FolderConnectGate`, browse demoted; deduped the repeated cloud-slow copy; added a `matchMedia` jsdom polyfill that fixed the pre-existing-red FolderConnectGate suite). The **demo-mode migrate-to-solo gate fix** (`093ef2776`, `MigrationGate` is suppressed under `getDemoMode()` because the demo fixture is a multi-user lab). RELAYED not built: the BeakerBot-mascot-for-vial swap on the AI surfaces (`docs/proposals/beakerbot-mascot-icon-swap.md` `776cc2c6a`, handed to the BeakerAI lane). NEW PREFERENCE (in this file `8c95dcbcc` + memory): for MOUSE-driven testing hand Grant a self-contained Claude-in-Chrome prompt, not orchestrator Preview/Playwright (synthetic events miss drag move-thresholds). GRANT'S PENDING (browser-only): Check-ins Phases 2-4 real-data flows (headless render passed 9/9; scratch folder `~/Desktop/researchos-checkins-test` + a prompt are ready), and the folder-connect dropzone visual eyeball. Aside: Grant's macOS native folder picker hangs because Box/OneDrive/iCloud File Provider extensions stall the NSOpenPanel; dragging a folder sidesteps it (hence the redesign).

> **CURRENT STATE (2026-06-12 evening, markdown embeds Phase 6 completion + Phase 7 polish arc).** All on LOCAL main, UNPUSHED, gate-verified (tsc 0, the embeds/sharing/cm suites green, icon-guard clean), NOT browser-verified beyond loopback + unit tests. Verify with `git merge-base --is-ancestor bb7b8b73f main`. Full briefing: **`docs/orchestrator-handoffs/2026-06-12-embeds-phase6-7-handoff.md`**. SHIPPED: Phase 6 share-with-dependencies is now COMPLETE (6d no-access placeholder `9cbec0a12`, 6c recipient destination picker `0f65c33f1`, a 6c "import a fresh copy" dup override `7e595fed2`). Phase 7 was scoped (proposal + interactive decisions mockup `816d0569c`, Grant approved option A in the recommended order) and FOUR sub-phases built: P7-3 in-place view switching (persists in the editor via a fragment rewrite, ephemeral in preview, `e2e5542a1`), P7-1 pinning + staleness (UNIFORM-SNAPSHOT into a per-document `<note>.ros-embeds.json` sidecar, freeze + stale badge + view-current + re-pin, INERT until an explicit Pin click so it was safe to land on main, `86a4e04f0`), P7-2 transclusion (`![[Note#Heading]]` normalized on save to a portable link, depth-guarded live render, `a377140a2`), and the P7-5 BeakerBot-authoring relay doc (`63c6d186a`). Plus a RENAME-DRIFT FIX so embeds show the LIVE object name, not the stale baked link-text caption (`7bc740c7a`, came out of an identity-stability talk-through with Grant; the architecture is sound, objects are id-keyed and cross-user is fingerprint/content-keyed, the embed caption was the one cosmetic gap). REMAINING: P7-4 external/literature embeds (HELD until Grant verifies the pin format on :3000, it reuses the sidecar) and P7-6 mobile/a11y (the last finishing pass). GRANT'S PENDING VERIFIES (browser-only, cannot be orchestrator-verified): the Phase 6 two-browser share test, the P7-1 pin/staleness flow, the P7-2 transclusion live-update. GOTCHA reaffirmed HARD: the shared main checkout absorbed one `--no-commit` merge into a concurrent session's commit and threw phantom stale-anchor deletions in unrelated files, the safety that worked every time is `git merge --no-commit --no-ff <branch>` then grep the `--cached` staged set for foreign bleed before committing.

> **CURRENT STATE (2026-06-12, entry-surface unification + welcome scroll + the launch splash).** Full briefing: **`docs/orchestrator-handoffs/2026-06-12-entry-surface-and-splash-handoff.md`**. Mostly PUSHED to `origin/main` (deployed), gate-verified tsc 0. SHIPPED + LIVE: the welcome page got a **free-scroll** OAuth landing (dropped `snap-mandatory`, the down-chevron is the intentional jump, `9b9483ebe`), a **bidirectional `Reveal`** (reveals down, settles out up; tuned 24px+0.98 scale/0.65s/brand easing, `1657108bf`) with **staggered card grids** (step/trust/companion, `8ec287a22`+`bfde4b6cb`) and a one-time **cost-table 40ms cascade** (`728f1d341`); the **tree-of-life section was removed** (`bfde4b6cb`). Every entry/loading surface was **unified on a shared `LandingBackdrop`** (extracted deck stage: radial wash + dot grid + parallax auroras/beakers + rainbow bars) and pinned light: UserLoginScreen, FolderConnectGate, StagedLoadingScreen, WelcomeBackSignIn, LabSignInGate, AccountTierChooser (`0a222bc6c`+`583d99f58`), and `OAuthFirstLanding` was DRY'd onto it (`326867483`). The shared `IntroBubbleBot` mascot got a **heart-pop easter egg** on click everywhere (`269839631`, uses `<Icon name="heart">` + CSS fill, no icon-guard trip). The BeakerBot **`Splash` is now the once-a-DAY launch-into-app moment** (`localStorage researchos:splash-day`, moved post-login, `4f7d71711`), and the per-login **`SuccessTransition` was retired** in favor of it (`cec740a6d`, component kept only for `/dev/account-setup`). Soft-lock sweep: fixed the **FolderConnectGate "Initialize folder" Cancel** (it left the handle attached so nothing happened; now `disconnect()`s, `4c45533de`) and added a **delayed escape to the LabSignInGate** in-progress card (was a hard-trap, `74cd254ef`); a sub-agent audited every other entry escape button and found no more re-render dead-buttons. **LOCAL-ONLY (NOT pushed yet):** the splash now renders as an OVERLAY on the real workbench so its rainbow exit reveals the live app, not a second BeakerBot (`5574f92ce`), plus a **`/dev/splash`** replay harness to verify that reveal (`5b12b1d9f`). Grant pushes those two once he likes the reveal on `:3000`. NOTE: tsc on main briefly went RED mid-session from another session's half-landed chemistry `starred_papers` feature (consumers before the api.ts exports + `star` icon); it finished landing and compiles now, but check tsc before any push since the tree is shared.

> **CURRENT STATE (2026-06-12, Check-ins arc complete + demo-button polish + global-nav slimming arc, the UI-explorers session).** All on LOCAL main, NOT pushed, gate-verified (tsc EXIT 0, targeted vitest green, icon-guard green), none browser-verified by the orchestrator (Grant verifies on his own `:3000`). DONE this session: the **Check-ins revamp Phases 1-4** (Phase 2 group check-ins + the D4 action-item->real-Task sync `67cc0908c`; Phase 3 the full IDP `c2d104a8b`+`e3e79aea9` with per-section sharing + values-always-private + the action-plan->task sync + the static competency catalog, plus seed templates/mentoring-compact/onboarding `d51eac8c3`+`430279293`; Phase 4 mentorship-tree + skip-level + presenter rotation + light committee + the contents-free "IDP on file" roster status `f1d247a3a`+`e69ca0661`+`f807b5b07`). The **demo-page floating buttons** were fixed + polished (`8b79a83e2` stack above the FAB dock, `8636a723c` unify the pill aesthetic via a shared `DEMO_PILL_CLASS` + group the dev-only tools bottom-left, `c9b9838d9` remove the redundant Read-the-docs pill). **IN FLIGHT / NEXT: the global-nav slimming build.** The persistent header (`AppShell.tsx`) is too heavy (11 text tabs); Grant approved slim-in-place + a More overflow + a USER-CONFIGURABLE inline/hidden split done by drag-and-drop with an edit mode entered WITHOUT a persistent bar button (Enter AND Esc both confirm). The approved spec is the mockup `docs/mockups/2026-06-12-nav-drag-customize.html` (`a396a20a3`); the REAL build is UNSTARTED (flagged data-shape: a new optional `UserSettings.navLayout?: { inline: string[]; more: string[] }` layered on the existing `visibleTabs`; two rough edges to handle = pointer-based drag for touch + responsive auto-overflow). Full detail + build scope in handoff (A). NOTE the shared-checkout hazard bit twice this session (main flips branches under you; a commit landed on the wrong branch, recovered cleanly), so build in isolated worktrees and cherry-pick onto main GUARDED on branch==main AND a clean shared index.

> **ACTION FOR GRANT, surface this on or after 2026-06-13.** Grant asked us to help him find a local Wisconsin lawyer (ideally Madison or Dane County based), affordable and genuinely good at tech/SaaS/IP and terms-of-service work, to do the final legal review of the new `/terms` Terms of Service before it ships to prod. He wants an agent to help him run the search and vet candidates. If you are the session Grant is talking to and it is on or after 2026-06-13, REMIND him and offer to help, by web-searching Madison/Dane County solo or small-firm tech-transactional/IP attorneys (flat-fee or low hourly preferred) and giving him a short vetting checklist. The `/terms` draft is on local main and was research-verified against AGPLv3-SaaS precedent (PostHog, Plausible, Grafana). The OPEN items for the lawyer are the exact liability-cap dollar floor, EU/UK consumer-rights plus a possible GDPR data-processing addendum for international academic users, and a sign-off on the new DMCA/takedown and eligibility clauses. Context in `docs/orchestrator-handoffs/2026-06-12-marketing-ai-demo-handoff.md` and this session's deep-research report.

> **CURRENT STATE (2026-06-12 late, markdown embeds Phase 5 + Phase 6).** All on LOCAL main, UNPUSHED, gate-verified (tsc 0, the sharing/export suites green), NOT browser-verified beyond a loopback simulation. Verify with `git merge-base --is-ancestor 2bb5d6669 main` (that commit is the snapshot of all this work; if not reachable, a concurrent session reset the shared main, recover from the SHAs in the handoff). Full briefing: **`docs/orchestrator-handoffs/2026-06-12-embeds-phase5-6-handoff.md`**. SHIPPED: Phase 4 backlinks wiring on all 3 detail surfaces (`3badc7991`); **Phase 5 export baking** (`a5c7d23a9`, `lib/export/bake-embeds.ts` + `pdf.ts`, every note embed freezes to a self-contained PDF figure, Zenodo inherits it free; the image-rasterize path needs a REAL browser export to eyeball); and **ALL of Phase 6 share-with-dependencies D1-D8 except 6d**, decisions locked in `docs/proposals/2026-06-12-phase6-share-with-dependencies.md`: 6a foundation (`source_uuid` portable identity + `canViewObject` + `scanNoteDependencies`; I caught+fixed a cross-owner-write bug in the bot's backfill), 6b-1 bundle contract (`BundleEmbeddedObject` carries embedded objects in the share bundle), 6b-2 sender dependency panel (`NoteDependencyPanel`), 6c recipient import (recreate-or-link by content identity, file into "Shared by <sender>", rewrite embed hrefs), and a GREEN loopback integration test (`2bb5d6669`, real build->seal->open->read->import). **Grant does the TRUE 2-browser share test next** (live relay + directory + real FSA, which the loopback cannot cover). REMAINING: 6d no-access placeholder + the 6c per-item picker UI (plumbed, UI stubbed) + Phase 7 polish. GOTCHA reaffirmed: the shared main checkout is a treadmill (merge worktree branches into main, never ff; watch for a foreign MERGE_HEAD; regenerate ai-helper after any new lib type or the build gate fails).

> **CURRENT STATE (2026-06-12, sequences extract + guided NCBI import + chemistry literature explorer + rich demo clips).** Continues the welcome demo-video session. All on LOCAL main, gate-verified (tsc 0 + targeted vitest green + icon-guard green). Full inheritor briefing: **`docs/orchestrator-handoffs/2026-06-12-demo-recorder-and-record-prep-agent.md`**. SHIPPED: (1) the four welcome demo clips expanded to rich 4-5 feature showcases + a record-mode-gated **precache ("movie magic")** that warms a clip's exact PubChem / Europe PMC calls + the RDKit wasm + SeqViz chunk during the 5s countdown (`lib/demo-video/prewarm.ts` + `lib/chemistry/fetch-cache.ts`, passthrough in prod so zero behavior change); (2) **Extract to new sequence** (`b5d0e6371`, `seq-extract-region-btn`, select a feature/range -> standalone library sequence via the existing pure `extractRegion`), **BROWSER-VALIDATED by Grant via the Claude-in-Chrome extension, 3/3 pass** (feature extract egfp 720bp, base-range extract, Gibson clone into pGEX-3X); (3) the **guided NCBI genome import wizard** (`GuidedNcbiImport.tsx`, organism -> reference genome -> contigs -> gene-NAME search -> windowed import, + accession escape hatch + whole-genome/chromosome options) on a fully tested new backend (`listAssemblySequences`, `parseGenePlacement`, windowed `efetchGenbank` + `geneWindow`, `esearchGenes` via E-utilities), built to the approved mockup, NOT browser-verified; (4) the **chemistry literature explorer** (`LiteratureExplorer.tsx`, filter rail + papers-per-year histogram with editable min/max year inputs Google-Scholar style + review/patent toggles + star-a-DOI) with `Paper.isReview` and an additive **DATA-SHAPE** `MoleculeMeta.starred_papers?` sidecar field, built to the approved mockup, NOT browser-verified. REMAINING: record the demo clips for the wizard (cyp51A/Af293 walk) + the gliotoxin lit/star beat with precache; Chrome-test the wizard + lit explorer; two optional follow-ups (dev-only Extract-button dock occlusion held; whole-genome import has no caps/size warning, `checkCaps` exists). Worked example: A. fumigatus Af293 / cyp51A on chr4 NC_007197.1 (full chr4 efetch = 7.26MB/1267 genes janks, the gene+1kb window = tens of KB instant). GOTCHAS: brief sub-bots to COMMIT EARLY (one finished but died before committing, salvaged from its worktree); merge a stale-anchored bot branch by PATCH not wholesale; mouse/drag tests go to the Claude-in-Chrome extension (Grant does the one-time native folder picker, preloaded test folder `~/Desktop/ResearchOS-Extract-Test`).

> **CURRENT STATE (2026-06-12 later, Data Hub v2 STATS EXPANSION).** Continues the block below; all on LOCAL main, gate-verified (tsc 0 + `cd frontend && npx vitest run src/lib/datahub src/lib/transparency`), NOT browser-verified (Grant's :3000 pass pending). Full inheritor briefing: **`~/Desktop/HandoffForAgents/2026-06-12_datahub-v2-and-wrangling.md`** (read the LATEST section at the top first). LANDED this session: the "export the whole chain" feature (verify+merged, `6c6fd5e24`); the BeakerAI "Help me choose?" button in NewAnalysisDialog (`88be6386e`); the transform ICON decided + built (Grant picked the funnel glyph, dedicated `transform` registry entry, swapped off the merge placeholder, `f82ccf374`); and **THEME 1 of the 18-card stats-expansion proposal COMPLETE** (`docs/mockups/data-hub-v2-proposal.html`, the Estimation layer): **E1** effect sizes + CIs (Hedges g + noncentral-t CI on t-tests, eta2/omega2 + noncentral-F CI on ANOVA, r2 + CI on correlation; authored noncentral t+F in `engine/dists.ts`; `c53086592`), **E3** power + sample-size (`engine/power.ts` + `PowerPlannerDialog` "Plan study"; `2d4ddf25d`), **E4** bootstrap CIs (`engine/bootstrap.ts` seeded mulberry32 percentile+BCa; `a2911dd11`), **E2** estimation plots (Gardner-Altman + Cumming, `estimation-plot.ts` + additive PlotSpec kinds; `c5040a25f`), AND both public differentiators wired (`9996816c4`): 20 transparency pins in the `/transparency` `datahub-stats` domain + show-the-code Python for effect sizes + bootstrap. Every stat scipy/statsmodels/pingouin-validated. **IN FLIGHT: Theme 2 / D1 (dose-response 4PL/5PL + EC50/IC50 analysis)** on worktree `worktree-agent-ab7363e4ad8d257c0`, verify tsc+vitest then land. **Theme 2 remaining: D2 model comparison, D4 logistic regression, D5 multiple regression, D3 global fitting.** STANDING RULE Grant enforced: every new stat lands WITH its transparency pin + show-code from the start, not as a follow-up. LANE SPLIT: BeakerAI owns `lib/ai/tools/*` (do NOT touch; relay specs to them, Grant relays). GOTCHAS this session: ALWAYS pass `isolation: "worktree"` to the Agent tool (a non-isolated bot did `checkout -b` in the shared root and dragged every session onto its branch); the shared-root index/working-tree is contended (other sessions' WIP trips the combined pre-commit hook + blocks merges, `--no-verify` is justified ONLY when the failure is a foreign uncommitted file and your own staged source is icon-guard-clean); sub-bot worktrees anchor on a STALE base, ALWAYS `git merge main` in the bot's worktree before trusting its gate, then re-run tsc (a "2 pre-existing errors" reading usually means the bot's base predates a fix on main).

> **CURRENT STATE (2026-06-12, welcome demo-video recording system).** All on LOCAL main. Built a deterministic in-app **demo engine** (`frontend/src/lib/demo-video/`) that drives the `/demo` UI with its OWN animated cursor by TARGETING ELEMENTS (never misclicks), so the welcome-page marketing clips record smoothly + reproducibly (the old "drive by guessing pixel coords through the extension" approach misclicked constantly — Grant: "that was painful"; the engine version: "holy shit that was PERFECT"). Workflow: open the launcher **`localhost:3000/dev/demo-videos`**, fullscreen, click a clip -> full-load to `/demo?record=1&demo=<id>` -> **5s countdown** -> the engine drives it (backtick `=` re-take, Cmd-R = clean reload); Grant captures the screen, the orchestrator authors the scripts. Record-mode (`?record=1`) now ALSO hides the Next "N" dev indicator, the floating dock, the BeakerBot flask, the dev chips, and the REAL OS cursor (`cursor: none`, so only the rendered cursor shows); and the `/demo` fixture (`wiki-capture-fixture.ts`) stale "DEMO:" prefixes were cleaned (extended `scripts/clean-demo-data-names.mjs`). **Chemistry clip VERIFIED (Grant loved it); Data Hub runs clean; Purchases built; Sequences spin works, Tm-drag unverified (background-throttle freezes it — a TESTING artifact, fine at foreground).** Grant's OPEN ask: **make every clip a RICH feature showcase (4-5 cool features per page)** — Gibson assembly, substructure search, protein translation, Map<->Seq flip, a detailed plasmid (pEGFP-N1), graph-style tweaks, order line items, etc. Full detail + every selector + gotchas (shared-tab contention, background-throttle freezes, studio links MUST be plain `<a>`): **`docs/orchestrator-handoffs/2026-06-12-demo-video-system-handoff.md`**.

> **CURRENT STATE (2026-06-12, marketing redesign + AI surfacing + demo polish, a DIFFERENT parallel session).** All on LOCAL main (interleaved with other sessions' datahub/embeds/checkins commits); gate-verified, NOT browser-verified (no 2nd next dev). Shipped this session: shared marketing motion system (`components/marketing/Reveal` + `MarketingBackdrop`), welcome page REBUILT to the approved IA (`docs/mockups/welcome-redesign-2026-06-11.html`, companion spotlight + metered-AI story), settings redesign (full-screen left-rail `SettingsShell`, X+Esc exit, profile merged in, avatar collapses to one "Settings", Usage & billing dashboard on fixtures, rail-bottom footer), pricing AI section + SciFinder in the cost table + the savings calc made HONEST (solo=$0, a lab pays the optional cloud) with an at-cost-AI highlight, product chrome rebranded to **ResearchOS LLC** (founder name kept ONLY in origin story + legal), and the **footer/nav IA split** (rich `MarketingFooter` + `MarketingNav` + new `/about` on public pages; brand-only `AppFooter` in-app) driven by `docs/audits/cross-linking-and-ia-audit-2026-06-11.md`. Routing fix: public marketing/legal pages bypass the folder-connect gate (`providers.tsx isPublicMarketingRoute`). Stale-claim sweep: real-time co-editing + cross-lab sharing are SHIPPED (relay + flags on in prod), roadmap pruned. The three at-pause sub-bots (demo-polish, ai-surface, ai-showcase-mockup) are ALL RECOVERED + merged. NEW this session: a public **`/ai` BeakerBot marketing sell page** (retired the old in-app `/ai` palette-opener, built to welcome-page quality, leads on how cheap the metered AI is; still ORPHANED, needs entry-point links from pricing/welcome + `MarketingNav`), and a research-verified DRAFT **`/terms` Terms of service** (built and stress-tested vs AGPLv3-SaaS precedent PostHog/Plausible/Grafana; $100 liability floor, consumer carve-out, eligibility 13+, DMCA, standard boilerplate; NEEDS a Wisconsin attorney before prod, see the `ACTION FOR GRANT` block above, surfaces 2026-06-13). **PENDING at next deploy: set `NEXT_PUBLIC_OAUTH_FIRST_LOGIN=true` in Vercel Production** (redesigned login built+local-only). View the login/welcome via `localhost:3000/?previewLogin=1`. Full detail in handoff (C).

> **CURRENT STATE (2026-06-12, markdown + ResearchOS embeds).** Built + on `origin/main` (`88f3d79d4`+): a Notion/Obsidian-style live-preview where object references (`[name](/deeplink#ros=view)`) render as rich cards in BOTH the editor and Preview. Rich renderers for every type except file (molecule structure, sequence ribbon, datahub table/plot/result, note/method/project/collection/task/experiment cards), inline mention chips with hover-cards, native-image caption+resize, the `/` picker defaulting to Embed, figure captions + opt-in per-doc numbering (a "Number figures" toolbar toggle inserts the hidden `<!-- ros:number-figures -->` directive), and the Phase 4 backlinks foundation ("Referenced in", `lib/object-backlinks.ts` + `ObjectBacklinks.tsx`). Design proposal `docs/proposals/2026-06-11-markdown-embed-hybrid.md`; BeakerBot author/read guide `docs/proposals/2026-06-11-beakerbot-embed-integration.md`. Phase 4 share-safety (warning + no-access message) was FOLDED INTO Phase 6 (share-with-deps). Remaining = Phases 5-7 (export/publish baking, share-with-dependencies + safety, pin/transclusion/external-embeds polish), design-heavy, need Grant's input. **GOTCHA reaffirmed this session: the single `main` checkout is shared across concurrent sessions and gets switched between branches, ALWAYS `git branch --show-current` before committing (a commit once landed on a `fix/...` branch; recovered via `git push origin <sha>:main` + `git update-ref`).** Full detail in the handoff doc linked above.

> **CURRENT STATE (2026-06-12, UI explorers + density + Check-ins, a DIFFERENT parallel session from the embeds one above).** All on LOCAL main, NOT pushed; Grant verifies on his own `:3000`. Shipped: Methods/Experiments(Workbench tab)/Notes(Workbench tab) card-grid->two-pane "explorer" redesigns (rail + dense rows, keep the click-through popup) + method **fork-creation** (`frontend/src/lib/methods/fork-method.ts`, clones per-type content); the **Today-at-a-glance rail scoped to GANTT-only** (`AppShell.tsx`); subpage **header chrome tightened** (Workbench/Methods/Purchases/Calendar/Links; the focus surfaces were already tight). **Check-ins revamp** (`docs/proposals/checkins-revamp.md` + `docs/mockups/2026-06-11-checkins-revamp.html`, memory `project_checkins_revamp.md`): generalize the 1:1 into any-account mentorship-tree + N-member group check-ins; 6 decisions LOCKED; **Phase 1 BUILT** (commit d84cec467: `OneOnOne` -> member-array space w/ `mentor` edge + `normalizeOneOnOne` back-compat shim, any-account creation, always-show tab, gentle carry-forward); Phases 2 (group multi-select picker + `assignee` task board + D4 action-item->GANTT sync) / 3 (career-stage templates + IDP modeled on AAAS myIDP + UW-Madison) / 4 (mentorship-tree viz + presenter rotation + committee) REMAIN. **`tsc` is RED on main from 2 PRE-EXISTING Data Hub errors** (`datahub/DataHubRail.tsx`, `datahub/transform/recipe.ts`), not this session's work, a fix chip was created and Grant STOPPED it (re-fire prompt in handoff (A)). Big open density item: collapse the persistent GLOBAL nav bar (mock up first). Detail in handoff (A) above.

> **CURRENT STATE (2026-06-11, Data Hub finish + build/toolchain fixes).** `origin/main` and local `main` are RECONCILED and in sync again (the 2026-06-10 divergence note below is resolved); pushes to `origin main` this session were clean fast-forwards, so the "do NOT push from local main" caution is no longer in force. **Data Hub is FEATURE-COMPLETE** behind `DATAHUB_ENABLED` (default OFF) — all four table types, embed, wiki, guided wizard, live-verified, persistence round-trip + edge cases tested (see §Data Hub). Three deploy/toolchain fixes landed + pushed: committed the `/ai` `/chemistry` `/chemistry-embed-check` wiki-coverage exclusions that were failing the build (`e80c0bae5`); silenced the pnpm `core-js` ignored-build-script warning via `package.json` `ignoredBuiltDependencies` (`5cedd8ebd`); and **pinned pnpm to 10.34.3** via `packageManager` + deleted the dead `frontend/pnpm-workspace.yaml`, which is what kept that file perpetually dirty (`12e540288`) — other local checkouts should pull `main` so Corepack auto-switches them. New committed helper `scripts/worktree-dev.sh` makes a live dev server runnable in a worktree (a real `pnpm install` is required for Turbopack; symlink/COW do not work). Remaining for Data Hub launch is Grant's call: flip the prod flag + capture the fixture-mode wiki screenshots.

> **CURRENT STATE (2026-06-10, demo launch).** Prod (`research-os.app`) is LIVE and public, configured **everything-on-but-billing**. Vercel flags (all plain readable vars, NOT "sensitive" which silently store blank): `SHARING_ENABLED`, `NEXT_PUBLIC_SHARING_ENABLED`, `NEXT_PUBLIC_EXTERNAL_COLLAB_ENABLED`, `NEXT_PUBLIC_INVENTORY_ENABLED` = `true`; `BILLING_ENABLED` + `MAINTENANCE_MODE` = `false`. So sharing, profiles/sign-in, external collab, real-time collab, and inventory are live; billing is off (everything free during beta). `NEXT_PUBLIC_*` bake at build, so flipping them needs a `vercel redeploy`, not just an env change. The **ResearchGate-style researcher directory is built and live** (`/researchers` search + `/researchers/[fingerprint]` profiles + ORCID publications + a lab directory), gated to logged-in OAuth researchers, opt-in, fingerprint-keyed so emails are never enumerable; it is just empty until people publish. Still unverified (needs a browser + 2 machines, cannot be done headless): the 2-browser real-time collab co-edit through the Cloudflare relay (relay worker confirmed awake, `/ws` returns 426), and the OAuth-publish-then-search round trip. Open follow-ups: a deferred **git worktree sweep** (~40 worktrees / 64 pinned branches; ~360 local branches were trimmed to ~283 by deleting merged + a 73-branch throwaway `claude/*` cluster), and the sponsor BeakerBot badge mockup is approved and parked at `docs/mockups/sponsor-beakerbot-badges-2026-06-10.html` waiting only on GitHub approving the Sponsors profile.

> **CURRENT STATE (2026-06-10, regression/dogfood pass + login redesign).** A demo surfaced several regressions, all fixed and SHIPPED to prod this session: primer panel no longer auto-opens on a region highlight; Results gets its `# Results:` header (scaffold at creation + an on-connect backfill migration for existing experiments); the inline editor code block is dark again + the language picker is back; Focus Mode raised above the LivingPopup z-band (z-index half of a deeper escape fix, see the handoff); companion photos auto-route to the open experiment; and **Telegram was fully removed** (code was already gone; copy + wiki screenshots de-Telegrammed, see §5). Also: a real auth bug fixed (all OAuth providers now gated on creds so dev does not 500), the **Microsoft (Entra) sign-in button added** behind `NEXT_PUBLIC_AUTH_MICROSOFT_ENABLED` (Grant registered the Azure app + is wiring the Vercel env), and a `/dev-*` wiki-coverage exclusion that **un-stuck the prod build** (the live site had been stranded on an older deploy). **IMPORTANT repo state: `origin/main` has DIVERGED from local main.** This session shipped ONLY its own commits to prod via a clean cherry-pick branch off `origin/main`; local main still carries everything (other parallel sessions' chemistry/Data Hub/business WIP under original SHAs, plus this session's work) and still fails the build. **Do NOT `git push origin main` from local main**; other sessions should rebase onto the new `origin/main` and push their delta. The **OAuth-first login redesign** is the big open task (mockup `docs/mockups/2026-06-10-entry-flow-oauth-first.html` approved in shape, deck-style dark landing + 3-tier chooser + OAuth-first + post-sign-in folder step + last-used badge), awaiting Grant's marked-up export before any code. Full detail: the handoff doc linked above.

---

## 1. What ResearchOS is

A local-first research project management app for science labs:

- GANTT scheduling with dependency-aware date shifts.
- Lab notes + results: markdown editor with image strip, drag-drop attachments, image gallery picker.
- PCR protocol builder with reagent calculators.
- Methods library with reusable protocols.
- Multi-user shared folders (OneDrive / Dropbox / iCloud) with per-user accounts, password gates, and shared-task editing.
- Telegram bot ingestion → image inbox.
- External ICS calendar overlays.

**Architecture:** Next.js 16 (App Router) + React 19 + TypeScript + Tailwind. **No backend.** All data lives in a folder on the user's disk, accessed via the **File System Access API**. The only server-side code is two thin proxy routes (`/api/telegram-file`, `/api/calendar-feed`) that exist purely to bypass CORS for those integrations.

**Hosted at:** [research-os.app](https://research-os.app/) (the live public domain, auto-deploys from `origin/main`). Also runs locally via `./start.sh` (frontend only; the old FastAPI backend was deleted).

**Positioning / pricing (UPDATED 2026-06-10, the SOLIDARITY MODEL, supersedes any flat "cost-recovery only" framing).** The local-first app is free and open source forever, **AGPLv3**, funded by a UW Distinguished Research Fellowship at UW-Madison plus voluntary GitHub Sponsors, so it stays free for low-resource labs. The ONLY paid part is **optional cloud storage** above a generous free pool. Pricing is **solidarity-based, NOT flat cost-recovery**: individuals and labs pay cost recovery (what storage actually costs us, no more, on the Free / Plus / Pro and Lab Free/Plus/Pro tiers), while **departments and institutions pay a modest sustaining rate ABOVE bare cost**, and that surplus keeps ResearchOS free for individual researchers and funds the open-source development. Dept and institution are **automated self-serve plan builders** (you build a monthly plan from labs/members/adoption), billed by an **auto recurring Stripe invoice to procurement** (GitHub Sponsors cannot invoice a university, so the invoice IS the institutional channel; Sponsors stays the give-extra path for individuals). The public **`/pricing` page is BUILT** (`frontend/src/app/pricing/`, every tunable number in `frontend/src/lib/pricing/assumptions.ts` as flagged placeholders). **ResearchOS LLC (Wisconsin)** is the merchant of record, Stripe live, but billing is OFF (`BILLING_ENABLED=false`) so everything is free during beta. Plus/Pro FINAL sticker prices stay unpublished (provisional), but transparent cost-recovery ESTIMATES are fine when clearly labeled an estimate. Branding nods: the app is **"Built in Madison" / a registered Wisconsin LLC** (Midwest, not a California cloud), Packers green-gold accent on the badge. Copyright Grant R. Nickles. **Funding acknowledgment (CONFIRMED 2026-06-11 by the OVCR project manager):** the official program name is the **"UW Distinguished Research Fellowship"** (NOT "UW-Madison RISE Initiative / RISE fellowship", that is the OLD name, do not revert to it). Approved verbatim wording, "Support for this research was provided by the University of Wisconsin-Madison, Office of the Vice Chancellor for Research with funding from the Wisconsin Alumni Research Foundation." Compact footer form, "Supported by a UW Distinguished Research Fellowship at UW-Madison (Office of the Vice Chancellor for Research, with WARF funding)." **Text acknowledgment only for now**, displaying UW-Madison / OVCR / WARF logos on the product site is PENDING confirmation from the OVCR (Cynthia Czajkowski), so the old RISE logo was retired (PNG kept at `frontend/public/credentials/uw-rise-logo.png`, just not shown). Full brand standard in `docs/branding/brand-rulebook.html`; positioning + messaging in `docs/branding/POSITIONING.md` + `MESSAGING.md`. **FORBIDDEN claims:** never write "no paid tier", "we never charge", or "free forever" about the cloud; and do NOT revert the page or docs to a flat "cost-recovery, never profit / not a money printer" framing, that is the OLD model, institutions now sustain the free tiers. **CANONICAL customer-facing billing copy is `docs/branding/BILLING_FACTS.md` (mirror `docs/reference/billing-copy-facts.md`), read it before touching ANY pricing/billing copy and keep it current whenever billing changes.** Rationale in `[[project_sustainability_pricing_model]]` and `[[project_llc_business_ops]]`. House style for all prose: concept-first and warm, **no em-dashes**, **no emojis** (every icon is a custom inline SVG; use the `<Tooltip>` component, never native `title=`), **no mid-sentence colons** (Grant flags both appositive and list colons as AI-speak; recast with a comma or period split), state the WHY, the mascot is always BeakerBot. **One exemption to the no-em-dash rule** (Grant, 2026-06-07): a lone em-dash used as the "no value" / empty-cell glyph (e.g. an empty table cell or stat tile rendering `"—"`) is allowed and should NOT be flagged. The rule still bans em-dashes inside real sentences and copy.

---

## 2. Repo layout

```
ResearchOS/
├── frontend/                           ← all app code
│   ├── src/
│   │   ├── app/                        ← Next.js pages
│   │   │   ├── api/
│   │   │   │   ├── telegram-file/      ← Telegram CDN proxy (server)
│   │   │   │   └── calendar-feed/      ← ICS proxy (server)
│   │   │   ├── page.tsx                ← home / dashboard
│   │   │   ├── gantt/                  ← GANTT view
│   │   │   ├── calendar/               ← calendar w/ ICS overlays
│   │   │   ├── methods/                ← methods library
│   │   │   ├── purchases/              ← purchase tracking
│   │   │   ├── results/                ← results editor
│   │   │   ├── experiments/            ← experiment list
│   │   │   ├── lab-overview/           ← multi-user lab dashboard (react-grid-layout widget canvas)
│   │   │   ├── lab/                    ← deprecated, queued for deletion in Lab Mode retirement R5
│   │   │   └── search/, links/, pcr/   ← misc views
│   │   ├── components/                 ← React components (60+)
│   │   └── lib/
│   │       ├── local-api.ts            ← THE API layer (was a FastAPI replacement)
│   │       ├── file-system/            ← FSA wrappers + provider context
│   │       │   ├── file-service.ts     ← read/write/list primitives + read-count instrumentation
│   │       │   ├── file-system-context.tsx ← FileSystemProvider, connect / reconnectWithStoredHandle
│   │       │   ├── indexeddb-store.ts  ← persists directory handle + current user
│   │       │   └── user-discovery.ts   ← scans `users/` dir
│   │       ├── storage/
│   │       │   └── json-store.ts       ← JsonStore<T>: CRUD by entity; getForUser/saveForUser for cross-user routing
│   │       ├── engine/
│   │       │   ├── dates.ts            ← weekend-aware date math
│   │       │   └── shift.ts            ← shiftTask: dependency cascade (accepts optional owner)
│   │       ├── notes/migrate-images.ts ← one-shot per-note image-ref rewriter
│   │       ├── telegram/               ← bot client + polling hook + token store (writes users/<u>/_telegram.json + auto-appends a .gitignore rule)
│   │       ├── calendar/               ← ICS parser + feed store + useExternalEvents hook
│   │       ├── attachments/            ← image folder utils, move-image, image-events bus
│   │       ├── auth/password.ts        ← PBKDF2 per-user password gate
│   │       └── utils/blob-url-resolver.ts ← FSA path → blob: URL, used everywhere an <img> renders
│   ├── package.json
│   └── tsconfig.json
├── scripts/
│   └── cleanup-migrated-images.mjs     ← legacy image cleanup w/ Desktop-zip backup
├── AGENTS.md                           ← (this file)
├── ARCHITECTURE.md                     ← deeper architecture doc — read if relevant
├── README.md                           ← user-facing
├── start.sh / start.ps1                ← local launcher (frontend-only — no backend)
└── frontend/src/lib/local-api.ts       ← single most-touched file in the codebase
```

Data folder layout (on user's disk):

```
{root}/
├── users/
│   ├── {username}/
│   │   ├── projects/{id}.json
│   │   ├── tasks/{id}.json
│   │   ├── dependencies/{id}.json
│   │   ├── methods/{id}.json
│   │   ├── notes/{id}.json
│   │   ├── goals/{id}.json
│   │   ├── pcr_protocols/{id}.json
│   │   ├── purchase_items/{id}.json
│   │   ├── results/task-{id}/
│   │   │   ├── notes.md
│   │   │   ├── results.md
│   │   │   ├── Images/                 ← per-task image folder
│   │   │   └── Files/
│   │   ├── inbox/Images/               ← Telegram arrivals waiting to be filed
│   │   ├── _counters.json              ← per-user auto-increment IDs
│   │   ├── _auth.json                  ← optional PBKDF2 password
│   │   ├── _shared_with_me.json        ← entries from other users
│   │   ├── _notifications.json
│   │   └── _calendar-feeds.json        ← ICS subscriptions
│   ├── public/                         ← shared methods/PCR protocols (cross-user)
│   ├── lab/                            ← deprecated lab pseudo-account folder, queued for cleanup in Lab Mode retirement R5
│   └── _user_metadata.json             ← username → color/created_at/hide_goals_from_lab
└── _global_counters.json
```

**Each user has its OWN id space** (separate `_counters.json`), so `task.id` alone isn't unique across users. Use `taskKey(task)` from `lib/types.ts` (`"self:N"` for own tasks, `"{owner}:N"` for shared) anywhere uniqueness matters: React keys, Map lookups, store selection, React Query keys.

---

## 3. The local-api surface

`frontend/src/lib/local-api.ts` is the most-touched file in the codebase (~2000+ lines). It exports a set of APIs that mirror what used to be the FastAPI backend:

- `projectsApi`, `tasksApi`, `dependenciesApi`, `methodsApi`, `pcrApi`, `goalsApi`, `eventsApi`, `purchasesApi`, `lab_linksApi`, `notesApi`, `inbox*`, `sharingApi`, `usersApi`, `labApi`, `attachmentsApi`, `filesApi` (was `githubApi`, kept aliased for legacy callers — renamed in commit ~`128033bf`).

Editable shared tasks: most mutating `tasksApi` methods (`update`, `move`, `delete`, `addMethod`, etc.) take an optional `owner` argument. When the popup is for a task that's `is_shared_with_me` + `shared_permission === "edit"`, the wrapper in `TaskDetailPopup` (`ownerScopedTasksApi(task)`) automatically threads `owner` through every call. `shiftTask` in `engine/shift.ts` is similarly owner-aware — when `owner` is set, every read and write (deps, tasks, project weekend lookups) is routed to that owner's directory.

---

## 4. Working conventions

### No business, financial, or personal info in the repo (it is PUBLIC)

`github.com/gnick18/ResearchOS` is a PUBLIC, clonable repo. NEVER commit business, financial, legal-strategy, or personal-operational details: bank or brokerage names, account/routing/EIN numbers, registered-agent name or address, capital-contribution mechanics, the owner's private legal plans, or any internal financial figures not already on the public `/pricing` page. Those belong in `~/Documents/ResearchOS_LLC/` (outside the repo) or in agent memory (`~/.claude`, not tracked). Customer-facing copy (`docs/branding/BILLING_FACTS.md`, the public pricing/terms/privacy pages) is the only billing content that belongs in-repo. If you need to record sensitive context, write it to the LLC folder and leave a non-sensitive pointer. A history-purge plan for already-leaked content lives at `~/Documents/ResearchOS_LLC/repo-privacy-cleanup-plan.md`.

### Sub-agents (spawn vs inline)

Big chunks of work were done by spawning fresh agents in their own worktrees via the `Agent` tool (or its older alias `mcp__ccd_session__spawn_task`). The orchestration model is **flat**: the master bot dispatches sub-bots directly, processes their reports, cherry-picks commits, and manages the in-flight queue. There is no intermediate manager tier; sub-bots are single-purpose and do not coordinate with each other.

**Spawn when:**

- Multi-hour work that doesn't depend on this chat's state (audit sweeps, large refactors, isolated features).
- Anything where the user explicitly says "spawn an agent."

**Don't spawn — do it inline — when:**

- Single-file fixes.
- Anything that needs ongoing dialogue with the user.
- Trivial cleanup.

Reliable recipe for a spawn prompt:

- **Self-contained briefing.** Spawned agents don't see prior chat history. Include the project's one-liner, the specific files to touch, the current commit they should expect to see, and what's out-of-scope.
- **Scope guards.** Always say what NOT to do (don't touch on-disk JSON format, don't refactor unrelated things, don't migrate to a new component if scope is "add `title` attrs").
- **Reproduction recipe** if you can give one (`as Grant open Kritika's shared task — should …`).
- **Worktree bootstrap.** A fresh worktree has no `node_modules` — without it, `npx tsc` falls through to a placeholder ("This is not the tsc command you are looking for") and the verification gate is silently bypassed. **For tsc + vitest (no dev server): SYMLINK, never install** — `ln -s /Users/gnickles/Desktop/ResearchOS/frontend/node_modules frontend/node_modules` (and the root one). A bare `npm`/`pnpm install` once triggered a macOS TCC permission revocation that killed every running agent; the symlink is the rule for the verify gate. **For a LIVE DEV SERVER (Preview MCP / browser walkthrough), the symlink does NOT work** — Turbopack rejects a node_modules that escapes the worktree root ("Symlink node_modules is invalid, it points out of the filesystem root"), and an APFS COW clone (`cp -c -R`) of this repo's **pnpm** node_modules leaves a broken `node_modules/node_modules` self-symlink Turbopack also rejects (the route 500s on first compile). The only thing that works for a dev server is a real `pnpm install --prefer-offline` in the worktree, which is SAFE + fast (the TCC-kill incident did NOT recur across many isolated-worktree installs 2026-06-11; pnpm hardlinks from the global store). Use the committed helper **`scripts/worktree-dev.sh <worktree-frontend-dir> <port> [ENV=V ...]`** (detects a missing/broken node_modules, installs if needed, execs `next dev` with any flags like `NEXT_PUBLIC_DATAHUB_ENABLED=1`); point a `.claude/launch.json` entry's runtimeArgs at it, then `preview_start`. **pnpm is PINNED (2026-06-11):** `frontend/package.json` `"packageManager": "pnpm@10.34.3"`, so Corepack auto-switches any local pnpm to 10.34.3 (first run downloads it once). The dead `frontend/pnpm-workspace.yaml` was DELETED — do NOT re-add it; build-script approvals live in `package.json` `pnpm.onlyBuiltDependencies` (sharp, unrs-resolver) + `ignoredBuiltDependencies` (core-js). Before the pin, local pnpm 11 vs Vercel pnpm 10 was what kept showing that file as perpetually dirty.
- **Verification gate**: `cd frontend && npx tsc --noEmit` must pass with exit 0. Optionally `npx eslint src/`. Live test in `http://localhost:3000`.
- **Staleness check.** Tell them: "If you don't see `frontend/src/lib/local-api.ts` or recent commit `<hash>`, the worktree is stale — stop and report." This catches the case where the spawned worktree is branched off pre-FSA-migration state.
- **Commit explicitly, don't push, don't merge.** Sub-bots have misread "don't push, don't merge" as "don't commit" and left uncommitted work that the orchestrator then had to copy file-by-file (the export-arc Sub-bot F mishap, 2026-05-13). Brief explicitly: "commit your changes on your branch in coherent chunks; do NOT push, do NOT merge — the orchestrator handles integration via branch merge." If the bot is in a worktree, this lets the orchestrator pull the branch cleanly.
- **Double-check shape claims against types/regex sources, not memory.** Two factual errors in the export-arc PCR-rendering brief (`pcr_gradient` was described as markdown when it's JSON-encoded, `source_path` was described as `pcr://{id}` when the real format is `pcr://protocol/{id}`) were caught by the sub-bot, not the orchestrator. When the brief asserts a data shape or path format, grep the actual `types.ts` or the regex source first.

### Sub-bot anchor drift (2026-05-27, learned the hard way)

A spawned worktree branches from the commit `main` pointed at **when the agent was launched**, not live `main`. During a long session the orchestrator lands dozens of cherry-picks onto `main`, so by the time a sub-bot finishes, its anchor can be 30+ commits stale. This bit us repeatedly in one night. Two halves to the rule:

**Sub-bot side — rebase before committing.** Brief every spawned bot: "FIRST run `git merge main --no-edit` in your worktree root and confirm it succeeds before doing any work. If it conflicts non-trivially, STOP and report." A bot that rebases onto current `main` produces a commit that cherry-picks cleanly with no conflict resolution at all. The telegram-image-path bot did this and its cherry-pick was a clean fast-forward; the bots that didn't caused the messes below.

**Orchestrator side — never `-X theirs` a stale-anchor commit onto a fast-moving main.** `-X theirs` resolves every conflict by taking the cherry-picked (stale) side. When that side is older than `main`, it **silently reverts** whatever landed on `main` after the anchor. Concrete damage from one night:
- A telegram-tutorial cherry-pick reverted 424 lines of `batch-routing.test.ts` coverage that had landed post-anchor (tests still "passed" — on a shrunken set). Had to restore main's file and re-graft the new tests.
- A workbench-create cherry-pick auto-merged `step-machine.ts` from the stale base and produced **duplicate step ids** + a dropped `settings-tour-calendar` slot, which only surfaced as test failures. Had to revert the whole commit and re-apply the change surgically by hand on top of current main.

**Verification after every cherry-pick:** run `git show <hash> --stat` and sanity-check the line deltas. If a file you didn't expect to shrink lost hundreds of lines (especially a test file), that's anchor-drift clobbering, not an intended edit. Diff the file against `main`'s pre-cherry-pick version (`git show <main-before>:path | wc -l`) and recover the lost content. Better: prefer **merging the sub-bot's branch** over `cherry-pick -X theirs` when the branch was rebased onto main; reserve `-X theirs` for the case where you've already confirmed the only divergence is the intended change.

**Circular-import footgun (same night):** `step-registry → WikiPointerStep → TourBootstrap → step-registry` closed a cycle that Next.js tolerated but vitest's module loader did not — it silently broke the entire `step-bodies.test.tsx` import (0 tests ran), masking every step-body assertion for who-knows-how-long. When a registry imports a step that imports a bootstrap that imports the registry, extract the shared leaf helpers into a dependency-free module (the `wiki-pointer-nav-flag.ts` / `lib/query-client.ts` pattern). If a whole test file reports "no tests" instead of pass/fail, suspect an import cycle, not an empty file.

### Sub-bot worktree discipline (2026-05-29, current hard rules)

The non-negotiable preamble every worktree sub-bot brief must carry, learned across the cohort sessions:

- **Symlink node_modules for the tsc/vitest gate; `pnpm install` only when a live dev server is genuinely needed.** `ln -s /Users/gnickles/Desktop/ResearchOS/frontend/node_modules frontend/node_modules` covers the verify gate (never `npm install` — wrong PM, and a bare install once revoked macOS TCC and killed every agent). But a symlinked/COW node_modules does NOT boot a Turbopack dev server (it escapes the worktree root), so a task that must drive the live app (Preview MCP, browser walkthrough) needs a real `pnpm install --prefer-offline` in its OWN isolated worktree via `scripts/worktree-dev.sh` — that ran cleanly many times on 2026-06-11 with no TCC recurrence. Never install into a worktree that is symlinked to or shared with the main store; isolate it. pnpm is pinned to 10.34.3 (Corepack auto-switches).
- **NEVER `git stash`.** The stash is a single repo-wide resource shared across every worktree; one bot stashing has wiped another session in-progress stash. If a bot needs a clean tree it commits or discards its own exact paths, never stash.
- **Never pipe a gate/test/build command through `tail`** (or any pipe). The pipe exit code masks the real one, so a failing `tsc`/vitest reads as green. Use `cmd > /tmp/x.log 2>&1; echo "EXIT=$?"` then read the log. The login shell here runs with `set -e`, so also guard any command that can legitimately exit non-zero (a `grep` with no match) with `|| true`.
- **Avoid apostrophes in heredoc commit bodies.** Commits use `git commit -m "$(cat <<'EOF' ... EOF)"`; a stray apostrophe inside the quoted heredoc breaks the quoting and produces a garbled or failed commit. Reword to drop it, or use `git commit -F /tmp/msg.txt` for a body that must contain one.
- **Realign onto live main before working** (see anchor-drift above) and **commit one coherent commit on your branch, do not merge or push** — the orchestrator cherry-picks. `git add` only your exact paths; the tree is shared with other agents.

These five are the standard preamble; paste them into every worktree brief.

### Commits

- Subject line: present tense, concise (under 70 chars). Body explains the **why**, not just the what.
- Sign-off: `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`.
- **Never commit without explicit user confirmation.** When unsure, ask.
- **Never push without explicit user confirmation.** "Push when ready" is the default until the user says push.
- Default remote is `origin`; `main` is the canonical branch. Feature work lives on `claude/*` branches and merges into local `main` after the user verifies the UI. Push to `origin` only on explicit confirmation. **Never `--force` push to `main`.** If the user asks for a force push, double-check the target branch first.
- `git add` specific paths, not `-A`. Other agents may have parallel uncommitted work in the tree.
- Use `git commit -F /tmp/researchOS-commit-msg.txt` for multi-paragraph messages with apostrophes — shell HEREDOCs choke on `'` inside `'…'`-quoted shells.

### Editing

- When the same worktree is shared with parallel agents, expect to see "intentional, do not revert" system reminders about files modified outside your scope. Treat those files as authoritative; don't undo their changes.
- Typecheck after every significant change: `cd frontend && npx tsc --noEmit`. The bar is exit 0.
- ESLint warnings are fine; new errors are not.
- **Icon-only buttons** → wrap in `<Tooltip>` from `frontend/src/components/Tooltip.tsx`. Native HTML `title=` is functionally invisible in this app (custom rendering layer hides it) — never use it for tooltips on new code. The native-tooltip migration sweep is mostly done; any new component should default to `<Tooltip>`.
- **Icons are a verified library.** Every icon comes from `@/components/icons` via `<Icon name="..." />`; the registry in `frontend/src/components/icons/registry.tsx` is the single source of truth (the icon equivalent of `brand/`), and the always-in-sync catalog lives at `/dev/icons`. The `icon-guard` vitest test (`frontend/src/components/icons/__tests__/icon-guard.test.ts`) ratchets against `frontend/icon-svg-baseline.json` and blocks any NEW inline `<svg>` outside the icon registry; migrating an existing inline SVG onto `<Icon>` is always allowed (the guard only allows the count to decrease). Adding a new icon means adding ONE registry entry, which is a VERIFIED ASSET and requires Grant's explicit sign-off before it ships — surface the new glyph to him, do not self-approve, and do not regenerate the baseline (`scripts/update-icon-baseline.mjs`, the escape hatch) to silence the guard. Migrate the ~237 grandfathered inline-SVG files onto `<Icon>` opportunistically when you are already in a file. **Commit-time backstop (2026-06-08).** A `pre-commit` hook now runs a fast copy of the ratchet (`frontend/scripts/icon-guard-precommit.mjs`, which calls `buildBaseline()` directly, no vitest boot) and BLOCKS a commit that introduces a new inline `<svg>` under `frontend/src`. It is scoped to fire only when staged files include `frontend/src` `.ts/.tsx` (docs/mockup commits pay nothing), fails OPEN on any tooling error, and is shared by all worktrees via git's common hooks dir, so it catches sub-bot commits too. It exists because a 10-`<svg>` regression reached main on 2026-06-08 without anyone running the guard, then got fixed twice. The hook lives in `.git/hooks/pre-commit` (not version-controlled) and is now a COMBINED hook (icon guard + credits self-heal, see the open-source credits note below) installed by the tracked `scripts/install-git-hooks.sh`; if `.git/hooks` is ever wiped, run `bash scripts/install-git-hooks.sh` (do not hand-edit the hook, edit the installer). Bypass in a true emergency with `git commit --no-verify`. The vitest `icon-guard` remains the source of truth in the suite.
- **One glyph per MEANING, not per word (Grant, 2026-06-12).** The icon library exists to dedupe the genuinely SAME thing, never to force semantically-different buttons onto one glyph. The rule both ways: if two buttons mean the SAME thing, they MUST share an icon; if their MEANING or intent differs, they MUST have distinct glyphs even when the word matches. A near word-match is NOT a license to reuse, watch for over-reuse where a glyph gets grabbed as generic decoration for an unrelated concept. Worked example that triggered this: the `tree` glyph was doing quadruple duty across the Check-ins lab mentorship hierarchy (people), the inventory StorageMap (physical-location nesting), and two unrelated "Results" section headers (Data Hub + sequence history, which are not "a tree" at all). Before reusing an existing glyph, check that its `concept` actually matches your meaning; if it does not, propose a NEW registry entry and get Grant's sign-off (icons are verified assets) rather than overloading a same-word glyph.
- **A popup must always read as a contained surface** (2026-06-08, Grant). A popup/modal/dropdown may NEVER be the same color as what is behind it without a visible border to contain it, or it becomes invisible (the loading popup that vanished mid-screen) or flat (every nested card the same near-black). Dark-mode shadows and `ring-black/*` are BLACK on a near-black page, so they do nothing. Depth in dark comes from DISTINCT SHADES PER ELEVATION (like Claude Code's nested greys), not from a crisp border line, which the eye reads poorly (Grant 2026-06-08). The dark elevation ladder: page `--surface` (#0a0e1a, darkest) < cards `--surface-raised` (#1c2638) < popups/modals `--surface-overlay` (#28344c, top, lightest); the `--border-subtle` hairline is a gentle white/0.16, not a hard line, so shade carries the separation. New popups should use the shared `LivingPopup` (which is `bg-surface-overlay border border-border`); custom overlays must use `bg-surface-overlay` (NOT `bg-surface`/`bg-surface-raised`) plus `border border-border`. The WCAG `contrast.gate.test.ts` enforces AA text on `--surface-overlay` too.
- **Brand tokens, not raw hex/sky utilities** (2026-06-05). Colors are tokenized in `frontend/src/app/globals.css`: use `brand-sky` (#1AA0E6, identity blue — BeakerBot + large marks only, fails AA for small text), `brand-action` (#1283C9, the accessible UI accent — text/links/small accents), `brand-ink` (#111827, headings/wordmark), `brand-purple` (#5B47D6). The rainbow lives in `--brand-rainbow{,-vertical,-vivid}` + the `.brand-rainbow-bg` / `.brand-rainbow-text` utilities. Don't hand-roll `#1283c9` / `text-sky-600` for new accents.
  - **The "ResearchOS" lockup** → use `<Wordmark>` from `frontend/src/components/Wordmark.tsx` (mark + wordmark, weight 800, brand-sky mark, brand-ink text). Never hand-roll `<BeakerBot> + <h1>ResearchOS</h1>`; that is how it drifted into three different weights/blues before.
  - **The BeakerBot mark / ResearchOS logo** → render ONLY via `<BeakerBot>` (or `<Wordmark>` / the SVG masters in `brand/`), never a hand-rolled beaker SVG. BeakerBot is ALWAYS the signature sky-blue, never black: the component self-applies `text-brand-sky` whenever you don't pass an explicit text color, so a sizing-only `className="h-28 w-28"` can't make him inherit a dark stroke (the old welcome-card bug). Guarded by `BeakerBot.brand.test.tsx`, do not remove that guarantee. Only override the color with a deliberate `text-*` class when you genuinely need it (rare); the liquid is a fixed rainbow gradient regardless.
  - **Primary CTA buttons** → use the `.btn-brand` class (the brand action→purple gradient, AA white text). When your arc touches a feature with `bg-sky-600` primary buttons, convert them to `.btn-brand` as part of that work (the app-wide sweep is deliberately per-arc, not a blind global pass). Leave toggles / segmented controls / calculator keys on their existing fills — `.btn-brand` is for prominent primary actions only.

### Surfacing design questions + feedback to Grant as interactive HTML (2026-06-07)

Grant's standing preference: for questions and feedback about areas of the website, **especially design things, and especially when there are multiple items to weigh**, deliver a self-contained INTERACTIVE HTML file he opens in the browser, not a markdown wall. The bar is `docs/audits/2026-06-07-surface-audit.html` (the surface-audit report).

- **Self-contained single `.html`** (no server, no build step, all CSS/JS inline), saved under `docs/` and opened with `open <file>` so it pops in his browser.
- **One card per item** with the concrete detail (file, element, why) plus a **visual whenever it helps the judgment**. For color/contrast items render live **before/after swatches** built from the real light + dark token hexes in `frontend/src/app/globals.css`, with the **computed WCAG ratio** shown, so the problem is visible, not just described. Structural items (depth, spacing, loading states) get a short note.
- **Interactable + copyable back to chat**: filters/search across items, and per-item decision controls (Agree / Keep as-is / Discuss + a free note) that **persist to `localStorage`** and **export to a plain-text summary he can copy straight back into the chat**. The whole point is he marks it up, hits Export, and pastes the result for the fix pass.
- **When to use which**: this HTML format for surface/design audits, "do you like X across these areas" reviews, and any multi-item design triage. A quick single fork still uses `AskUserQuestion` (clickable options). The HTML is for when there are many items or he needs to SEE it.

**UI redesigns specifically (2026-06-07, locked on the BeakerSearch palette).** When I have a concrete redesign to propose, Grant reviews it as an interactive **before/after** HTML mockup BEFORE I build it. The bar is `docs/mockups/beakersearch-palette-redesign.html`. The mockup must:

- Show **before vs after side by side**, faithful to the live tokens in `frontend/src/app/globals.css`, with a **light/dark toggle**.
- Be **actually interactive** (clickable rows / selection, arrow-key nav, hover states), not a static picture, so he can feel the change.
- Let him **add comments in the HTML**, a "comment mode" where clicking any element drops a numbered pin and opens a note editor; pins persist (`localStorage`) and list in a side panel.
- Let him mark **each proposed change Approve or Disapprove** (colored, persisted), and tag individual comments with a verdict too.
- **Export** everything (Copy to clipboard + Download .md) as a Decisions list plus comments, so he pastes one clean artifact back into chat.

He reviews change-by-change (approve / suggest / deny each), not all-or-nothing. After he marks it up, build exactly what he approved in a worktree, verify, merge. Mockups live in `docs/mockups/`. Strict voice applies to mockup copy too (no em-dashes, no emojis, no mid-sentence colons).

### Bot-driven UI verification (2026-05-13)

The master bot can spawn a sub-agent with Chrome MCP access to verify UI fixes against `?wikiCapture=1` fixture mode (`mcp__Claude_in_Chrome__*`). The constraint set that proved necessary on the first run:

- **For this fixture-mode flow, do not load `localhost:3000` against the real folder.** Drive the parallel `localhost:3001` instance instead (start it ahead of time with `next build && next start -p 3001`). Re-check before each navigation — a redirect can quietly land on `/`. (For plain eyeball checks you CAN attach a browser to Grant's live `:3000` in demo mode, no second server involved — see "Agents may drive Grant's live :3000 in Chrome themselves" above. The thing that stays forbidden while :3000 runs is SPAWNING a second dev server against the same `frontend/`.)
- **Append `?wikiCapture=1` to every URL** to skip the File System Access picker and seed fixture data (2 users: alex + morgan). The mock signs in as alex by default. The fixture covers the methods/PCR/results/lab-mode surfaces today; `users/alex/_shared_with_me.json` seeds a shared project (morgan/1) and a shared task (morgan/5) for receiver-side verification.
- **Use `mcp__Claude_in_Chrome__tabs_create_mcp` for every navigation.** Bots should not navigate the user's existing tabs. Close every MCP tab via `tabs_close_mcp` when done.
- **React `onClick` handlers don't appear as inline `onclick` attributes in the DOM** — they're bound on the React fiber. A bot that greps the rendered HTML for `onclick=` will erroneously report "no handler wired" when the click handler is actually there. Brief the bot explicitly on this so it doesn't false-FAIL React-handled events. Use `tabs_context_mcp` + actual click events to test, not DOM inspection.
- **Unsaved-changes "Leave site?" guards trap tab close.** If the bot edits any markdown body during verification, the popup's beforeunload guard will block subsequent `tabs_close_mcp` calls. Either don't edit (read-only verification only), or dismiss the dialog before closing.
- **Bot brief shape that worked:** project one-liner → expected commit hash → setup steps (ToolSearch deferred tools, list_connected_browsers sanity check, dev-server URL) → hard rules (no :3000, no non-fixture URLs, read-only, MCP tabs only, time budget) → recipe table (one row per fix with explicit pass/fail criteria) → cleanup step → report format (one line per recipe, max 300 words). See the chip-sweep verification recipe in the 2026-05-13 session for a working template.

Fixture coverage gaps to think about when adding new verifications: if a recipe needs `is_shared_with_me` data, extend `scripts/generate-demo-data.mjs` (the fixture is regenerated from there into `wiki-capture-fixture.ts`). If a recipe needs to verify a real-data behavior (the user's actual files on disk, real telegram/calendar tokens, prod OAuth env), it's not bot-doable — punt to the user.

### Agents may drive Grant's live :3000 in Chrome themselves for eyeball checks (2026-06-13, standing rule)

Pointing the Claude in Chrome MCP (`mcp__Claude_in_Chrome__*`) at Grant's already-running `localhost:3000` is SAFE and encouraged for human-eye verification (visual rendering, did-the-badge-show-up, does-the-control-actually-work). A browser attached to the existing server starts no new process, so it cannot corrupt anything. You do NOT need to hand every visual check to Grant; do the quick ones yourself inline so a real human eye has confirmed the change.

- **Always go through DEMO MODE so you never touch Grant's real research folder.** Navigate to `http://localhost:3000/demo` (seeds the fictional in-tab lab, signs in as `alex`, unlocks the data-folder gate). Demo data is fictional and resets on reload, so editing demo tables to force a state is fine and expected (e.g. flipping cells to make a dataset perfectly separable to trip the logistic Firth fallback, 2026-06-13). The `?wikiCapture=1` fixture path also works.
- **This is DISTINCT from the no-:3000 rule, which is about SPAWNED servers, not about attaching a browser.** What you must never do while Grant's :3000 is up is start a SECOND dev server against the same `frontend/` (Claude Preview MCP `preview_start`, or a bare `next dev`). Two servers share the one `.next` dir and corrupt Turbopack's persistent cache. So: attach a browser to the live :3000 = fine; spin up your own server = only on a separate port with its own build dir, or use the parallel-`:3001`/fixture setup in the section above, or not at all while :3000 runs.
- **Mechanics that held up:** `list_connected_browsers` to confirm the extension is live, then `navigate` + `read_page`/`get_page_text` (both deterministic, no model call) + coordinate clicks via the `computer` action. Note `find` routes through a model call and can hit this account's 5-hour rate limit; when it 429s, fall back to `read_page` (accessibility tree with refs) + coordinate clicks, which keep working. `zoom` + `save_to_disk` gives Grant a crisp proof image.

**BIG checks still go to Grant (2026-06-13, standing preference).** For involved / multi-surface verification, Grant prefers to run the Claude in Chrome extension himself rather than have an agent tie up a session babysitting the browser, so the agents stay freed to work on other things. Use judgment on the line: a quick "did the value labels render, did the Firth note appear" eyeball, do it yourself inline; a long walkthrough that would monopolize a session, hand Grant a self-contained prompt (same shape as the mouse-driven section below). This is the general case of the mouse-feel preference that already lived in the next section.

### Mouse-driven testing: hand Grant a Chrome-extension prompt (2026-06-12, standing preference)

Grant's standing preference: when a feature needs MOUSE-driven testing (real drag-and-drop, long-press, pointer reorder, hover, anything where genuine pointer movement matters), do NOT default to driving it yourself through Claude Code (Preview MCP / Playwright / a headless verifier bot). In practice the **Claude in Chrome extension driving Grant's own browser works noticeably better for this than an orchestrator-run server**, and Grant prefers it. So for mouse-heavy verification, write Grant a self-contained prompt he can paste straight into the Claude in Chrome extension and run himself. He pastes it, the extension drives the mouse against his live app, he relays the result.

Synthetic `dispatchEvent` pointer events often do NOT cross a component's drag move-threshold (confirmed on the nav drag-customize build, 2026-06-12); only a genuine pointer drag fires it. That is the core reason the extension path is more reliable for this class of test.

Keep using orchestrator-driven Preview/headless verification for everything that is NOT mouse-feel dependent (structure, snapshots, console errors, responsive resize, navigation, state reads, persistence wiring) and for work Grant should not have to babysit. Reserve the hand-Grant-a-prompt path for the mouse-feel bits.

The prompt you hand Grant should be self-contained and explicit, since the extension has no session context:
- One-line feature description + the exact URL/route to open (his `localhost:3000` is fine here, it is HIS browser and HIS choice, the no-:3000 rule is only for orchestrator-spawned servers).
- Any setup (connect a scratch folder, enter a mode, navigate to the surface that shows the feature).
- A numbered list of mouse actions with the EXACT pass/fail criterion for each (for example, "drag the Methods tab left of GANTT, release, confirm the inline order becomes Workbench, Methods, GANTT").
- What to report back (per-step pass/fail + anything that looked off).
- House voice still applies (no em-dashes, no emojis, no mid-sentence colons).

PRELOAD THE DATA, never make Grant (or the extension) hand-enter it. The extension drives the DOM, NOT the native OS file-open dialog, so the File System Access folder picker is NOT extension-drivable. Use demo mode instead: open `http://localhost:3000/<surface>?wikiCapture=1`, which installs the in-memory demo fixture and signs in as `alex` (the demo PI) with zero folder picker. If the feature needs data that the demo lab does not already have (e.g. a new analysis type, a new table shape), SEED IT into the demo fixture first so the agent opens straight into a populated surface and spends its clicks on the feature, not on data entry. The Data Hub demo tables are seeded via `frontend/src/lib/datahub/__seed__/seed-datahub-demo.test.ts` (run `SEED_DEMO=1 npx vitest run src/lib/datahub/__seed__` to regenerate the on-disk `.json`/`.loro` mirrors under `public/demo-data/users/alex/datahub/`) AND inlined into the browser fixture `frontend/src/lib/file-system/wiki-capture-fixture.ts` (the generator script is gone, so hand-add the new `["users/alex/datahub/<id>.json", {...compact json...}]` line + bump the `datahub` count in both the on-disk and inlined `_counters.json`); gate with the seed test + `demo-datahub-chemistry-fixture.test.ts`. Reuse a validated dataset (e.g. a transparency-pinned one) so every fit converges cleanly. Once it loads in demo mode, navigate by in-app clicks, not URL edits, so the fixture stays installed.

### Claude Preview MCP verification preamble (2026-05-25)

When dispatching a sub-bot that uses `mcp__Claude_Preview__*` tools to drive the live app, run this preamble first or the bot will silently test stale code. The trap: a previous round may have patched `<worktree>/.claude/launch.json` to start `node /tmp/proxy_<port>.js` which forwards to a long-stale `next-server` process. Claude Preview faithfully connects to that proxy and sees code from whatever commit the dev server was first started against, even though source on disk is current.

Symptom: behavioral tests show fixes "regressed" while source-grep confirms they're present. Round 2 personas (2026-05-25) burned hours filing false-positive P0s before one halted on a sanity check.

Verification preamble (bake into every Preview-using brief):

1. Kill every Next.js dev server + proxy on the common port range: `lsof -nP -iTCP -sTCP:LISTEN | grep -E '300[0-9]'` then `kill -9 <PIDs>` individually (the `lsof -ti ... | xargs kill` form has a race that misses some PIDs).
2. `rm -rf /Users/gnickles/Desktop/ResearchOS/frontend/.next` and `rm -rf .../node_modules/.cache` so any restarted server compiles fresh from current source.
3. Delete every `/tmp/proxy_*.js` and `/tmp/r7d_proxy*.js` so no stale proxy can be auto-restarted by `launch.json`.
4. Inspect `<worktree>/.claude/launch.json`. If any entry runs `node /tmp/proxy_*.js`, overwrite it with a direct dev-server launcher: `bash -c "cd /Users/gnickles/Desktop/ResearchOS/frontend && exec npm run dev -- -p <free port>"`. Preview rejects absolute `cwd` values, hence the `bash -c "cd ..."` wrapper.
5. `preview_start` → fresh `next-server` spins up, compiles in ~5s from current source.
6. **Immediately call `preview_resize` to 1440x900.** The iframe default is ~726px wide, which silently puts right-edge nav elements off-screen and breaks `elementFromPoint`-based assertions. This single step prevented at least one Round 2 false positive about "demo overlay intercepting nav clicks" — the nav element was just out of frame.
7. Sanity check: `preview_eval` for a known recent feature (e.g. `document.body.innerHTML.includes("Edit session")`) to confirm the live bundle matches expected commit. If the check fails, the cleanup didn't take — re-run steps 1-5.

Round 2 reports tagged everything that worked as "FALSE_POSITIVE" once the live app was actually serving current main. Don't re-walk this trap.

### True fresh-user verifier setup (2026-05-25)

Fresh-eyes verifiers must walk the tour as a **real first-time user** in their own fresh account, NOT in demo mode. Default behavior the bot will reach for if you don't pin it down: signing into one of the seeded demo fixtures (Mira / Alex / Morgan) and walking from there. Wrong. Those accounts have weeks of pre-seeded research data, dozens of widgets, custom layouts, real comment threads. A first-time user has none of that. The friction findings you actually want emerge from the empty-state experience.

Setup the bot must perform before driving the tour (bake into every fresh-eyes brief):

1. From the picker, **click "Connect a new folder"** (or whichever copy is current) instead of selecting a fixture. Point at a temp scratch directory (e.g. `/tmp/researchos-verifier-<agent-id>/`) where the bot has read+write access. The scratch dir must be empty so the first-run gate fires correctly.
2. Walk through pre-onboarding (display name, lab name, color, etc.). Use plausible-but-disposable values, not "test" or "asdf" (those bias copy that mentions the user's name to read as broken).
3. Trigger the v4 walkthrough through the normal first-run path (it should auto-start on the empty account). The dev "force walkthrough" button is acceptable if the auto-start gate doesn't fire on the bot's environment, but note it in the report.
4. Walk the full sequence. Take the same actions a real user would: read the speech bubbles, click through, don't peek at source.
5. When the section ends, clean up: kill the dev server, `rm -rf /tmp/researchos-verifier-<agent-id>/`.

The demo-mode-vs-real-mode critique scaffolding fresh-eyes verifiers should explicitly apply: for every section, ask "does this concept teach better with the empty real account, or would it land more clearly if BeakerBot navigated me to the demo lab to show a populated example?" Some concepts (creating a project, naming things) teach best in the user's own account; some (busy Gantt, member workload widget, comment threads) need pre-seeded data to make sense. The verifier reports which side of that line each step falls on, and flags any mismatch where BeakerBot is teaching a data-dense feature against an empty canvas, OR is keeping the user in demo mode for a hands-on flow they should be practicing in their own account.

Mechanics verifiers can stay in demo mode (DOM-level checks are mode-agnostic). Spec-compliance verifiers don't drive the live app at all. Only fresh-eyes needs the real-user setup.

### Preview MCP FSA-picker limitation (2026-05-25)

A Preview-MCP-driven sub-bot CANNOT execute steps 1-2 of the runbook above. `window.showDirectoryPicker()` opens a native OS dialog Preview cannot drive (no FSA polyfill exists in `frontend/src/lib/file-system/`). Pick a fallback in this order:

1. **`?wikiCapture=picker&wizard-preview=1`** (preferred). Picker variant installs the in-memory fixture but leaves `currentUser` empty, so `UserLoginScreen` renders. Use the dev "Force walkthrough → User setup walkthrough" button to mint a `Test-N` user; the `wizard-preview` flag flips `isV4PreviewMode()` true so `V4MountForUser` mounts and the tour auto-fires on the fresh sidecar. Caveats: alex / morgan / mira appear in the picker (ignore), Test-N lives only in fixture memory, hard nav back to `/` re-seeds the fixture and wipes Test-N. Stay on soft routes only.
2. **`?wikiCapture=1&wizard-preview=1&wizardSeedStep=<step>`** (last resort). Loads alex's pre-seeded fixture and lands at the requested step. Defeats the empty-canvas premise but lets you exercise step bodies in isolation. Flag any fresh-eyes finding made under this mode as "fixture-data caveat" in the report.

Worth filing as separate fix chip: the Dev "User setup walkthrough" button mints a Test-N user that does NOT auto-fire the v4 tour in plain `?wikiCapture=1` mode (no `wizard-preview` flag). `providers.tsx:192` gates `V4MountForUser` on `isV4PreviewMode()` inside the fixture branch, so the new Test-N sidecar's `wizard_force_show: true` has nothing to mount into. Either drop the gate when a Test-N user is active, or have the dev button stamp the sticky preview flag itself. **Fixed 2026-05-25** at `0984fe02`: dev button now stamps `V4_PREVIEW_STICKY_KEY` before user swap. Real-folder paths unchanged.

### Don't preview a worktree-only fix from master cwd (2026-05-25)

When verifying a fix that lives on a sub-bot worktree branch (not yet cherry-picked to main), DO NOT start a Preview MCP server from the master worktree's `cwd`. The master's dev server compiles from `frontend/` on the master branch, which does NOT contain the worktree branch's changes. You will see "ghost" pre-fix behavior even though source-grep on the worktree branch shows the fix is present, leading to false negatives and dispatch of unnecessary debugging chips.

Symptom observed 2026-05-25 during the §6.1 nav-fix chain: a click-swallow "regression" was traced to a stale preview the verifier launched from master's checkout. The fix existed on the worktree branch but the preview was serving main's pre-fix code.

Rule: when verifying a worktree-branch fix, either (a) cherry-pick first then preview from master, or (b) start the preview from the worktree directory directly (with the bash-cd-into-frontend launch.json pattern using absolute worktree paths). Never split the worktree branch from the preview's source root.

### Mechanics verifiers: full sequential walk required (2026-05-25)

Seed-jump probing and sequential walks are complementary, not substitutes. Mechanics verifiers MUST do at least one full sequential walk per redesign verification, in addition to any per-step seed-jump probing. Seed-jumps isolate a single step's behavior from a clean room; sequential walks expose state-dependent bugs where a flag or DOM mutation set in an earlier step changes how a later step behaves.

The runbook for every mechanics-verifier brief must include: "Walk sequentially from `<start step>` to `<end step>` at least once before declaring the section verified," alongside any per-step `wizardSeedStep` probing the brief calls for. Treat the sequential pass as the integration test and seed-jumps as the unit tests.

Recent example (§6.2b R3, home widgets). The R3 mechanics verifier seed-jumped directly into Step 5 from clean fixture state and reported the onEnter actions as PASS. The R3 fresh-eyes verifier walked sequentially from Step 1 and reported Step 5's onEnter `el.click()` BROKEN: the add-demo action in Step 3 had armed the InputLockOverlay's capture-phase handler, which then swallowed the Step 5 click. Seed-jumping bypassed the Step 3 setup entirely, so the bug was invisible to the mechanics pass.

### Verifiers fix spotlight / highlight-box bugs in-place (2026-05-25)

Standard verifier rule: read-only, file findings, defer fixes to a separate chip. Carve-out: the v4 tour's blue spotlight / highlight box around UI elements. Verifiers with the live Preview MCP session already up should fix those in-place and re-verify in the same session, rather than round-tripping to a separate fix chip.

Why: visual positioning bugs (wrong `data-tour-target` selector, target off-screen, ring sized wrong, dim layer mis-placed, spotlight pointing at parent when it should point at child) require live render to dial in. A separate fix chip has to re-setup the Preview environment, re-walk the tour to the failing step, and iterate without the verifier's existing mental context. In-place fix is strictly less work.

Brief addendum to bake into every walkthrough-verifier brief: "Standard rule: do not modify code. Exception: spotlight / highlight-box positioning bugs (wrong target selector, off-screen target, ring sized wrong, dim layer mis-placed). For those, fix in-place + re-verify in the same preview session; report the diff + commit hash alongside the verification finding. All other bugs file as findings; master will dispatch a separate fix chip."

Verifiers still defer fixes that touch step bodies, copy, sequence logic, or anything requiring a design decision.

### Field migrations

When a field on Task / Method / Project / Note / etc. is renamed or restructured, follow the **lazy-normalize + on-demand-repair** pattern the cleanup pass landed (commit `147db270`, 2026-05-13). The whole point is that shared on-disk files from other users with legacy shapes keep working transparently — no flag-day cutovers, no broken receivers.

1. **Rename the field** in `frontend/src/lib/types.ts` and update every caller in lockstep. (See the §6 "Field renames" trap for the grep checklist — partial renames silently typecheck.)
2. **Add a `normalize<Entity>Record` helper** at the read boundary in `frontend/src/lib/local-api.ts`. Templates: `normalizeMethodRecord`, `normalizeTaskRecord`. The helper detects the legacy shape, rewrites it in-memory to the new shape, returns the normalized record. Apply on every read path (`.get`, `.list`, `fetchAllXIncludingShared`, …) so callers never see the legacy shape.
3. **Add a one-shot repair button** to `frontend/src/app/settings/page.tsx` under "Data maintenance." It iterates every stored file of that entity, normalizes, writes back. Report scanned / repaired / already-clean counts. Safe to re-run.

Use this for any field rename. **Do NOT do hard on-disk cutovers** — rewrite-and-leave-no-fallback breaks shared-from-another-user files at the moment the migration lands.

---

## 5. Integrations in flight

### Data Hub (GraphPad Prism alternative, FEATURE-COMPLETE behind the flag 2026-06-11)
The final major feature: a free open-source Prism alternative (statistical analysis + scientific plotting) in its own `/datahub` tab, fully client-side (NO backend, all compute in the browser, data in `users/<owner>/datahub/<id>.{loro,json}`). Locked architecture: native-JS engine (`@stdlib/stats` + `ml-levenberg-marquardt` + `ml-matrix`, with the covariance/SE/CI math authored in-house like scipy `curve_fit`); optional opt-in lazy Pyodide kernel only, never default; plotting is a custom SVG layer on the in-bundle `d3-scale`/`d3-shape` (free vector export, every figure exports SVG + hi-DPI PNG + copy-to-clipboard); Loro CELL-LEVEL version-controlled storage; project/subfolder scoped like sequences; `exceljs` round-trip (read data+formulas, re-plot natively; embedded Excel charts are NOT round-trippable client-side, accepted). The differentiators that beat Prism: a guided test-selection wizard with a plain-language assumption Report Card (now spans every table type), show-the-code (the equivalent scipy/statsmodels/lifelines Python), auto error bars from replicates, one-click significance brackets, and a reference-pinned validation gate.

**FEATURE-COMPLETE on main, all flag-gated + verified (`DATAHUB_ENABLED`, default OFF).** Foundation: engine `120cedb24`, cell-level model+api `9e6ee3963`, tab mockup `9477a1056`. Slices: Column grid + analyses + results/show-code (`1321dc502`, `85eddac09`), publication graphs (`c20e39e6c`), guided wizard (`843dc752a`), CSV/paste + xlsx import (`78e6d2968`, `3d43244f6`). All THREE remaining table types shipped: XY (correlation + linear regression + fitted-curve scatter, `573dce82c`), Grouped (two-way ANOVA + grouped bar, `c1481e1c4`), Survival (Kaplan-Meier + log-rank, NEW engine math `engine/survival.ts` reference-pinned vs R survival on the aml dataset, `0a7ecb47f`). Embed phase (`2911ae7e6`): a `"datahub"` ObjectRefType + `/datahub?doc=<id>` route + ObjectChip + page deep-link resolver + Copy reference button. Wiki page `/wiki/features/datahub` + `APP_ROUTE_TO_WIKI` (`b352a145a`). Guided wizard extended to two-way + survival (`9521f1858`). Demo deep-link query-preservation + resolver fix (`db3107d2f`). ENGINE TRUTH held throughout: only survival added new math, reference-pinned; no model/types or on-disk schema changes for the new table types (they reuse the x/y/group roles + datasetId/subcolumnKind). 250+ datahub tests green incl. a persist->reload round-trip and analysis edge/guard cases. See it: `NEXT_PUBLIC_DATAHUB_ENABLED=1` in `frontend/.env.local`, restart, open `/datahub` (or use demo mode, which seeds fixtures).

**Remaining before public launch (Grant's call):** (1) flip the prod flag `NEXT_PUBLIC_DATAHUB_ENABLED` (Vercel env + redeploy; it bakes at build); (2) capture the fixture-mode (`?wikiCapture=1`) annotated screenshots the wiki page references (it shows clean "pending capture" placeholders until then). Any future work: consume the engine/model/store (read their real exports), do not modify the locked shapes; every new statistic needs a reference-pinned test. Full inheritor briefing: `/Users/gnickles/Desktop/HandoffForAgents/DATA_HUB_HANDOFF.md` (now historical).

**v2 STATS EXPANSION (the 18-card proposal `docs/mockups/data-hub-v2-proposal.html`, Grant "do it all"). THEME 1 + THEME 2 COMPLETE on local `main` (2026-06-12, NOT pushed, NOT browser-verified).** Every new statistic ships under a HARD GATE Grant enforces: a `/transparency` validation pin vs scipy/statsmodels/R AND its show-the-code Python, in the SAME arc (never a follow-up). The deeper UI phases (change-params+rerun, right-click CRUD, plot-code export, subcolumn formats, transforms/wrangling engine phases 1-2, exclude-value, lineage chain-export) all landed earlier. THEME 1 (estimation): E1 effect sizes + CIs (Hedges g, eta2/omega2, noncentral-t/F CIs, authored in `engine/dists.ts`), E2 estimation plots (Gardner-Altman + Cumming), E3 power/sample-size (`engine/power.ts` + PowerPlannerDialog), E4 bootstrap CIs (`engine/bootstrap.ts`, seeded mulberry32 percentile + BCa). THEME 2 (dose-response + regression): D1 dose-response 4PL/5PL + EC50/IC50 (`engine/fit/`), D2 model comparison (extra-sum-of-squares F + AICc, `engine/fit/compare.ts`), D3 global/shared-parameter fitting (`engine/fit/global.ts`, one model across several Y curves with shared/local params), D4 simple logistic regression (`engine/regression-logistic.ts`, IRLS MLE), D5 multiple linear regression (`engine/regression-multiple.ts` + shared `engine/linalg.ts`). Each is a new AnalysisType + a `Normalized*` result + a chooser entry + a ResultsSheet table, additive on data shapes. The transparency `datahub-stats` domain gate (`datahub-stats.gate.test.ts`) enforces `comparisons.length === STAT_PINS.length`; ~104 pins. The BeakerAI session built consume-only BeakerBot tools for all 5 Theme 2 analyses (lane split holds, engine owns every number). DEMO DATA: every Theme 2 analysis has a seeded demo table (alex demo lab datahub docs 4-7, in `seed-datahub-demo.test.ts` + inlined into `wiki-capture-fixture.ts`), so `/datahub?wikiCapture=1` opens straight into populated dose-response / logistic / multiple-regression / global-fit tables. REMAINING: Theme 3 (survival/repeated-measures: Cox PH, Gehan-Breslow, RM-ANOVA, mixed-effects) and Theme 4 (diagnostics: ROUT/Grubbs, QQ/residual plots, ROC), each item with its validation pin + show-code from the start. Latest handoff: `/Users/gnickles/Desktop/HandoffForAgents/2026-06-12_datahub-v2-and-wrangling.md`.

### Phylogenetics (`/phylo`, BUILT behind the flag 2026-06-12, NOT pushed)
A new science tab (sibling to Chemistry + Data Hub), flag-gated `NEXT_PUBLIC_PHYLO_ENABLED` (default off, demo-visible). Client-side only, NO server compute, NO tree inference in-app (Grant's hard rules). Two halves: a **Tree Builder** that only GENERATES tree-building scripts (pure `generateRecipe(BuilderOptions)` in `lib/phylo/recipe.ts` filling a VERIFIED curated catalog `lib/phylo/catalog.ts`, every flag doc-checked; BeakerBot fills the catalog and calls the generator, it never writes a flag), and a **Tree Studio** iTOL alternative that renders trees in NATIVE SVG (`lib/phylo/render.ts` `renderTreeSvg`, never webR) with ggtree code EXPORT-only. Vetted wizard frozen spec: `docs/proposals/2026-06-12-phylo-wizard-build-spec.md` (3 pipelines: single / supermatrix-via-AMAS-then-IQ-TREE / coalescent-then-ASTRAL; advanced section; MrBayes scaffold). Demo seeds 3 REAL verbatim trees (`lib/phylo/__seed__/sources/`: C.auris/HMP/HPV58). Transparency proves our layout matches ggtree (Grant runs `scripts/gen-phylo-ggtree-golden.R` offline -> committed goldens; gate `phylo-plots.gate.test.ts` green, corr 0.99+/depth 1.0). Embeds: `phylo` ObjectRefType + `/phylo?doc=<id>` + `components/embeds/PhyloEmbed.tsx`; card markdown `[name](/phylo?doc=<id>#ros=studio)`. `render.ts` is a sanctioned baselined inline-`<svg>` (a data figure, like the datahub plots). BeakerAI contract: `docs/proposals/2026-06-12-beakerbot-phylo-contract.md` (they shipped consume-only `lib/ai/tools/phylo-tools.ts`; `generate_tree` + a sequence->FASTA binding still gated on us; Grant flagged auto PDF-extraction of a paper's phylo methods as the next synergy). IN PROGRESS: published-tree reproduction transparency (RF scorer `lib/phylo/rf.ts` done; cases HPV58/turtle/firefly-opsin sourced; framework + Grant's offline runs pending). GOTCHA: `-T AUTO` is a footgun on tiny alignments (38 min ModelFinder on a 6-seq smoke test). Full state: `docs/orchestrator-handoffs/2026-06-12-phylogenetics-handoff.md`. LAYOUT (v3, 2026-06-13): the Studio uses the shared split shell (`components/SplitShell.tsx`) with a left COLLECTION RAIL (`PhyloCollectionRail.tsx`) and a right TABBED ACTION RAIL (reuses `components/sequences/SequenceOperationsRail.tsx`); same rail shell as Sequences/Chemistry/Data Hub. See the dated UPDATE at the top of this file.

### BeakerBot + BeakerSearch (the AI assistant, MAJOR arc shipped 2026-06-12)
BeakerBot is the in-app AI assistant (browser-side agent loop, thin proxy, Fireworks gpt-oss-120b, only per-turn context leaves the device). BeakerSearch is the unified command palette it lives in. A separate running session "BeakerAI" owns this lane; the Data Hub itself is owned by the "Data v2" session, LANE SPLIT: BeakerBot CONSUMES the Data Hub engine/transforms/embeds READ-ONLY, it never duplicates them, and BeakerBot only does what a user could do by hand. Coordinate via shared docs + Grant relays (cannot send_message to Data v2, it is unsupervised).
- SHIPPED on main: the unified centered surface (search morphs into chat in place, de-clutter behind ">", adaptive dodge around the spotlight, dock retired, conversation persisted in a root store); context-awareness; the cross-type artifact index (search_my_work) + per-type read tools; the root popup host (notes/tasks/experiments open in place from a tile); coworker tools (run_datahub_analysis, make_datahub_graph, write_note, experiment/GANTT create+reschedule+chain with dependency arrows + GANTT highlight, sequence Tm/translate/revcomp/ORF/primers/create reusing the validated engines, chemistry PubChem/create/import, transform_table with a block-approval card); the analysis-finder "Help me choose?" wizard (system-prompt protocol); and inline EMBEDS (BeakerBot renders molecule structures + Data Hub result/plot views inline via Data v2's ObjectEmbed dispatcher, so new renderers auto-appear). All under `frontend/src/lib/ai/` + `frontend/src/components/ai/`; the prompt is `system-prompt.ts`.
- SHIPPED session 2 (2026-06-12): the FULL AI BILLING system, Phases 1-4 (Neon token ledger `ai_balances`+`ai_ledger`, proxy enforce+record in `/api/ai/chat`, Stripe one-time top-up `/api/billing/ai-topup` + webhook crediting, real balance/cost UI) behind the server env `AI_BILLING_ENABLED` (fail-closed when on, unset = byte-identical passthrough); design `docs/proposals/beakerbot-ai-billing-build.md` + `[[project_ai_billing_build]]`. GRANT RULE, NO free AI in beta, AI does NOT ship until billing + account-link are live (go-live checklist in the session-2 handoff). Also shipped: the molecular CLONING coworker suite (fetch_sequence NCBI / extract_feature / assemble_gibson / digest_ligate / list_sequences / read_sequence_features); `wrangle_table` (relational Data Hub transforms over `executePipeline`, `{sources, recipe}`); effect-size + robustness surfacing in run_datahub_analysis, estimation plots in make_datahub_graph, `plan_study` power tool, primer self-complementarity, PubChem property set + sidecar persistence; and a branded THINKING INDICATOR (morphing beaker-blue blob default + a grey live status line mapped from the agent loop `LoopStatus`; dev switcher for the beaker/blink alternates). The v4 onboarding-tour teardown LANDED (`effe6f60c`).
- SHIPPED (2026-06-12): chat-log export to a new/existing note OR an experiment Lab-Note / Result, with embeds carried over. A "Save to..." button in `BeakerSearchAskHeader` opens `ExportConversationPicker`; `lib/ai/conversation-to-markdown.ts` serializes the transcript and `sendMarkdownToTarget` (in `lib/references/send-to-target.ts`) appends open-aware as its own dated block (never clobbers). Embeds ride FREE because an assistant `message.content` already holds the `[name](/path#ros=view)` markdown, so the destination note/result renders the same live embeds.
- SHIPPED session 3 (2026-06-12): the FULL Theme 2 analysis-tool family (consuming Data v2's engine fields, all consume-only, non-gated, store + navigate + `#ros=result&analysis=<id>` embed, every number owned by the engine), `compare_models` (extra-sum-of-squares F + AICc model comparison), `run_logistic_regression` (binary Y vs the table X), `run_multiple_regression` (Y + 2+ predictors, predictors-to-spec), `global_fit` (shared-parameter curve fitting across 2+ Y curves); plus `get_analysis_code` (relays the engine's show-the-code Python so the model can drop the exact stat code into a note). Four BeakerBot polish items also landed (significance brackets in make_datahub_graph from a linked ANOVA, restriction-overhang cloning primers in design_primers, a since/until date window on search_my_work, method-template auto-attach in create_experiment_chain). The vial glyph was swapped for the real `<BeakerBot pose=...>` mascot on the assistant surfaces (conversation headers, "Help me choose", thinking beaker variant). **BeakerSearch's permanent home is now a bottom-center ask bar** (`BeakerSearchBottomBar`, Option A, opens the same surface as the pill/Cmd K; the top-nav pill stays with a TODO until the nav-slimming session coordinates its removal). **REVIEW MODES shipped** (replaces ask/auto): a two-mode control (step-by-step default, whole-plan) in `BeakerSearchAskHeader`, a `review-mode-store` (`getReviewMode()`), a `previewable` AiTool flag on the six instant analysis tools, and a rewritten `gateToolCall` (step mode previews+confirms every action OR previewable step; plan mode runs previewable free and gates actions until the plan is approved; destructive hard-stop ALWAYS confirms in both modes). Previews currently ride the existing approval card via each tool's sync `describeAction`; the bespoke rich per-step preview block is the queued follow-up.
- SHIPPED session 4 (2026-06-12): the SUMMARY SUITE (per-type deterministic aggregators `summarize_experiments/notes/projects/purchases/inventory` + cross-type `lab_digest` + the guided ask-only-when-ambiguous filter wizard + `list_lab_members`; the TOOL owns every count/total, the model only narrates). The HARD NO-INTERPRETATION scope is now a system-prompt rule (BeakerBot expands/summarizes the user's OWN content, relays tool data, operates the app; NEVER interprets results, concludes, hypothesizes, or invents science; textbook facts OK; one scoped carve-out for the PDF-reproduce flow = light FACTUAL method comparison only, `[[project_beakerbot_pdf_reproduce]]`). PHYLO read tools (`list_phylo_trees`/`read_phylo_tree` + tree-card embeds) and **`generate_tree`** (fills `BuilderOptions` from `@/lib/phylo/catalog`, calls `generateRecipe`, surfaces the runnable RecipeOutput, NEVER writes a flag; sequence->FASTA binding still the open co-design with the phylo lane). New stats consume-tools (nested t-test/ANOVA, contingency, Cox/ROC/RM-ANOVA/mixed/Grubbs). GUI: stop/cancel button (AbortController through `runAgentLoop`), follow-up chips (model `<!-- followups: ... -->` directive, parsed+stripped), provenance source chips (cite tool deepLinks, renders via ObjectChip), the living blue-blob thinking indicator restored. CRITICAL FIXES from a Chrome verification pass: lab_digest crash hardened (a thrown `undefined`/non-Error in `runToolCall`/`gateToolCall` could reach Next 16's `.digest` handler and BLANK the page; now normalized + the gate stays fail-closed), purchase money-MISCOUNT (`summarize-purchases` byId map keyed by plain per-user id collided across owners, double-counting; fixed with a compound `owner:id` key, true demo total $6,966.00/36 now asserted; SAME class fixed in experiments + notes), project ids resolved to NAMES, and a VERBATIM-ECHO rule (tools return preformatted `*Display` strings the model must copy char-for-char, never re-type a number). GOTCHA this session: `isolation:worktree` bots branch from a FIXED harness anchor that can predate your own in-session merges; before integrating a bot run `git merge-base main <branch>`, and CHERRY-PICK (not `merge --no-ff`) stale-base bots so overlap surfaces as a conflict, then grep a sentinel line to confirm earlier work survived (`[[feedback_subagent_anchor_drift]]`).
- QUEUED at pause: Bot C (reopen-blank chat restore + queue-a-message-while-streaming) is now MERGED (`b3e9494f9`, cherry-picked off the stale anchor, Build1 stop/followups verified intact, tsc 0 + 39 conversation tests green; root cause was a `revealAnswer`->`saveChat` race nulling `currentThreadId` so the save was skipped). NEXT UP and now UNBLOCKED: per-user memory (bounded file injected capped each turn + a remember tool + periodic consolidation; Grant: "keep a file on the user but occasionally consolidate it and always prevent it from getting too big"; edits `conversation-store.ts` + `system-prompt.ts` so it waited on C). The PDF-reproduce-analysis feature is SPEC'd (`docs/proposals/beakerbot-pdf-reproduce-analysis.md`, `[[project_beakerbot_pdf_reproduce]]`) and handed to the phylo lane to review outputs 3+4. Re-run the Chrome verification tail (tests D2/E/F/G never reached the usage limit).
- SHIPPED session 5 (2026-06-13): per-user MEMORY (bounded file + remember/forget tools + consolidation), `setup_experiment` composite, `save_summary_as_note` artifact, PDF-reproduce OUTPUTS 1+2 (`draft_paper_summary`/`extract_paper_method`, verbatim) + OUTPUT 3 (`generate_tree` extended + `assemble_tree_fasta`, contract converged with phylo), calculator/purchase/inventory tools, CHAT MODERNIZATION (live status line + per-turn token plumbing + Copy/Regenerate/Revert + the AI balance ring), IMAGE/VISION behind `NEXT_PUBLIC_BEAKERBOT_VISION` (router to `AI_VISION_MODEL`), PDF ingestion (pdf.js, real worker), VOICE input, BeakerSearch always-on (search free, only AI gated). Full detail in the session-5 handoff.
- SHIPPED session 6 (2026-06-13): the WHOLE A-LIST + PDF-reproduce OUTPUT 4. **@ mentions + / commands** (composer `@` picker over the global object index with PER-TYPE COLORED chips removable by x or Backspace-at-0; data-driven `/` registry summarize/plot/cite/digest/setup/draft that pre-fills intent; `ComposerMentionPicker`/`ComposerSlashMenu` + `attachedRefs` in conversation-store, merged `b03d4c02e`). **Editable Canvas** (docked right panel, tabs-per-draft with unsaved-dot, the read-only draft card REPLACED by a "Drafted in Canvas" pointer line; Save = consent writing the user's EDITED text via the draft descriptor's new `applyEdit` hook + a `draft-save` ApprovalDecision; `LiveMarkdownEditor` slimmed via `showShortcutsHelper={false}` + a new `hideAttachments` prop; new `BeakerBotCanvas.tsx` + `canvas-store.ts`, merged `475c391de` + polish `6b0a9fa54`). **`match_figure_style`** (Output 4: vision reads a paper figure's STYLE into a `PhyloFigureSpec`, writes it onto the user's OWN tree via `phyloApi.create`/`updateMeta`, opens Tree Studio `/phylo?doc=N#ros=studio`; built to the phylo lane's locked contract, NEVER binds PhyloStudio internals, no-interpretation held; merged `3aea4b024`). All three A-features LIVE-VERIFIED end-to-end with a real model via Claude-in-Chrome on a contained dev server, which surfaced TWO PROD-BREAKING bugs now fixed (see traps): the `stream_options` 400 and the dead `AI_VISION_MODEL` slug (use `kimi-k2p6`). The reported duplicate-send was investigated and confirmed a NON-bug (React StrictMode dev artifact) with a regression guard (`44eec7b52`). Vision model DECISION: Kimi for image turns is approved (US-hosted on Fireworks, wrapped by the same system-prompt guardrails). Only B-list (workflow macros, resumable plan card) remains.
- Full session handoffs: `docs/orchestrator-handoffs/2026-06-13-beakerbot-ai-session-6-handoff.md` (latest, the A-list @/commands + Canvas + Output 4 + live Chrome verification + the prod 400/vision fixes), `docs/orchestrator-handoffs/2026-06-13-beakerbot-ai-session-5-handoff.md` (memory + setup_experiment + summary-artifact + PDF outputs 1-3 + vision/PDF/voice + chat modernization), `docs/orchestrator-handoffs/2026-06-12-beakerbot-ai-session-4-handoff.md` (the summary suite + no-interp scope + generate_tree + verification fixes), `docs/orchestrator-handoffs/2026-06-12-beakerbot-ai-session-3-handoff.md` (the analysis-tool family + review modes + ask bar), `docs/orchestrator-handoffs/2026-06-12-beakerbot-ai-session-2-handoff.md` (billing arc), and `docs/orchestrator-handoffs/2026-06-12-beakerbot-ai-session-handoff.md` (session 1).

### Chemistry Workbench (ChemDraw alternative + free SciFinder-style literature search, FEATURE-COMPLETE behind the flag 2026-06-10)
A new opt-in module: a built-in molecular structure editor (a ChemDraw alternative), a project-linked molecule library, a browser-direct PubChem importer, and the headline differentiator, a free "find the papers and patents that mention this structure" companion (the thing chemists pay SciFinder for). Fully client-side, NO backend, behind `NEXT_PUBLIC_CHEMISTRY_ENABLED`. Stack researched + verified (two deep-research passes), design locked, mockup approved (all 14 proposals). Locked stack: Ketcher (Apache-2.0) canvas on Indigo wasm + RDKit.js (BSD-3) engine, ~5.7 MB gzip first-open lazy-loaded (accepted, the spike measured it); molecules stored as `users/<owner>/molecules/<id>.mol` (Molfile, keeps 2D coords) + `<id>.meta.json` (`MoleculeMeta` with `project_ids[]`), mirroring the sequences `.gb`+meta store. Literature is browser-direct, CORS-open, no key: PubChem `xrefs` (compound to papers+patents, ALREADY free, aspirin = 26k papers / 111k patents), Europe PMC Annotations (full-text chemical mentions), SureChEMBL (substructure to patent compounds, async submit/poll/results). CAS-proprietary parts (Markush deconstruction, CASREACT reactions, hand-curation) are deliberately NOT replicated, the copy must say so. Binary `.cdx` is the one real format gap (only OpenBabel reads it, GPL-2.0, license-incompatible), CDXML is best-effort via RDKit. Note PubChem renamed CanonicalSMILES to ConnectivitySMILES (2025), so derive SMILES from the SDF via RDKit, not the property endpoint.

**On main (all behind the flag, tsc clean + chemistry/icon suites green):** Phase 0 flag+seam+`MoleculeMeta` `d76bbc6a6`; design proposal + mockup + stack spike `f4df2f6bc` (`docs/proposals/CHEMISTRY_WORKBENCH_PROPOSAL.md`, `docs/mockups/chemistry-workbench-mockup.html` is THE visual spec and a LIVE demo, `docs/spikes/chemistry-stack-spike.html`); deps + `/chemistry-embed-check` probe `7132c7233`; Turbopack transpile + CSP `98aa004bd`; PubChem client + 3-source literature companion `014c3a725`; RDKit loader service + wasm assets `3b32f7a82` (merged `a513ae42d`). The editor-independent service layer is complete: `lib/chemistry/{config,api,rdkit,pubchem,literature}.ts`. **The Ketcher embed is CONFIRMED working (2026-06-10):** the `transpilePackages` recursion fix holds; the real blocker was the CSP, the Indigo Emscripten worker needs `'unsafe-eval'` (`'wasm-unsafe-eval'` is not enough, the violation fires in worker scope so it reads as a silent hang), fixed globally in `40ea1ac19`, verified live. The iframe pivot is NOT needed. **The `/chemistry` hub + structure editor are BUILT + live-verified (`66b65cd71`, reopen fix `00ec3738b`):** `molecule-store.ts` (the flagged data-shape, `molecules/<id>.mol` + `.meta.json`, unit-tested), `api.ts` wired to it (list/get/create/update/remove + listByProject, RDKit identity on save, the Phase 0 empty seam is gone), `ChemistryHub` (search/sort, 3 action cards, RDKit-thumbnail grid, empty state), `MoleculeEditorPopup` + `KetcherCanvas` (Ketcher in a LivingPopup + live RDKit identity rail + copy SMILES/InChIKey), `MoleculeThumbnail`, and a flag-gated Chemistry nav tab. Verified end to end: draw caffeine, save, it lands in the library with computed C8H10N4O2 / 194.19 g/mol + a thumbnail, reopen restores the structure.

**The workbench is now FEATURE-COMPLETE, every chunk built + verified, all behind the flag (2026-06-10):** PubChem import dialog (`889aafcf6`, search a name/CID browser-direct, preview + one-click import with `source: "pubchem"`); literature companion (`9ffadab44`, both surfaces, the editor rail "Papers & patents" tab + the hub "Find in literature" mode over Europe PMC + PubChem xrefs); import file (`8eb83ed06`, `.mol`/`.sdf`/`.smi`/`.smiles` parsed in-browser via RDKit, ChemDraw gets an honest export-as-MOL message); project Molecules section + editor project-linking (`d6ad16122`, `MoleculesInventory` on the project popup + a "Linked projects" rail control, deep-link `/chemistry?molecule=<id>` auto-opens the editor); the Today-rail hidden on `/chemistry` (`8e320caf7`, mirrors `/sequences`); SureChEMBL substructure-to-patent search (`719c0e53d`, the headline PubChem-lacks capability, verified returning 10k patent compounds for an aspirin fragment); inline molecule note refs (`5fa969c36`, a `"molecule"` ObjectRefType + the editor "Copy reference for a note" tool, the chip uses the registry `vial` icon so no icon-guard bump); and a PubChem-preview polish (`33c6035e2`). All live-verified in isolated worktrees (draw/import/library/edit/literature/patents all exercised); the only piece not browser-E2E'd is the project-section DISPLAY of a linked molecule (the preview harness chokes on this app's stacked dialogs), but it is a near-exact mirror of the proven `SequencesInventory` with a unit-tested `listByProject` filter. **Next:** to ship in prod, flip `NEXT_PUBLIC_CHEMISTRY_ENABLED` (Vercel env + redeploy), add the interest pick to feature setup, and write `/wiki/features/chemistry` (then drop `/chemistry` + `/chemistry-embed-check` from wiki-coverage `EXCLUDED_PREFIXES`). Optional follow-ups: a thumbnail-in-note custom renderer (the generic ObjectChip shows an icon, not the structure), CDXML best-effort parse, and retiring the `/chemistry-embed-check` probe. Consume the service layer, do not modify the locked shapes. **Full inheritor briefing: `/Users/gnickles/Desktop/HandoffForAgents/chemistry-workbench-handoff.md`.** Caution: the shared `main` checkout is heavily contended (Data Hub + business + calculators arcs), stage explicit paths (never `git add -A`), verify HEAD before committing.

### Mobile companion app + store launch (in flight, 2026-06-07)
- A free, thin Expo companion app lives in `mobile/` (snap a bench photo into the
  inbox, push notifications, today/calendar glance; NOT a port). Design:
  `docs/proposals/MOBILE_COMPANION.md`.
- Store launch prep is DONE and documented: `docs/proposals/MOBILE_STORE_LISTING.md`
  (copy, assets, data-safety, reviewer notes), `docs/ops/mobile-publish-runbook.md`
  (the `eas build`/`eas submit` steps tagged [YOU]/[AGENT]), and
  `docs/ops/mobile-dev-accounts-setup.md` (accounts).
- EAS is configured: project `@gnickles/researchos-companion`, `eas.json` profiles,
  bundle id `app.researchos.companion`. **Dev-account plan updated 2026-06-10, the
  LLC D-U-N-S was assigned** (9-digit, stored in `/admin/business` + the LLC vault,
  not in repo), which unblocks enrolling both stores under the LLC. iOS ships first
  on the existing **Individual** Apple account ($99/yr paid), then App-Transfers to
  the LLC org after launch (Grant's call, 2026-06-10). Android builds FRESH on a new
  **LLC organization** Play account ($25), which is exempt from the 12-tester /
  14-day closed test that gates personal accounts. Runbook plus the App-Transfer
  criteria are in `docs/ops/mobile-dev-accounts-setup.md`.
- BeakerBot app icons are in `mobile/assets/images/` (generator `brand/src/`); the
  Play feature graphic is `brand/png/researchos-play-feature.png`. Both are listed
  in `brand/BRAND_MANAGER.md`.
- Remaining before submit: app feature-complete + a reviewer demo mode, device
  screenshots, then `eas build`.

### Mobile cosmetic convergence (2026-06-11, 13 of 14 screens locked)
The mobile app was driven to the signature standard (sky #1AA0E6 primary, coral
#FF6F61, amber #F59E0B, near-white grey #f2f3f7 canvas, 34px/800 large titles,
grouped containers with hairlines) via an ideal-vs-live convergence.
- **The artifact + loop.** `docs/mockups/ideal-vs-current.html` is the bar, a
  side-by-side of an authored ideal frame (left) vs a live dev-client screenshot
  (right) for every tab, popup, and sub-screen; an entry carries `final:true` for a
  green Locked badge. It is also served from `/private/tmp/ros-cmp/docs/mockups` on
  :8100, so MIRROR any edit there. Rubric is `docs/mockups/CONVERGENCE_RUBRIC.md`;
  section 0 is the bidirectional prime directive (the live app is sometimes RIGHT,
  so the stale ideal gets regenerated, not the reverse, e.g. the chooser's semantic
  amber/green/violet icons and clearer section labels beat the old sky/coral ideal).
  The loop, audit a screen vs its ideal, author a one-screen fix branch off
  `origin/main`, the mobile master clean-merges + tsc-checks + device-verifies, then
  re-capture and set `final:true`. Grant is the lock gate (he reviews redesigns as
  an options mockup first, e.g. `docs/mockups/popup-decisions-options.html`).
- **Locked (13):** Notebook, Inventory, Timer, Calc x5 (Scientific/Molarity/
  Dilution/Serial/Buffer), Settings, Send/Inbox, Connect, Quick note, Wiki rendered
  article. Branches landed + merged: connect-convergence, calc-accent-callouts,
  settings-toggle-color, quicknote-inbox-discard, wiki-article-card,
  chooser-bigger-icons-inbox.
- **The only open item:** the chooser sheet re-shoot + a footer-button-height
  judgement (the reused gray square icon may read too tall in its new dashed
  button), both on Grant's iPhone pass. The chooser app change is already merged; it
  CANNOT be captured on the emulator because `NotebookChooser` only opens with a real
  paired session that has notebooks (`userX25519PubHex && nbList.length > 0`), and
  demo mode has neither, so captures route straight to the Inbox and the sheet never
  opens. Wiki base stays unlocked pending the post-launch Ask AI chip.
- **Emulator gotchas worth inheriting.** (1) Drive one emulator with
  `adb -s emulator-5554`; a physical phone can share the bus. (2) The dev-client runs
  main over Metro 8081, so this is land-then-verify, do not preview an unmerged
  branch. (3) The doc renders the live screenshot downscaled ~3.6x into the same
  300px frame, so a live element ALWAYS looks smaller than the native-rendered ideal,
  judge on-device sizing on the device, never off the side-by-side. (4) Emulator taps
  can queue and fire on a stale screen (this once tripped the camera flow), so prefer
  deep-links, `researchos://note` and `researchos://wiki/<leaf-slug>` work but a wiki
  section slug or two-segment path bounces to the default tab. (5) Run on a device
  with `cd mobile && npx expo start --dev-client` (expo-dev-client is a dep so the QR
  targets the dev build, not Expo Go); mirror an Android screen for a demo with
  scrcpy (`brew install scrcpy`, then `scrcpy`).

### Demo method seeds (2026-06-13, MERGED to main `88a57f2ef`)
Demo mode now ships one fully-authored, openable method per method type (pcr, lc_gradient, mass_spec, qpcr, western, staining, culture, cloning, extraction, compound kit, markdown, pdf, coding), each with a real `MethodProjection` so read mode renders identically to a real published method. Seed data + `getDemoMethod(uid)` + `DEMO_METHOD_SNAPSHOT` live in `mobile/lib/method-library.ts`. The library tab routes a demo tap to `/method-detail?demo=<uid>` (was the shared empty `?read=1`), `method-detail.tsx` has a `?demo=<uid>` reader branch (synchronous, no pairing/network), and `fetchSnapshot('method')` in demo now returns `DEMO_METHOD_SNAPSHOT` (was `null`) so the Today active-experiment band is demoable. Also mirrors the web three-ramp rainbow (pastel/vivid/luminous) into RN tokens in `mobile/lib/design.ts` (prep for dark mode, no behavior change). Commit `72f951729` on `mobile-redesign-foundation`. tsc clean, NOT pushed, NOT device-verified (land-then-verify). Handoff: `docs/handoffs/2026-06-13-mobile-demo-method-seeds.md`.

### Companion active-experiment band (2026-06-13, BOTH parts MERGED to main)
Grant approved a dedicated "active experiments" band (separate band, not inline cards). Two parts, both now on main:
- MERGED to main (`4578ef848`): `frontend/src/components/TodaySnapshotPublisher.tsx` now AUTO-publishes the `'method'` snapshot for the focused active-today experiment-with-method (earliest start, then lowest id), reusing `publishMethodToAllDevices` 1:1 (NO snapshot-shape change), slow cadence `METHOD_PASS_EVERY=5` staggered after the library pass, content-gated by a `taskId:owner` key. This is what makes the Method-tab recs band populate without the manual "View method on phone" press. NEEDS LIVE PHONE VERIFY.
- MERGED to main (cherry-picked `624a0f64f` as `3d883f287`, 2026-06-13): the Today-pull-down "ACTIVE EXPERIMENTS" band (experiments lifted out of the inline Today list into purple cards with linked-method name + "Day N of M"). Touches `frontend/src/lib/mobile-relay/today-snapshot.ts` (additive OPTIONAL `linkedMethodName`/`linkedMethodType` on experiment SnapshotTasks), `mobile/components/TodayPanel.tsx` (the band), `mobile/lib/snapshots.ts` (the two `SnapshotTask` field additions only). Cherry-picked the SINGLE commit (not the whole `worktree-agent-af8955a1ce87fdd9a` branch, which carries unrelated beakerbot-vision/phylo work); `snapshots.ts` auto-merged cleanly so the `recordSnapshotGeneratedAt` liveness wiring AND the demo `DEMO_METHOD_SNAPSHOT` serving both survive (the old "deleted liveness wiring" caution did not apply to this single commit). NEEDS LIVE PHONE VERIFY (relay + EAS dev build).

### Phone ingestion (companion app, Telegram REMOVED 2026-06-10)
- The phone path is the **mobile companion app only**. Telegram was fully removed (Grant, 2026-06-10): `lib/telegram/*`, `/api/telegram-file/route.ts`, `TelegramPairingModal`, `TelegramStatusBadge`, and the polling hook are all gone. The companion relay (`lib/mobile-relay/poll.ts`) replaced it. Only inert remnants remain (a dormant onboarding `picks.telegram` tour beat that never fires since the pick is no longer offered, internal `telegram_*` artifact function names, and code comments); the user-facing copy + wiki screenshots were de-Telegrammed. Do NOT reintroduce a Telegram surface.
- Incoming photos land in `users/{user}/inbox/Images/` by default. **Auto-routing (2026-06-10):** when an experiment popup is open and a photo arrives with no explicit phone route command, `poll.ts` reads `activeTask` + `activeTaskTab` and lands it in that experiment's active tab (Results when on Results, else Lab Notes), then fires `capture:routed` so the popup switches and the image strip refreshes. An explicit phone route command still wins; falls back to the inbox when nothing is open.
- `InboxPanel`, `InboxToast`, `InboxBadge` render the inbox; `ImageStrip` reads from the inbox + task Images folder.

### External calendar (live, ICS-only)
- `lib/calendar/external-feeds-store.ts` persists ICS subscriptions in `users/{user}/_calendar-feeds.json`.
- `lib/calendar/ics-parser.ts` parses the feed text.
- `/api/calendar-feed/route.ts` is the Vercel function proxy (15-min edge cache, SSRF-protected).
- `useExternalEvents()` hook merges external events into the calendar view.
- **OAuth (Google + Microsoft) was removed 2026-05-14.** Earlier builds had read/write OAuth integrations under `/api/auth/{google,microsoft}/*` writing tokens to `users/{user}/_calendar-oauth.json`. Grant decided the maintenance overhead (PKCE flow, refresh-token rotation, deployer-side OAuth-client registration, Google's CASA verification) wasn't worth it; ICS subscriptions cover the read use case. The whole surface is gone (routes, lib clients, UI). Legacy `_calendar-oauth.json` files in users' folders are orphaned but harmless. Legacy `kind: "google"|"outlook"` entries in `_calendar-feeds.json` are silently filtered out by the v3 normalizer in `external-feeds-store.ts` (they can't be fetched anymore — user resubscribes via ICS). Wiki page `/wiki/integrations/calendar-oauth` deleted; `/wiki/integrations/calendar-feeds` is now the only calendar-integration doc.

### Wiki + screenshot pipeline

- **Content lives at** `frontend/src/app/wiki/<path>/page.tsx` as pure TSX server components (no MDX). One default-exported component per page returning a `<WikiPage>` wrapper.
- **Shared primitives** (`frontend/src/components/wiki/`): `<WikiPage>`, `<Callout variant="info|tip|warning|danger">`, `<Screenshot src caption width height noZoom>`, `<Steps>` + `<Step>`, `<Kbd>`. Note: `<Tip>` / `<Warning>` / `<Highlight>` do **not** exist as separate components — use `<Callout variant=...>`.
- **Navigation tree** is the single source of truth at `frontend/src/lib/wiki/nav.ts` (`WIKI_NAV`). When adding a page, register a node and the sidebar/breadcrumbs/prev-next links update automatically.
- **Pre-auth bypass**: `frontend/src/lib/providers.tsx` short-circuits the FS-picker gate for `/wiki/*` so visitors can read setup guides before connecting a folder. Don't break this.
- **Screenshot capture is automated** via Playwright. Script: `scripts/capture-wiki-screenshots.mjs`. NPM: `cd frontend && npm run wiki:screenshots`. Pre-req: `npx playwright install chromium` once. PNGs land in `frontend/public/wiki/screenshots/<name>.png` at 1440×900 @ 2× DPR. Red-ring highlights are injected via `page.evaluate()` at capture time (inline CSS, no React component) — the PNG comes pre-annotated. Documentation: `scripts/WIKI_SCREENSHOTS.md`.
- **Fixture mode**: appending `?wikiCapture=1` (signed-in) or `?wikiCapture=picker` (folder-picker) to any URL installs an in-memory file-service mock seeded from `frontend/src/lib/file-system/wiki-capture-fixture.ts` (2 users, 4 projects, realistic data). Hard-blocked outside `localhost`. The script runs three route phases (PUBLIC, PICKER, FIXTURE) in separate browser contexts so IndexedDB doesn't bleed.
- **🚨 NEVER capture screenshots against the user's real data folder.** Grant's `users/` folder contains unpublished research. All wiki / demo / docs screenshots MUST use fixture mode. The fixture is gated by URL flag + hostname; bots adding new captures stay inside this system. If a story needs richer fake data, **enrich** `wiki-capture-fixture.ts` with believable-but-fake content (the test: "embarrassing if leaked to a competitor lab?" → too real).
- **Adding a new screenshot**: add a route entry `{path, file, waitFor, highlight?, action?}` to the appropriate list in `capture-wiki-screenshots.mjs`, update `scripts/WIKI_SCREENSHOTS.md`, re-run the script. Naming convention: `<page-key>.png` matches the `<Screenshot src=…>` value on the consuming page.
- **Capture-time gotchas**: production `next build && next start -p 3001` is much faster than `next dev` (Turbopack first-compile is slow). Don't use port 3000 (the user runs dev there). `?wikiCapture=picker` requires a fresh browser context if a signed-in capture ran first — the script handles this. **`npm run demo:data` wipes `frontend/public/demo-data/` and only writes JSON/MD** — it does NOT regenerate the watermarked PNGs. If you regen demo data and then capture, you'll get 35-of-36 timeouts on `networkidle` because the fixture mock tries to load 404'd PNGs and Playwright's in-flight set never empties. **Always run `npm run demo:images` after `npm run demo:data`** to restore the PNGs, then restart `next start` (Next 16's prod server doesn't pick up new `public/` files added after boot). **Same trap for `demo-lab.zip`**: `npm run demo:data` does NOT regenerate it — only `npm run prebuild` (via `build-demo-zip.mjs`) does. After regenerating demo data, also run `npm run demo:zip` (or `npm run prebuild`) so the downloadable zip stays in sync with the on-disk tree + fixture. Surfaced 2026-05-14 stamp-fixture worker.
- **Wiki voice** (from tone pass `5ebfc8d6`): no em dashes, no semicolons except in code, use `(e.g., …)` / `(i.e., …)` for asides, contractions throughout, brand names properly capitalized. ALL CAPS reserved for the Shared Lab Accounts danger callout only.
- **Wiki coverage gate** (`scripts/check-wiki-coverage.mjs`). Runs as part of `prebuild` (so Vercel deploys + local `npm run build` both invoke it). Scans `frontend/src/app/` for top-level routes with a `page.tsx`, diffs against `APP_ROUTE_TO_WIKI` in `nav.ts`, and fails the build on UNMAPPED (app route with no wiki entry) or ORPHANED (map entry pointing at a missing wiki page) drift. STALE entries (map → missing app route) warn but don't fail. **When you add a new top-level app route, add a matching entry to `APP_ROUTE_TO_WIKI` in `nav.ts` and create the wiki page**; the build will refuse to deploy until you do. Routes that intentionally have no docs (alternate entry points, server-only paths) go in `EXCLUDED_PREFIXES` inside the script. Manual report: `npm run wiki:coverage` (no `--ci` flag, just prints). **Deploy trap (2026-06-11): the build runs the COMMITTED `check-wiki-coverage.mjs` from origin, so an `EXCLUDED_PREFIXES` (or `APP_ROUTE_TO_WIKI`) addition that is only DIRTY in the working tree still fails the deploy.** This file is heavily contended (every new route touches it across parallel sessions), and it kept being left uncommitted — the build failed on `/ai`, `/chemistry`, `/chemistry-embed-check` being UNMAPPED while their exclusions sat unstaged. If you add/exclude a route, COMMIT and push the script change; don't leave it dirty. When the file is contended and you cannot cleanly commit it alongside your own change, still land the one-line exclusion explicitly (it is the difference between a green and a red deploy).
- **Wiki freshness marker system** (added 2026-06-10, the thing that makes targeted re-audits possible). After a codebase change, do NOT re-audit the whole wiki. Run the two checkers to get the exact subset that drifted, then re-do only that. Prose: `node scripts/wiki-audit-check.mjs` reads `frontend/wiki-audit-ledger.json` (each page mapped to the source files it documents plus the commit it was last screened at) and reports STALE / never-screened / excluded. Screenshots: `node scripts/wiki-screenshot-check.mjs` reads `frontend/wiki-screenshot-ledger.json` (each shot mapped to the components it depicts plus its captured commit) and reports the shots to re-capture. Workflow: change code, run both checkers, re-audit/re-capture ONLY the flagged pages and shots, then re-anchor the touched entries' `screenedAtCommit` / `capturedAtCommit` to the new HEAD. After capturing new shots, regenerate the screenshot ledger with `node scripts/build-wiki-screenshot-ledger.cjs` so they get tracked. Both ledgers carry exclusion lists (`excludedRoutes` / `excludedShotPatterns`) for the out-of-scope hot zones (mobile/companion, sharing, lab, billing) so the checkers never flag those, and the screenshot ledger scopes the capture fixture as a source only to data-driven shots (notifications/trash/deposit/version-history/attachments/annotation) so a fixture tweak does not flag every shot. This is what catches silent drift like a component migration emptying a data-driven screenshot (it caught the Loro migration emptying the version-history shots). Pass `--json` to either checker for machine output.

### Vercel
- Deployed at `https://research-os-xi.vercel.app/`.
- No environment variables required for the core app. The two proxy routes work with no setup.
- Vercel Web Analytics is integrated (commit history; branch `vercel/install-vercel-web-analytics-ruqdgf` exists upstream).
- `cd frontend && npx vercel` works for self-hosting.

---

## 6. Known traps / things I've stepped on

- **The AI proxy must NOT send `stream_options` on a non-streaming request.** OpenAI-compatible providers (Fireworks, our provider) HARD-REJECT `stream_options` with a 400 (`"stream_options is only valid when stream=true"`) whenever `stream` is not true. BeakerBot's agent loop runs `stream:false` to read `tool_calls` from one complete response, so a 2026-06-13 bug that included `stream_options` on non-streaming calls made EVERY BeakerBot turn 400 ("The model provider returned an error"). Fixed `f6fd2c311` (`frontend/src/app/api/ai/chat/route.ts`, only send it when `stream && billingOn`; non-streaming reads usage from the native completion JSON) with a regression guard in `route.test.ts`. This was INVISIBLE to gate checks (vitest mocks fetch, never hits a real provider) and only surfaced in a LIVE-MODEL Chrome test. LESSON: provider-contract bugs need a real-provider smoke test; the route deliberately does not echo the upstream body, so temporarily log `await upstream.clone().text()` in a disposable worktree to read the actual error. Two related facts from the same session: (1) the old `AI_VISION_MODEL` slug `llama-v3p2-11b-vision-instruct` is DEAD on Fireworks (404 Model not found) — on our account only `kimi-k2p6` is multimodal (glm/deepseek reject images), and the prod Vercel var must be updated to a live model too (full path `accounts/fireworks/models/kimi-k2p6`, never shortened). (2) `NEXT_PUBLIC_DEV_AI_IN_DEMO` is HARD-GATED to `NODE_ENV !== "production"` (useAccountCapabilities.ts), so it does NOTHING on ANY Vercel deploy (Preview included — Vercel builds are `NODE_ENV=production`); it only works in local `next dev`, so never set it in Vercel.

- **Parallel top-level sessions must each run in their own worktree, never share the main checkout.** The current git branch is a property of the working directory, not the session. So when several interactive sessions are all launched in `/Users/gnickles/Desktop/ResearchOS`, they share one `HEAD`, and a `git checkout` in any one of them silently drags every other session onto that branch. On 2026-06-08 one session ran `git checkout security/p1a-invite-key-out-of-email` to park inventory/sharing/mobile WIP, every other session in the main checkout was instantly on that branch too, and FleetView showed them all on `p1a-invite` instead of `main`. The parked work then had to be reconciled back. The rule going forward is to spawn each parallel top-level session in its own `git worktree` (use `scripts/new-session-worktree.sh <name>`), and keep the main checkout pinned to `main` as the integration tree. A running session's cwd is fixed at launch, so you cannot relocate one mid-flight; cycle existing sessions into worktrees as they wrap. **Reconciling parked work** (when it has already happened). Do it from a throwaway worktree off current `main`, cherry-pick only the genuinely-unmerged commits (find them with `git log --oneline --cherry-pick --right-only main...<branch>`, since `--cherry-pick` ignores commits already re-applied under a different SHA), verify, then land with `git merge --ff-only` at a moment when none of the touched files are dirty in the shared checkout. A whole-branch merge re-drags already-reconciled commits and conflicts on them; the cherry-pick-the-delta path stays clean.

- **The "drag image to trash deletes a sibling too" bug.** Fixed in commits `6d77e739` and `6f068e1d`. Root cause was `gcUnreferencedAttachments` not counting subfolder-style refs (`Images/{folder}/foo.png`) as protecting `foo.png` at the top of `Images/`. Fixed by extracting basenames at any depth. If you see similar "this code is dropping refs to legacy paths," look for `referencedRelativeNames` and `migrateNoteImages`.

- **`h-full` chain only works under a definite-height ancestor.** Non-fullscreen popups used `max-h-[90vh]` (no explicit height) and the editor's internal scroll broke. Fix: ensure `h-[Nvh]` or equivalent on the popup. (Commit `c0876e3d`.)

- **OneDrive / iCloud picker can block JS for 30-60s.** `connect()` flips into a staged loading screen BEFORE calling `showDirectoryPicker` and yields two `requestAnimationFrame` ticks so React commits. CSS animations on the loading screen continue running during the JS block; `setInterval`-driven counters don't. See `StagedLoadingScreen.tsx`.

- **FSA handles persist via IndexedDB but permission grants don't.** On reload, call `handle.queryPermission({ mode: "readwrite" })` first; if `"granted"`, reconnect silently. Otherwise show a "Continue" button that fires `requestPermission` (needs user gesture). See `reconnectWithStoredHandle` in `file-system-context.tsx`.

- **Per-user ID collisions.** Grant's task id 1 and Kritika's task id 1 are different tasks. Always use `taskKey(task)` for in-memory uniqueness.

- **Cross-user dependency cascade is namespace-bounded — but receivers now get a shift notification.** `shiftTask(taskId, …, owner)` still only walks deps in that one user's directory (cross-user writes would break the ownership invariant). What landed at `87b738d6` is the Option-C "notify-on-receive" half: every `tasksApi.move` that affects a task with `shared_with.length > 0` appends an entry to `users/<owner>/_shifted-alerts.json` (writer side), and every receiver scans her owners' sidecars at `NotificationBadge` mount via `sharingApi.scanShiftAlerts`, dedups against her local `_seen-shift-alerts.json`, and synthesizes a `ShiftAlertNotification` into the existing `_notifications.json` inbox. The notification offers "View task" / "Ignore" — it does NOT auto-realign a dependent task because dep edges are intra-namespace today (`dep.parent_id` / `child_id` carry no owner; there is no cross-user dep to walk). If you're adding a new code path that calls `shiftTask` or writes to a task, make sure it goes through `tasksApi.move` so the sidecar gets appended — direct `tasksStore.save` bypasses the alert. **Where the alert lives** is the namespace `shiftTask` ran in (= the `owner` parameter, fallback current user); `shifted_by_user` is the actor (= current user), which is how a receiver filters "morgan-shifted-alex's-task vs alex-shifted-her-own" without double-noise. See `frontend/src/lib/shift-alerts.test.ts` for the dedup invariants the receiver scan relies on.

- **Field renames sometimes outpace callers.** When you see a typecheck error in `local-api.ts` for a Notes/Project/etc field that doesn't exist on the inferred type, it's usually because an interface in `lib/types.ts` was renamed by another agent and a caller wasn't updated. The typical fix touches **three layers in lockstep**: (1) the TypeScript interface in `frontend/src/lib/types.ts`, (2) the corresponding `*Api` adapter in `frontend/src/lib/local-api.ts` (read/write/normalize paths, including any lazy migration), and (3) the React component callers that pass the field. Grep the old name across all three before declaring the rename complete — partial renames often typecheck because callers go through `any`-shaped intermediaries. **Note:** Zod runtime validation was consolidated out (commit `f1e3d7be`, 2026-05-13); types are now hand-written TS interfaces in `types.ts`. If runtime validation is ever wanted again, reintroduce it surgically at the call site rather than as a parallel schema file.

- **Drops over `<img>` need native capture-phase listeners — React's `onDrop` doesn't win.** When a user drops a native OS file over a rendered `<img>` inside the markdown body, Chrome's per-element drop default ("replace image with file URL") fires BEFORE React's synthetic event delegation reaches inner element handlers. React's `onDrop` on the `<img>`, the markdown block, and the editor wrapper all silently miss the event; the file falls through to the window-level `GlobalDropGuard`. The fix that actually works: register a native `dragover`/`drop` listener via `useEffect` + `addEventListener(..., true)` (capture phase) on the editor's outer wrapper. Capture fires top-down BEFORE inner elements get the event. Pattern is in `frontend/src/components/LiveMarkdownEditor.tsx` around line 575 (added by `4ae53082`, 2026-05-13). Don't waste time trying React-side fixes for drops on `<img>` — skip straight to native capture-phase.

- **Worktree edits silently land on the main checkout if you Read/Edit by main-checkout absolute path.** When operating in a worktree (`.claude/worktrees/<name>/`), `Read` and `Edit` use whatever absolute path you give them — the harness does NOT redirect paths under `/Users/<u>/Desktop/ResearchOS/frontend/...` to the equivalent worktree path. So if you `Read` a file at the main-checkout path while exploring, every subsequent `Edit` to that file also goes to main. You'll find this out when `git status` inside the worktree returns clean but `git status` in main shows your changes. Recovery is easy (the diff is real, just on the wrong branch), but the cleaner habit is: in a worktree, either `cd` to the worktree first and use relative paths, or pass the explicit worktree-anchored absolute path. Hit this on 2026-05-13 mid-chip-sweep; nothing broke but a few seconds of confusion.

- **Next.js 16 client-component `params` are async.** A change from Next 15: in client components, route params are now a `Promise<{...}>` rather than a plain object. Code that does `const { slug } = params;` typechecks but throws at runtime. Discovered by the Demo v2 catch-all route bot on 2026-05-14; the implementer worked around it cleanly using `usePathname()` + string slice (no `params` access at all). For dynamic-segment client components, either `await` the Promise via `use(params)` from React, or sidestep `params` entirely with `usePathname()`. Server components are unaffected — they can `await params` normally.

- **Verification bot's dev server running from a stale worktree.** When a verification bot probes `localhost:3001` (or wherever your dev server is), Next.js serves whatever the dev process's `cwd` was when started — which can be a sub-bot's worktree that was checked out earlier in the day, not the current `main` checkout. Symptom on 2026-05-14: verification bot reported ALL 7 Demo v2 recipes INCONCLUSIVE because the bundle was missing `FloatingLeaveDemoButton.tsx`, `OpenDocsButton.tsx`, `TryInDemo.tsx`, and the catch-all `[[...slug]]/page.tsx` route — those existed in `main` (`74ecd115`, `80471925`) but not in the worktree the dev server happened to be in (`competent-tesla-1c0608` at `412a9fe0`). The failure mode looks like "the feature didn't ship" (broken-looking pages, hung loading screens) rather than "the server is wrong" — easy to misdiagnose. Diagnose by `ps -o pid,command,cwd <pid>` on the dev process or by grepping for one of the merge SHAs in the served bundle. Fix is just `kill <pid>` + restart from the right cwd. Bot brief should ideally tell the bot to verify the bundle freshness first (e.g. check that a known-new component name appears in the page source) and INCONCLUSIVE-out the whole run cleanly if not.

- **`/wiki/*` belongs to a dedicated wiki sub-bot, not the feature sub-bot.** The pattern is: feature ships in main, master summarizes what changed, master spawns a separate wiki-focused sub-bot to draft the page with the right voice plus screenshots. The feature sub-bot you spawn should LIST the wiki implications in its report so master can synthesize the next brief; it must not touch `frontend/src/app/wiki/**` or `frontend/src/lib/wiki/nav.ts`. The LabArchives revamp arc on 2026-05-14 violated this by drafting `/wiki/integrations/labarchives/page.tsx` inline (commit `b84a1e35`, 328 LOC), leaving first-draft material the wiki sub-bot then had to either adopt + revoice or replace from scratch. When briefing a feature sub-bot: explicitly carve `frontend/src/app/wiki/**` out of scope and add the phrase "Surface wiki implications in your final report; do not write wiki pages." Equivalent rule for `scripts/capture-wiki-screenshots.mjs` and `frontend/src/lib/file-system/wiki-capture-fixture.ts`.

- **Server-side proxy rate limits are per-instance by default.** ResearchOS exposes 5 server-side proxy routes (`/api/calendar-feed`, `/api/telegram-file`, `/api/labarchives/fetch-image`, `/api/auth/labarchives/login`, `/api/auth/labarchives/refresh`). Each makes upstream HTTP calls and burns serverless function time. The default rate limiter (`frontend/src/lib/api/rate-limit.ts`) is in-memory per-instance — fine for casual abuse, but a determined attacker rotating across Vercel cold-start instances can still drain serverless budget. For sustained DDoS protection on public deploys, set `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` env vars and `npm i @upstash/ratelimit @upstash/redis` (free tier: 10k requests/day). Or deploy behind Cloudflare (free tier) for edge-level per-IP rate limiting. Both packages are dynamically imported only when the env vars are set, so the default bundle stays clean. See `frontend/DEPLOYMENT.md` for the deployer-facing recipe. The wrapper is type-preserving and sits OUTSIDE `signedLabArchivesFetch` / `safeFetch` — those run unchanged once the rate-limit check passes.

- **Cross-owner task→project sharing is bidirectional and self-heals on drift.** When alex's task is shared INTO morgan's project (Option C, landed 2026-05-14), the task carries `external_project: { owner, id, sharedAt } | null` and the destination project has a sidecar manifest `users/<projectOwner>/projects/<projectId>-hosted.json` listing every foreign-hosted task. **Both sides must agree** — a task without a matching manifest entry, or a manifest entry without a matching `external_project` on the task, is drift. The read-time normalizer (`normalizeProjectHostedManifest` in `frontend/src/lib/sharing/project-hosting.ts`) drops mismatched manifest entries and persists the repaired manifest back asynchronously. The mirror direction (task points at a project the manifest doesn't list) is fixed by either the next unshare call or the Phase-5 background sweep (`reconcileHostedDrift`). **Don't write either side without writing the other** — the `tasksApi.shareIntoProject` / `tasksApi.unshareFromProject` API wraps both writes; raw `tasksStore.save` calls that touch `external_project` bypass the manifest update and produce drift the user only sees as "the hosted task disappeared from the destination Gantt" on the next read. Per-user ID collision still applies: the manifest's `(owner, taskId)` and `external_project.id` always operate in the namespace they came from. Cycle case (task hosted into a foreign project whose owner has the task's native project shared back) is NOT detected for v1 — possible but rare.

- **react-pdf needs only `connect-src data:`, and the scary "hang" is dev-server-only.** `@react-pdf/renderer` (4.5.1) lays out via yoga-layout (3.2.1), whose browser WASM ships as a `data:application/octet-stream;base64,...` module loaded with `fetch(dataUrl)`. Under the production CSP that fetch is blocked (`connect-src` lacked `data:`), logging "Fetch API cannot load data:... violates the document's Content Security Policy". It is NON-FATAL though: yoga catches the rejected fetch and decodes the embedded base64 synchronously (its `wa()` fallback), so in a real `next build` the PDF still builds and downloads even with the original CSP. Verified 2026-05-29 with a local prod build + headless Playwright driving `buildCombinedPdf` (produced a 22KB `application/pdf`, download fired) under BOTH the original CSP and CSP+`data:`. The hard "Building..." HANG that looks like a CSP break is a DEV-SERVER-ONLY artifact: Turbopack (and webpack) dev load lazy chunks via `eval`, which `script-src` blocks (no `'unsafe-eval'`), so `await import("@react-pdf/renderer")` never resolves and the build spins forever. `connect-src data:` does NOT fix the dev hang (only a dev-only `'unsafe-eval'` would); do not broaden the prod CSP chasing a dev symptom. Minimal prod change (committed 2026-05-29, Grant-signed-off): add `data:` to `connect-src` ONLY, which removes the console violation and lets yoga use its intended load path (`data:` URIs are inert self-contained bytes, no remote endpoint, so not a data-exfiltration channel). NO `worker-src`/`blob:` (react-pdf spawns no Worker, grepped the entire dep tree) and NO `'unsafe-eval'` (the `'wasm-unsafe-eval'` already granted covers yoga's `WebAssembly.instantiate`; a transitive `function-bind` + generator-feature-detection `eval` does violate in prod but is caught / has a native fallback, non-fatal). An earlier attempt added `worker-src 'self' blob:` + `connect-src data: blob:` and got flagged too broad: the verified-minimal set is the single `data:` token. Also note: the project-deposit combined PDF has a SEPARATE, non-CSP bug surfaced during this work, real demo content with `*italic*` markdown throws "Could not resolve font for Inter, fontWeight 400, fontStyle italic" because only Inter Regular + Bold are registered (see `registerExportFonts` in `pdf.ts` and the `em` token renderer in `combined-pdf.ts`); that is a font-registration gap, not a CSP issue.

- **Parallel-chip integration: three traps that compound at merge time.** When N parallel chips run against shared surfaces (the Methods Expansion v1 arc ran 4 type-additions + 1 retrofit + 1 schema-add + 1 markdown-overlay all touching `TaskMethodAttachment` + `local-api.ts` + `methods/page.tsx`), three failure modes show up in sequence at integration and stack:
  - **(1) Shared method-id picks collide.** Phase 2C and Phase 2D both branched off the same pre-chip main (where alex's methods went up to id 6 = LC) and both picked id 7 for their new fixture method. Detected only at merge time; cell culture renumbered to id 8 at integration. Fix at chip-brief time: assign explicit id ranges per chip up front (e.g., "your fixture method gets id 8, the parallel chip gets id 9"). Cheap up front; expensive at integration.
  - **(2) `awk`-union doesn't survive structurally interleaved conflicts.** When the orchestrator tries `awk` to mechanically strip conflict markers and union both halves across many files (the "keep both append-only additions" pattern), it works for stereotyped sites (imports, type unions, registry entries, sibling API blocks) and breaks where conflict markers fall mid-statement / mid-object-literal / mid-JSX. The first Phase 2D merge attempt broke `methods/page.tsx` (9 conflicts in JSX), `import/apply.ts` (if-chain branches), `parse.ts` / `html.ts` / `pdf.ts` / `raw.ts` (function-body interleaving). Master aborted, spawned a focused merge sub-bot in its own worktree to resolve carefully — that's the right pattern. For future N≥3 parallel-chip arcs: build the merge-sub-bot pattern in from the start; save `awk` for chips with genuinely append-only conflicts only (1-2 conflict regions per file at most).
  - **(3) `git add -A` in mid-merge sweeps `.claude/worktrees/*` as embedded git repos.** Already covered in §4 ("git add specific paths, not -A") but the symptom in mid-merge is loud: the merge commit picks up ~60 worktree directories as submodule references plus any other untracked files in the tree. The routing-fix merge hit this; commit was amended with `git rm --cached -r .claude PUSH_REPORT_*.md` + `git commit --amend --no-edit`. Use explicit-path staging when finalizing a merge: `git add <conflicted-files> && git commit --no-edit`.
  All three surfaced during the Methods Expansion v1 arc (`98e45aaa`, 2026-05-15). Re-read before any future N≥3 parallel-chip arc.

- **LabArchives deployer credentials can live in TWO places — env vars or a sidecar file in the data folder.** The integration was originally env-var-only (`LABARCHIVES_ACCESS_KEY_ID` + `LABARCHIVES_ACCESS_PASSWORD`), then 2026-05-14 added a UI-driven sidecar (`_labarchives-deployer.json` at the data folder root) so single-user self-host deployers don't have to edit `.env.local`. Env vars still win when set; sidecar is the fallback. **Trust-model trade-off the sidecar consciously accepts**: the institutional `access_password` lives plaintext on disk in the user's data folder. Equivalent to plaintext `.env.local`. Fine for local-first single-user installs; for shared deployments, env vars are still the correct path (which is why they take precedence — a leftover sidecar a user created on their laptop won't override the institution-locked env config). The sidecar is gitignored automatically on write. Demo / wiki-capture mode NEVER reads or writes it (gated via `isDemoOrWikiCapture()`). Sources of truth: `frontend/src/lib/labarchives/deployer-store.ts` (FSA helpers), `frontend/src/lib/labarchives/config.ts` (`readLabArchivesCredsFromRequest` does env-first → body fallback), and the three signed-call API routes that accept `body.deployerCreds` as the fallback channel. Browser-side, `connect.ts` postMessages the sidecar creds into the sign-in popup; `api-client.ts` reads the sidecar once per `fetchInlineImages` batch and includes it in each `/fetch-image` POST body. **If you're adding a new signed-call LabArchives route, call `readLabArchivesCredsFromRequest(req, body)` not `readLabArchivesCreds()` — the latter is server-env-only and will 500 for sidecar-mode users.**

- **Stale-branch-root trap: cherry-pick source-only when chip branches root behind main's progress.** When a chip's branch is rooted at SHA X but main has merged additional commits since X, the chip's full diff against current main shows TWO categories: (a) the chip's intended source edits, and (b) "negative deltas" representing main commits the chip's branch doesn't know about. A naive `git merge --no-ff <chip-branch>` brings BOTH categories in — meaning main reverts the work that landed between X and now. Hit at least 10 times in the 2026-05-19 / 2026-05-20 session: walkthrough chip, WASM CSP chip, methods consolidated proposal, README chip, JOSS CI chip, migration-probe-sweep chip, tombstone Case C chip, onboarding v2 doc-chain, warnings cleanup chip, wizard-gate fix chip. Each chip individually was clean; the pattern is structural to long-running orchestration where the session-start branch anchor goes stale as main accumulates commits, and any sub-bot spawned later inherits that stale view as its branch root. **Detection at master-merge step:** before `git merge`, run `git diff --stat main..<chip-branch>` and check whether any files show NEGATIVE deltas that don't match the chip's report. If yes, branch is stale. **Recovery (locked-in pattern from this session):** `git checkout <chip-branch> -- <intended-source-files>` to cherry-pick only the named files, skip stale AI Helper prebuild artifacts + stale `wiki/*` files + any files NOT in the chip's report. Verify with `git diff --cached --stat` before commit. The post-stash-diff-verify discipline in chip-brief boilerplate doesn't catch this because the stale state isn't from a stash — it's from the branch root predating main's progress. Two ways the bot can pre-empt: (a) bot can rebase its branch on main before final commit (carries risk of conflict resolution drift); (b) bot can list files it actually touched in its report so master cherry-picks by name. The cherry-pick-source-only pattern has worked reliably at master-merge step across all 10 recurrences this session. Worth keeping in chip-brief boilerplate as a master-side merge discipline reminder: "if the chip's branch is rooted >5 commits behind current main AND touches autogen-adjacent surfaces, expect to cherry-pick source-only at merge time and skip the autogen artifacts + any files the chip didn't claim to touch."

- **Sibling-chip dangling-reference trap.** When chip A merges with a forward-reference to chip B (e.g. a wiki link, an APP_ROUTE_TO_WIKI entry, an import path, or a UI button pointing at a not-yet-created target), and A's commit message says something like *"B is being created in a sibling chip and will 404 briefly until that lands"*, do not trust that sentence. The sibling chip can silently never land — its branch closes, master moves on, no one notices A's reference is now permanently broken. Hit on 2026-05-15 when picking up the ELN-import wiki handoff: rename-bot's chip `3f46a27d` (`claude/labarchives-rename-b1b0ef`) renamed entry points and added a link to `/wiki/getting-started/labarchives-export` with the commit message promising the page was in a sibling chip. That sibling chip never landed. The 404 lived in main for ~24h until master spawned a wiki sub-bot that traced the chain back. **Detection pattern when you inherit a handoff:** for every chip in the arc you're closing, search its commit message + branch log for `sibling chip`, `will land`, `coming soon`, `being created`, or `in another chip` — then verify each forward-referenced target actually exists on main. **Fix discipline going forward:** if you ship chip A that references chip B, either (1) wait for B to merge first and reference it concretely in A's commit body with a SHA, or (2) drop the forward-reference entirely and let A function standalone. Don't ship A with a dangling-reference and hope someone tracks the dependency. Common surface for this is wiki cross-links (chip A adds a button pointing at `/wiki/foo/bar`, chip B was supposed to create `bar/page.tsx`), but the same trap applies to any cross-chip import or named-target reference.
  - **Sub-trap: cross-arc state assertions in sub-bot reports.** When a doc-chain or relay sub-bot writes a report that asserts cross-arc state (*"X is on main"*, *"Y was updated"*, *"Z's SHA is blah"*) for master or a downstream wiki sub-bot to act on, that assertion MUST be verifiable in the sub-bot's own report via either (a) the output of `git log --oneline main | grep <expected-text>`, (b) the output of `git branch --contains <expected-sha>` against main, or (c) an explicit *"I have not verified this; assume false until master confirms"* caveat. Surfaced 2026-05-19 on the encrypted-backup doc-chain chip (`9a601e55` in the arc): the doc-chain bot relayed assertions that the impl chips were on main + SECURITY_AUDIT.md had been updated. Neither was true at the time. The subsequent wiki sub-bot trusted the assertion and shipped `/wiki/security v8` based on the false premise. Master closed the gap retroactively by merging impl + doc-chain in the same session, but the SHAPE matches the parent trap above: chip A claims chip B's state is durable when it isn't. **Discipline:** any sub-bot brief that involves relaying state for a downstream chip OR asserting cross-arc state must require verification-or-disclaim in the sub-bot's own report. Sub-bots running these chips paste the git output verbatim into their relay message OR explicitly disclaim. No silent assertions.
  - **Sub-trap: cross-arc dispatch collision (two chips, same surface, no shared view).** When two parallel chips touch the same surface without sharing the in-flight view, both chips do the same work independently. Sub-bots produce parallel commits that overlap on the same file (typically a helper extraction or shared component) because neither knew the other was already in flight. Surfaced 2026-05-20 on the auto-strip-refs arc: one chip extracted a helper at `lib/attachments/strip-references.ts`; a separately-dispatched PDF-panel-delete-wiring chip re-extracted the same helper independently before adding the panel wiring. Both helpers were equivalent but committed with different SHAs (`4caefa14` and `55e3038a`). Resolution worked (merge the first branch + cherry-pick the unique wiring commit from the second, drop the duplicate helper extract) because the per-surface wiring commits were file-disjoint. Would have been worse if both chips had truly overlapping diffs on the helper itself. **Discipline going forward:** (1) master maintains the in-flight carve-out enumeration and uses it as the gate before firing any new chip on a shared surface. (2) High-touch surfaces include markdown editor (`HybridMarkdownEditor` / `LiveMarkdownEditor`), telegram routing (`image-router.ts` / `batch-routing.ts`), methods registry (`lib/methods/`, `methodsApi`), tasks API (`tasksApi`), attachments helpers (`lib/attachments/`), and file-system context (`FileSystemProvider`, `indexeddb-store`). Never dispatch two chips touching the same high-touch surface in parallel; serialize them or merge them into one chip. **Detection at merge time:** two branches both modify the same shared module (typically a helper extract) with similar-but-not-identical commits implies cross-arc collision. Resolution: merge one branch in full, cherry-pick the unique commits from the other, drop the duplicate extract.
  - **Sub-trap: invisible native `title=` masquerading as a dead-click bug.** When a QA persona reports *"click does nothing"* on an icon-only or otherwise unlabeled affordance, the instinct is to investigate the click handler. Twice this session the actual cause was that the affordance had `title="..."` as its only tooltip surface, and native HTML `title=` is functionally invisible in this app's render layer (per [[feedback_tooltip_component]] and the standing `<Tooltip>` migration policy). The persona hovered, saw no tooltip, assumed the button was dead, and never confirmed by clicking hard enough to be sure the handler ran. Receipts: Calendar a11y chip `61683c7b` (2026-05-20) where Persona 11's *"Manage button no visible state change"* was first investigated by reading source, `onClick` was correctly wired, and the missed signal was the invisible `title=` (resolved by swapping to `<Tooltip>`); also the broader icon-only sweep at `c57ff2b8` (*"Migrate icon-only buttons from native title= to <Tooltip>"*), second instance this session. **Diagnostic order to encode in bug-fix chip briefs:** when a QA report says a click does nothing, BEFORE assuming the handler is broken, grep for `title=` on or near that element. If present, swap to `<Tooltip>` and re-verify; the original click handler is probably fine. The giveaway in the persona's report is the phrase *"no visible state change on hover"* or *"no tooltip appeared"*: the click likely fired, only the affordance never showed any indication it could be clicked.
  - **Sub-trap: sub-bot worktree dev-server port discipline.** When a sub-bot spawned in a worktree wants to run a dev server for browser verification, it cannot assume :3000 (the user owns this) or :3001 (often held by the master orchestrator). One of two things happens: (a) port is in use, sub-bot can't bind; (b) port is bound by a DIFFERENT worktree's dev server, and the sub-bot's changes aren't visible because the running process is on another branch entirely. Symlinking `node_modules` across worktrees as a quick fix is also rejected by Turbopack (out-of-root symlink, same constraint as the "Worktree spawn lacks `node_modules`" trap above). Receipt: sub-task cascade chip on `claude/eager-hamilton-f95deb` (`4d0b5748`, 2026-05-20), where browser verification was blocked because :3000 was held by parallel worktree `claude/youthful-fermat-ea81c5`; sub-bot tried :3010, Turbopack rejected the `node_modules` symlink, and the sub-bot shipped without browser verification (acceptable that chip because the diff was trivial and source review was definitive). **Discipline to encode in sub-bot briefs:** (1) free a port in the 3050-3099 range, never assume :3000 or :3001 are available. (2) If a port can't be bound cleanly, do NOT attempt to reuse another worktree's running dev server: the process is from a different branch and won't reflect this chip's changes. (3) If no free port is available AND the chip's verification depends on running code, ship the source-review verdict plus an explicit disclaimer (*"browser verification skipped: port collision"*). Source review can still be definitive for trivial diffs; the gap should be named, not papered over.
  - **Sub-trap: brief contains main-repo absolute paths, sub-bot edits the main checkout.** When a chip brief drafted by master pastes absolute paths like `/Users/gnickles/Desktop/ResearchOS/frontend/src/...` into the instructions, a sub-bot spawned in a worktree will follow the path verbatim and write to the main checkout instead of its assigned worktree. This is the brief-authored variant of the existing main-vs-worktree trap chain (compare line 245 for the Read/Edit-tool variant inside a single session, and line 281 for the shell-CWD-bounce variant across parallel bots). Root cause is the same as both ancestors: absolute paths into the main checkout silently work from anywhere, so the bot's mental model ("I am in worktree X") never gets contradicted by an error. Receipts (both this session, both recovered cleanly): the §6 sub-traps chip itself (commit `f4e1a50d`, merge `cef67748`, 2026-05-20) where the sub-bot edited `/Users/gnickles/Desktop/ResearchOS/AGENTS.md` directly before recovering and re-editing in its worktree; and the wiki Workbench Notes callout chip (commit `55aeff85`, merge `acf59949`, 2026-05-20) where the sub-bot ran its initial verification against an identical edit accidentally placed in main's working tree before recovering. **Discipline (sub-bot side):** the first action in any sub-bot session is `pwd` followed by a cross-check that the result is inside `/Users/<u>/Desktop/ResearchOS/.claude/worktrees/<branch-name>/`, not the main checkout root. Before the first Edit/Write, translate every `/Users/gnickles/Desktop/ResearchOS/...` path in the brief to `$(git rev-parse --show-toplevel)/...` (resolved once at session start, then concatenated by hand for each path). Don't paste main-repo absolute paths into tool calls even if they appear in the brief. **Recovery if caught mid-edit:** `git checkout HEAD -- <file>` in the main checkout BEFORE the next commit lands there, then re-apply the same edit using the worktree-rooted path. Cross-check both trees with `git -C <main-path> status` and `git -C <worktree-path> status` before reporting. **Discipline (master side, the brief author):** templates and chip briefs should embed the worktree-root pattern up front (one-line snippet: *"Sub-bot worktree root: $(git rev-parse --show-toplevel). Translate any /Users/gnickles/Desktop/ResearchOS/... paths in this brief to that root before editing."*) so the sub-bot has the translation rule before reading the first path reference.

- **`@vercel/analytics` Next adapter has no useEffect cleanup — stale `<script>` tag survives a runtime offlineMode toggle.** Surfaced by the Vercel Analytics chip at `62a34df0` (2026-05-15). The `<Analytics />` component (Next adapter) injects its tracking script into `document.head` on mount but does NOT remove it on unmount. Consequence: mid-session toggle of `offlineMode` (which conditionally unmounts `<Analytics />`) leaves a dormant `<script>` tag in the DOM forever — no further pageviews fire (the adapter stops calling its hooks), but the script element itself isn't cleaned up. Not a behavior bug: the offline-mode semantic holds on the durable-state path (reload with `offlineMode` persisted = script never loads at all), and the dormant script doesn't phone home. But it IS a "leaves stale DOM artifact" worth knowing if you're DOM-diffing for verification, doing CSP audits, or wondering why DevTools' Elements pane shows an analytics script on a session that supposedly toggled it off. **No workaround at the app level** — the cleanup gap is in the upstream `@vercel/analytics/next` package. Either accept it, or wrap `<Analytics />` in a custom component that manually removes the matching `<script>` on unmount via an explicit effect cleanup.

- **FSA `createWritable()` truncates the underlying file immediately, so a torn write leaves zero bytes, not "the old contents."** The default FSA write pattern is `createWritable()` then `write()` then `close()`. The truncate happens at `createWritable()` time, BEFORE any new bytes are committed. A tab close, polling-tab crash, or unhandled rejection between createWritable and close leaves the on-disk file as zero bytes. `readJson` returns null; the UI shows the file as missing; the user assumes a deletion bug. The fix lives in `fileService.writeJson` and `fileService.writeFileFromBlob` (both at [frontend/src/lib/file-system/file-service.ts](frontend/src/lib/file-system/file-service.ts)): write to `${path}.tmp` first, then `move()` to the final name (Chromium FSA atomic rename) so the failure mode becomes "old contents survive" instead of "file truncated to empty." If you add a new write path, route it through these methods or replicate the pattern. **Do not call `createWritable()` directly on the final file unless you have an explicit reason (streaming export, large append)**: the public `fileService.createWritable` helper is preserved for that case but bypasses the atomic guarantee. Non-Chromium browsers (Firefox, Safari) lack FSA `move()`; the helper falls back to `removeEntry(final)` then rewrite (non-atomic but the `.tmp` still acts as a checkpoint). Surfaced by the `_telegram.json` deletion investigation (2026-05-15); fixture and test coverage at [frontend/src/lib/file-system/__tests__/file-service-atomic.test.ts](frontend/src/lib/file-system/__tests__/file-service-atomic.test.ts).

- **CDP-controlled Chrome silently no-ops `scrollTo({behavior: "smooth"})`.** Surfaced by the Settings page hash-scroll verification (2026-05-15). When a verification bot drives Chrome via the DevTools Protocol (Claude-in-Chrome, Puppeteer, etc.), the `behavior: "smooth"` branch of `Element.scrollTo` / `window.scrollTo` resolves to no scroll at all — `scrollTop` stays at the prior value. `behavior: "auto"` works as expected. CSS `scroll-behavior: smooth` plus a direct `scrollTop` assignment is also no-op'd. The browser still reports `prefers-reduced-motion: reduce` as `false`, so any code that branches on the media query (smooth for default-motion, auto for reduced-motion) lands on the unobservable smooth path. **Implication for verification chips:** any UI feature that smooth-scrolls (auto-scroll-to-hash-target, scroll-into-view animations, smooth-jump-on-tab-switch) cannot be observably verified in CDP-controlled sessions. The chip's offset-math + ref-attachment + auto-branch are all verifiable; the smooth animation itself is not. Workarounds for the verification bot: (1) force `prefers-reduced-motion: reduce` via `Emulation.setEmulatedMedia` (CDP exposes this but `Claude_in_Chrome` doesn't surface it as a tool today), (2) call `behavior: "auto"` once for an immediate position-set and accept the perceived-motion delta on real users, (3) accept the gap and have Grant or the maintainer verify smooth-scroll in their real (non-CDP) Chrome. Default going forward: document the gap, leave the chip as-is — smooth-scroll is correct UX for real users and the verification gap is a CDP tooling limit, not a code bug. Surfaced by the `/settings#ai-helper` cold-load behavior verification chip.

- **Adding a library that compiles WASM at runtime silently breaks production CSP.** Currently `@react-pdf/renderer`'s PDF export trips this — the dependency chain `@react-pdf/renderer` → `@react-pdf/layout` → `yoga-layout` ends in a base64-inlined WASM module (Facebook's flexbox engine compiled to WASM) that `WebAssembly.instantiate`s at runtime during the first PDF render. (The pre-fix hypothesis was that fontkit-wasm font subsetting was the trigger; the actual source is yoga-layout for flex layout — fontkit in this tree is pure-JS. Verified via `grep -rl "WebAssembly\.compile\|WebAssembly\.instantiate" frontend/node_modules/ --include="*.js"`.) The fix is `'wasm-unsafe-eval'` in `script-src`, NOT `'unsafe-eval'`. The narrow opt-in (`'wasm-unsafe-eval'`, Chrome 102+ / Firefox 102+ / Safari 15.4+) allows WASM compile + instantiate without enabling regular eval. The broad opt-in (`'unsafe-eval'`) re-opens `eval()`, `new Function()`, `setTimeout(string, ...)` and is much worse — a one-character typo when copy-pasting the directive is a real risk. Watch for this when adding deps that do font subsetting, image decoding, crypto, parquet, ML inference, layout, or other WASM-accelerated work; the WASM source is sometimes a transitive dep two levels deep, not the package you added. Surfaced 2026-05-19 on the CSP-wasm chip after a routine PDF export hit `CompileError: WebAssembly.instantiate(): Compiling or instantiating WebAssembly module violates the following Content Security policy directive`. SECURITY_AUDIT.md §3.2 is the canonical reference and lists every other WASM source the bundle audit turned up (all build-time or server-only — yoga-layout is the only one reaching the user-facing browser bundle today).

- **Sub-bot working in main checkout instead of its worktree (shell-cd-bounces / CWD trap).** Surfaced THREE TIMES in a single arc (Onboarding v3, 2026-05-20): Follow-up A, Follow-up B, and P2b sub-bots all started edits in their assigned worktrees but ended up writing to the main checkout's absolute paths instead. **Failure mode:** the Bash tool's shell resets CWD between invocations (you can see it in the "Shell cwd was reset to ..." message after each command). A sub-bot that uses `cd /Users/<u>/Desktop/ResearchOS && <git/edit/test commands>` runs the commands inside the main checkout for that one invocation, then CWD bounces back to the worktree root on the next invocation. If the bot also runs Read/Edit/Write with main-checkout absolute paths (a separate variant of the existing §6 worktree-edit-by-main-path trap, but at the shell level rather than the tool-call level), file writes land in main checkout. The bot believes it is editing its worktree because its mental model says "I was spawned in worktree X"; in reality only the harness CWD is in the worktree, and the bot's git operations + file edits have crossed over. **Detection:** files appearing in main checkout's `git status` (modified or untracked) that the sub-bot believes it created in its worktree. Cross-check by running `git -C <worktree-abs-path> status` AND `git -C /Users/<u>/Desktop/ResearchOS status` from the bot's session; if the worktree shows clean but main shows the bot's changes, the bot has been writing to the wrong tree the whole time. **Resolution if detected mid-run:** abort the chip and report immediately. Do NOT attempt to copy-recover files between paths, that compounds the confusion and produces stale state in two places. Master will either (a) cherry-pick from wherever the sub-bot ends up landing committed work, or (b) treat the in-main-checkout state as never-having-happened and re-dispatch. **Mitigation in every future chip brief** (codified after this arc): include the verification clause `"VERIFY YOUR WORKTREE PATH BEFORE ANY FILE EDIT. Run pwd first; ensure it matches your assigned worktree (not /Users/gnickles/Desktop/ResearchOS). Use git -C <worktree-abs-path> exclusively for git commands, never cd + bare git. If you find files in main checkout's path during your run, ABORT and report immediately, do not attempt copy-recover."` The trap recurred three times this arc precisely because the chip briefs implied "use worktree" but didn't make verification a hard gate. **Outcomes from the three recurrences this arc:** Follow-up A's sub-bot self-detected via `git -C` cross-check after a few edits, copied files into its worktree, reverted main, re-verified, committed clean to `claude/pedantic-hellman-acb59d` @ `b8ac4ee1`. Follow-up B's sub-bot hit the same trap and recovered identically, landing on `claude/happy-heyrovsky-9561a7` @ `931ae36c`. P2b's sub-bot hit the trap and did NOT self-recover; its WIP (W1-W9 step files + import additions to `OnboardingWizardV3.tsx`) ended up stranded in main checkout uncommitted while two parallel sub-bots reported around it, requiring master to stash the WIP, complete unrelated cherry-picks, and unstash to restore the state for the eventual proper-branch landing. **Trap variant relationship:** this is the shell-level sibling of the existing "Worktree edits silently land on the main checkout if you Read/Edit by main-checkout absolute path" trap (above). That earlier entry covered the Read/Edit-tool variant within a single bot session; this entry covers the parallel-bot / shell-CWD-bounce variant where the bot has its own session but the shell sub-process flips trees on `cd`. Same root cause (absolute paths into main checkout silently work even from a worktree-rooted session), different surface (shell vs Read/Edit tool). **Dispatcher-side complement (codified 2026-05-20 after a wiki copy-fix chip recurrence):** the bot's `pwd` verification clause does NOT help if the brief itself cites file paths anchored to main checkout. A bot dispatched to a worktree that reads "fix the callout at `/Users/gnickles/Desktop/ResearchOS/frontend/src/app/wiki/...`" will Read/Edit that exact main-checkout path regardless of how diligently it pwd-checked. **Brief-writing discipline (master side):** when citing file paths in a chip brief for a worktree-bound sub-bot, use repo-relative paths (`frontend/src/app/wiki/features/experiments/page.tsx`) OR the sub-bot's specific worktree absolute path (`/Users/gnickles/Desktop/ResearchOS/.claude/worktrees/<name>/frontend/...`). NEVER the main-checkout absolute path. Repo-relative is preferred because it survives copy-paste between worktrees and reads naturally as "from your worktree root." Recurrence at `55aeff85` (2026-05-20): a chip brief to a wiki copy-fix sub-bot in worktree `tender-lovelace-7481b4` cited main-checkout absolute paths; the sub-bot pwd-verified worktree correctly, then misrouted the edit anyway because Read/Edit followed the brief's literal path strings. Single-file fix and recoverable (revert main, re-apply in worktree, single commit) but the trap is real and structural to how briefs reference files.

- **Parallel-arc AI Helper autogen collides at merge time. Resolve by delete-both + regenerate, not pick-a-side.** Surfaced by the Project Surface UI × Onboarding v3 simultaneous-arc-merge collision (2026-05-20) on `frontend/public/ai-helper/{full,lean,minimal}.md` + `manifest.json`. The AI Helper prebuild (`npm run --prefix frontend prebuild`) scans the WHOLE codebase to generate the four autogen files describing every API surface, type, schema. The output reflects whatever source code exists when prebuild runs. When two parallel arcs each touch AI-Helper-scanned surfaces (anything affecting `projectsApi` / `tasksApi` / `sidecar.ts` / `types.ts` / new exports under `lib/`), each arc independently runs prebuild on its branch and commits an autogen file that reflects only ITS view of the codebase. At merge time both files diverge — neither is "wrong" but neither is right after merge either. Git can't pick a winner. **Resolution:** `git checkout --theirs frontend/public/ai-helper/*` (the `--theirs` vs `--ours` choice is irrelevant — both get nuked next), then `npm run --prefix frontend prebuild` to regenerate canonical autogen reflecting BOTH arcs' source state. Confirm `git diff --stat HEAD` shows only the 4 ai-helper/* files post-prebuild. Then stage + commit the merge. Picking one arc's version + relying on the next prebuild to overwrite is the anti-pattern: it ships stale autogen until the next AI Helper-touching commit (could be days), during which time the AI Helper prompt advertises an inconsistent API surface to the user's Claude/ChatGPT/Gemini. **Collision predictor (refined after 4 data points 2026-05-20):** parallel arcs only collide here when BOTH have touched **TYPE contributions to the AI-Helper-scanned surface** — not just "any new lib/ export." The discriminator is whether the change adds a new TYPE that the AI Helper's surface-scanner picks up (sidecar field, new API method signature with new payload shape, new exported interface in a public API module, new entry in `types.ts`) versus pure utility/UI code the scanner skips. **Data points behind the refinement:** PS UI P2 (`projectsApi.getOverview/setOverview` — new API methods with new payload shapes) + v3 P2a (`feature-picks-tabs.ts` + sidecar.ts schema extension) → both BUMPED schema_hash, COLLIDED. PS UI P5 (`lib/project-activity/event-log.ts` — new module but only utility-function exports, no types in the AI-Helper-scanned surface) → schema_hash UNCHANGED, no collision. PS UI P6/P7/P8 (all UI-only edits) → schema_hash UNCHANGED, no collisions. Future master orchestrating multiple parallel arcs: anticipate this trap when phases touch `lib/types.ts` / `lib/local-api.ts` method signatures / `lib/onboarding/sidecar.ts` / new per-API modules under `lib/` that EXPORT TYPES. Phases that touch only UI components, internal helpers, or utility-function lib modules without type exports are collision-safe.

- **Source-only cherry-pick via `git checkout <branch> -- <file>` is WHOLE-FILE REPLACE, not diff-apply — reverts main's intervening edits when both bot and main modified the same file.** Surfaced by Onboarding v4 P0 cherry-pick 2026-05-21: bot's branch rooted at `f21800f3` adding `shareTaskAs` to `local-api.ts`; main at `c0c0ffa1` (-8 commits) had also modified `local-api.ts` with Project Surface P2's `getOverview`/`setOverview` + P5's `recordProjectActivity` emissions. Master ran `git checkout worktree-agent-X -- frontend/src/lib/local-api.ts` to source-only cherry-pick the bot's additions. The whole-file replace pulled in bot's STALE version of `local-api.ts`, OVERWRITING Project Surface's intervening work (-118 lines of `getOverview`/`setOverview`/`recordProjectActivity` wiring). Caught via diff stat `+197/-118` on a commit that was supposed to be `+97 net` per bot's report; reverted at `8f4ba897` + redone via `git cherry-pick 01aedf85` (diff-apply with 3-way merge) which correctly preserved both sides' changes. **The lesson:** `git checkout <branch> -- <file>` works ONLY when (a) the file is entirely new on the bot's branch, OR (b) main hasn't modified that file during the stale interval. When both bot AND main modified the same file, use `git cherry-pick <SHA>` instead — it applies the bot's DIFF rather than replacing main's file wholesale, and resolves the 3-way merge correctly. **For autogen files that both sides touched**, the cherry-pick will conflict on them; resolve via the b6c6dc73 pattern (`git checkout --theirs ai-helper/*` then `npm run prebuild` to regenerate from the merged source tree). **Predictor:** any time a stale-branch-rooted sub-agent's report shows the target file modified by `+N -M` (not just `+N` net), main likely has intervening changes to that file. Run `git diff <merge-base> <main> -- <target-file>` before cherry-picking to confirm; use `git cherry-pick <SHA>` if main shows any changes. The whole-file-replace pattern from 45c4bb88's source-only cherry-pick discipline is still correct for new files + untouched-in-main files; this entry refines the discipline for the both-sides-modified case the original entry didn't anticipate.

- **Staged-but-uncommitted work in main checkout blocks ANY parallel-arc merge, even when the staged files don't overlap.** Surfaced by repeated PS UI × Onboarding v3 collisions during cross-arc merges (2026-05-20; three recurrences). Standard master workflow: cherry-pick source-only from a sub-bot branch via `git checkout <branch> -- <files>`, which STAGES the files in main checkout's index without committing. Between the staging step and the final commit, the index is dirty. If master then tries `git merge <other-branch>` during that window (to land a parallel arc's chip), git aborts with "Your local changes to the following files would be overwritten" — even if the other branch and the staged files have ZERO file overlap. Git refuses to merge while there's a pending partial commit; it can't safely interleave a merge with the in-flight index state. **The aggregate behavior surprises an orchestrator thinking "my files don't touch theirs, safe to merge."** It's git's documented contract, but in a multi-arc workflow it's a structural footgun. **Resolution / discipline:** master-side cherry-pick + commit should be atomic. Don't pause between `git checkout <branch> -- <files>` and `git commit`. If a pause is unavoidable (e.g., need to investigate something mid-cherry-pick), either (a) commit a partial WIP and amend later, or (b) `git stash` the staged state so the index is clean. **Companion to 5950bcaf:** that entry covers sub-bots writing to main checkout instead of their worktree; this one covers master itself leaving staged work in main checkout between cherry-pick and commit. Both manifest as "dirty main checkout blocks parallel arcs" but root causes are distinct: 5950bcaf is sub-bot-CWD-confusion, this is master-workflow-pause. **Predictor:** any arc where master runs cherry-pick + commit as separate uninterruptible operations is safe; arcs where master pauses mid-pipeline (to investigate a sub-bot report, write a §8 refresh, draft a follow-up brief, etc.) risk blocking other arcs' merges during the pause window.

- **Worktree spawn lacks `node_modules`; the symlink-for-verify shortcut needs explicit cleanup before commit.** Surfaced by repeated observation across the Onboarding v3 + Project Surface UI arcs (2026-05-20; three chip cycles). Sub-bots verifying their work via `npm run typecheck` / `lint` / `prebuild` / `vitest` from a freshly-spawned worktree hit "command not found" or `ENOENT` because Agent/`spawn_task` worktrees don't inherit `node_modules` from main checkout (it's gitignored + heavy). Three sub-bots independently converged on the same workaround: `ln -s ../../node_modules frontend/node_modules` (or the equivalent relative path), run verify, then `rm frontend/node_modules` BEFORE any git operation. **The cleanup step IS the trap.** If the symlink survives into `git add`, git stages it as a regular file (it follows the link by default), and the commit becomes a 50MB+ artifact tracking the entire `node_modules` tree. Always run `git status` after verify + before `git add`; a "new file: frontend/node_modules" or "modified: frontend/node_modules" line means cleanup was missed. **Cross-reference §4 line 142:** the symlink works fine for `typecheck` / `lint` / `prebuild` / `vitest` but BREAKS under Turbopack / `npm run dev` ("Symlink node_modules is invalid, it points out of the filesystem root" — Turbopack rejects out-of-root symlinks). Static verification workflows can use the symlink shortcut; live dev-server workflows need a real `npm install` in the worktree. **Master-side brief discipline:** when spawning a sub-bot whose verify pass needs npm scripts, the brief should explicitly cover (a) the symlink shortcut as a faster alternative to `npm install`, (b) the `rm` cleanup before commit, (c) the `git status` cross-check, and (d) the Turbopack incompatibility if any test path tries to boot the dev server.

- **Password-field paste-blocking is a browser-extension behavior, not a CSS concern.** Surfaced by the Telegram bot-token paste investigation (2026-05-15) + the followup at commit `5aed62dc` that superseded `bd5d52e8`'s broken type-toggle pattern. Chromium extensions (Claude in Chrome, password managers in their content-script form, some security extensions) attach `paste` listeners at `window` and intercept based on `focused field === type="password"`. The intercept overwrites the input value with watermark text like `"[BLOCKED: Sensitive key]"` — confirmed via live DevTools event-bubble trace. **There is no CSP / Permissions-Policy / React-prop fix.** The block lives in the extension's isolated world; the page can't suppress it. **If you need both paste-compatibility AND visual masking on a non-credential field** (API keys, bot tokens, configuration secrets that aren't user-typed passwords), use `type="text"` with CSS `-webkit-text-security: disc` instead of `type="password"`. The CSS does the visual masking without triggering the extension heuristic. Trade-off: password-manager autofill won't fire for the field — usually a feature, not a bug, since these aren't user credentials. Pattern landed at [TelegramPairingModal.tsx](frontend/src/components/TelegramPairingModal.tsx); mirror that shape for similar fields. **Anti-pattern**: a show/hide toggle that flips `type="password"` ↔ `type="text"` makes the eye icon a paste-gate (users must click reveal first to paste), which contradicts every standard password-field UX. Use the CSS approach instead.

- **`main` parked in a parallel session's worktree blocks integration from the master checkout (2026-06-02).** With several sessions live, `main` is often checked out in another session's worktree (e.g. `seq-integ`), so `git switch main` in the master checkout fails with `fatal: 'main' is already used by worktree at ...`. If you then `git cherry-pick <sha>` while still on your own branch (which may already contain those commits), the pick silently resolves to "nothing to commit" and leaves a half-done cherry-pick state, recover with `git cherry-pick --abort`. You cannot advance `main` except where it is checked out. Options: (a) the worktree-owning session lands it, (b) ask Grant to free `main`, or (c) Grant lands it. Do NOT cherry-pick inside the other session's worktree on a hunch, a concurrent git-index operation there can corrupt that session. Prove the integration is clean before handing off: `git diff <fork-base> <main-HEAD> -- <changed-files>` empty means a conflict-free cherry-pick. To update a doc like this one against live main while main is parked, branch a fresh worktree off `main` (`git worktree add -b <name> <path> main`), edit there, and hand the branch off for landing.

- **A full-screen modal mounted inside a launcher's click-to-close backdrop must `stopPropagation` at its OWN root (2026-06-02, `3c14a772`).** The annotate editor (`ImageAnnotatorModal`) is `position: fixed` but rendered as a DOM descendant of `ImageMetadataPopup`'s backdrop `<div onClick={onClose}>`. `position: fixed` changes painting, NOT event bubbling, so every click inside (the rectangle tool, any tool, the canvas) bubbled to the backdrop and closed the whole popup (Grant: "I clicked the rectangle and it exited annotate mode"). Fix is one line on the modal root: `onClick={(e) => e.stopPropagation()}`, making the editor self-contained regardless of which launcher mounts it. Applies to any full-screen overlay launched from inside a backdrop-close container.

- **Browser-tab leader election must NOT steal the lock on focus (2026-06-02, `b8be3e27`).** The Telegram poller force-claimed the cross-tab localStorage lock on every window `focus` / `visibilitychange`, so with two ResearchOS tabs open, switching tabs ping-ponged the lock and the two `getUpdates` long-polls collided on Telegram's one-poll-per-bot rule (HTTP 409), dropping multi-step image flows mid-conversation. Fix: a focus only re-drains if THIS tab already holds (or can freely claim) the lock; it never steals from a live leader (`if (tryClaimLock(tabId)) pollController?.abort()`); failover still happens when the leader's lock goes stale. General rule for any cross-tab single-owner resource: stable leader + stale-failover, never steal-on-focus. The standby tab shows a calm "another tab" badge, not a "close your tabs" alarm, because results land in shared local data either way.

- **Correctness-critical numbers must be transcribed from a fetched authoritative source, never recalled (2026-06-02).** The nearest-neighbor primer-Tm (`lib/calculators/tm-nn.ts`) is a port of Biopython's `Bio.SeqUtils.MeltingTemp.Tm_NN`; the thermodynamic parameters (SantaLucia/Allawi NN table, von Ahsen salt-equivalent) were transcribed verbatim from the Biopython source curled to `/tmp` and read as RAW BYTES (not via the WebFetch summarizer, which can mangle numeric tables) and pinned by tests against Biopython's documented reference Tm values. Do this for any scientific/financial constant: fetch the real source, transcribe exactly, add a reference-value test. The OSS-credits generator follows the same rule for licenses, it reads the actual installed `package.json` `license` field, never guesses.

- **Adding an npm dependency is a bundle-weight plus shared-manifest decision (2026-06-02).** `mathjs` (added for the Scientific calculator) rode the synchronous `AppShell` shell chunk and added ~104 KB gzipped to EVERY page's first-load JS (not code-split). Swapped to `expr-eval-fork` (MIT, ~8 KB gz) for ~96 KB/page saved. Lessons: (1) profile bundle weight before adding a heavy lib to this local-first / no-backend app, a `next build` size delta is the proof; (2) isolate the engine behind a thin tested wrapper (`lib/calculators/scientific.ts`) so swapping it is a one-file change with the test suite as the contract; (3) `package.json` + `pnpm-lock.yaml` are shared manifests, commit a dep add atomically with the feature, and beware a concurrent session leaving the shared `node_modules` with a missing top-level hoist link (package intact in the `.pnpm` store), which breaks imports for everyone until `pnpm install`. Run binaries directly (`node_modules/.bin/tsc`, `node_modules/.bin/vitest`); `pnpm exec` triggers a dep-status reinstall that fails on the sharp/unrs-resolver build-scripts approval.

- **The vendored SeqViz files are `@ts-nocheck`, so `tsc` does NOT catch undefined-variable or out-of-scope references in them (2026-06-04).** `frontend/src/vendor/seqviz/Linear/{Primers,SeqBlock,Linear}.tsx` and `Circular/Circular.tsx` all open with `// @ts-nocheck` (vendored, kept out of strict typecheck). A clean `tsc --noEmit` is NOT a real check for these files. During the primer base-render redesign a reference to `seqFontSize` that was never destructured in `Linear.render` compiled clean and then crashed the ENTIRE viewer at runtime via SeqViz's error boundary ("seqFontSize is not defined"). After editing a vendored seqviz file, manually grep the enclosing render/function scope for every new identifier you reference and confirm it is actually defined or destructured before claiming done. Related styling trap in the same files, the shared `annotation` style (`vendor/seqviz/style.ts`) sets `fillOpacity: 0.7` AND `strokeWidth: 0.5`, and because an inline `style=` beats SVG presentation attributes, applying `style={annotation}` silently grays out any `fill` and overrides any `strokeWidth` set as an attribute. The primer base shapes deliberately drop that style and set a solid fill plus a real border width instead.

- **The base-level primer render spans four files that must stay in sync (2026-06-04).** SnapGene-style primer drawing lives in `lib/sequences/primer-base-layout.ts` (pure column-mapping math, strict + tested), `vendor/seqviz/Linear/Primers.tsx` (the SVG, annealing box + raised 5' tail box + pulled-back 3' arrowhead + bases + label), `vendor/seqviz/Linear/SeqBlock.tsx` (`primerRowHeight` sizes each primer row taller in the base view and adds a second box-lane for tailed tracks plus barb headroom; `primerRowsHaveTail` detects the tail), and `vendor/seqviz/Linear/Linear.tsx` (`blockHeight` must add the SAME per-track height so stacked blocks never clip). Change the geometry in one without the others and you get clipped boxes or off-screen overflow. The proportion constants (`baseColor`, `boxFill`, `borderW`, `headLen`, `barbRise`, `barbBack`) are named at the top of the primer element in `Primers.tsx` for live tuning. A primer with no stored oligo note (`/note "primer <SEQ>"`) gets its bases by pinning the layout directly to the recorded binding span (forward template, reverse-complemented for a reverse primer), NOT by re-searching, so a short or repeated region cannot make the bases land on a duplicate elsewhere. Full inheritor briefing in `docs/orchestrator-handoffs/2026-06-04-sequence-editor-handoff.md`.

---

## 7. Recent landed work (top of `main`)

For the canonical current state, prefer `git log --oneline -30 main`. Below is a snapshot of the major workstreams in motion as of 2026-05-23. Treat the bullets as orientation, not gospel; refresh the snapshot when arcs close.

- **V4 onboarding tour** replaced V3 wholesale. New surface: `frontend/src/components/onboarding/v4/` with `TourBootstrap.tsx` (the auto-mount + fresh-user gate), `step-types.ts` (typed step-graph schema replacing the v3 `WizardStepMachine`), per-step React components, and an `InputLockOverlay` driving the cursor-playback / scroll-lock during scripted scenes. V4 fires on fresh users (no main user in `_user_metadata`, no completed/skipped flag on the sidecar) and is verified via the persona break-bot panel (literal / explorer / distracted / skeptic / restart). Existing v3 sidecar fields (`feature_picks`, `wizard_completed_at`, `wizard_skipped_at`, `wizard_force_show`, `wizard_resume_state`, `lab_tour_pending`) are honored under the v4 schema; pre-v4 sidecars normalize to "wizard not auto-mounting" the same way they did under v3.
- **Lab Mode retirement (R1 through R6 in flight).** R1 (unified sharing primitive) landed, replacing the Lab pseudo-account share path with direct user-to-user `shared_with`. R1b/R1c migration helpers landed (lazy-normalize on read + repair button in Settings). R2 (widget framework via `react-grid-layout`) landed. R3 (widget catalog port) is in flight via a sub-bot. R4 (PI walkthrough rip), R5 (`/lab/*` route + `users/lab/` folder deletion), R6 (wiki rewrite) are queued. R6 is paired with the #115 wiki audit sweep and held until R3 through R5 stabilize. Design doc: `LAB_MODE_RETIREMENT_PROPOSAL.md`.
- **Lab Head account type (Phase 1-6, all landed).** New account type with `Lab Head` capability bits in `_user_metadata.json` (schema v5), Lab Inbox (`/lab-overview`), threaded comments on shared tasks, cross-lab metrics aggregation, session edit mode, and per-task archiving. The `_user_metadata.json` schema bump is the load-bearing piece. Older sidecars normalize on read.
- **BeakerBot scene system.** Eleven scenes shipped under `frontend/src/components/animations/scenes/` (CoffeeRefill, BugStomp, BlowingBubbles, plus 8 others), each composed of BeakerBot pose primitives plus per-scene props. Reward animation mode triggers on milestone hits; bug-stomp splat fires when error-capture catches a runtime error. BeakerBot is the only mascot. The 4 candidate alternates were retired (per `feedback_mascot_is_beakerbot`).
- **Animation revamp.** `ANIMATION_METADATA` registry now drives 10 custom SVG icons that replaced inline emoji throughout the app (toolbar buttons, empty-state graphics, notification glyphs, tour callouts). Single source of truth, themable, recoloring under dark mode handled centrally.
- **Lab Overview (`/lab-overview`, formerly `/lab-inbox`).** React-grid-layout widget canvas with a sidebar widget rail (drag widgets in/out, persist layout per user). Filled-color calendar events (events render with their source-color fill instead of just an accent bar). Replaces the old static dashboard.
- **Experiments and TaskDetailPopup redesign.** Modal-quality bar (progress + state pills), shortcut palette, skeleton placeholders during load, simplified header. Touches `TaskDetailPopup.tsx` and the experiments list row primitive.
- **Feedback / error capture flow.** GitHub issue templates (`bug.yml`, `feature.yml`, `feedback.yml`) ship in `.github/ISSUE_TEMPLATE/`. `FeedbackButton` floats on every page; auto-error capture wires through `ErrorBoundary` so uncaught exceptions surface as pre-filled issue drafts.
- **Dev walkthrough button.** Two-mode menu on the floating cluster: folder setup mode (fresh data folder bootstrap) and user setup mode (within-folder user provisioning). `IS_DEV`-gated.
- **Calendar color UX** is in flight right now (chip dispatched, sub-bot working).
- **Lab calculators + integration hardening (2026-06-02).** The "Lab calculators" modal (`components/CalculatorsButton.tsx`) gained a general-purpose **Scientific** tab (now the default first tab) built on a thin, tested wrapper over `expr-eval-fork` (`lib/calculators/scientific.ts`, DEG/RAD via scope-overridden trig, Ans/memory). The **Primer Tm** tab moved off the crude %GC formula to nearest-neighbor thermodynamics (`lib/calculators/tm-nn.ts`, a reusable Biopython port; the sequence-editor arc's primer design should import it rather than vendoring a second Tm) and now needs only the sequence, with optional salt / Mg2+ / dNTP / oligo for accuracy. Also this session: the annotate-mode click-bubbling fix (`3c14a772`), the Telegram single-tab leader-election fix (`b8be3e27`), the de-bloat x sequence-editor project-link **seam** (`lib/sequences/api.ts` `sequencesApi.listByProject` returning `[]` until sequence Phase 1 lands; cross-arc decision, Grant), and the "Built on open source" credits page + `THIRD_PARTY_NOTICES` + `ACKNOWLEDGEMENTS.md` (auto-generated via `scripts/build-open-source-credits.mjs`).

- **Branding + landing + BeakerBot avatar arc (2026-06-04, on local main).** Real brand kit in `brand/` (logo mark, avatars, banners, favicon SVG masters + hi-res PNGs + generators + README). Favicon swapped to BeakerBot (`frontend/src/app/icon.svg` + RGBA `favicon.ico`). BeakerBot gained an opt-in `alive` idle (subtle sway/blink/gaze, gated to neutral poses, reduced-motion-aware; preview `/dev/beakerbot-gallery`), now applied to the standing mascots. The `/welcome` landing was redesigned (light + rainbow balanced hero, sequence-editor + cross-boundary-sharing bands, 3-way SnapGene comparison, stale fixes). Standalone talking-head avatar tool for recording tutorials at `tools/beakerbot-avatar/` (mic + MediaPipe face tracking). **Full state + open threads (login-screen redesign, screenshot capture, parked sharing/sequences wiki plans) are in `docs/handoff/branding-landing-avatar-arc.md`, read that to continue this arc.**

- **Migration to solo + welcome relaunch arc (2026-06-10, on origin/main + deployed).** Three things shipped together this session.
  - **Phase 7a migration (multiuser `users/` folder to one-folder-one-user).** `lib/lab/migrate-to-solo*` planner + executor + node/FSA adapters, made data-layer iron-clad (synth CI matrix + crash-safe executor + real-folder every-primary runs; 3 real bugs fixed incl a latent FSA `exists()` prod bug). Shipped a role-aware BLOCKING on-connect gate (`components/lab/MigrationGate.tsx`, mounted in providers): the owner gets "convert this folder to mine", a labmate gets "take my data to my own folder" (self-export auto-disconnects to avoid a zombie re-create). admin/business carved out via `lib/routes/operator-surface.ts`. Live-verified both paths. Depth in `[[project_migration_to_solo_ironclad]]`.
  - **Welcome page bold-rainbow reimagining.** `components/welcome/WelcomePage.tsx` rebuilt: 8px rainbow ribbon + `RainbowFrame`, the giant hero loop replaced by an NIH Data-Management-Plan hero card (Grant's call), a "start solo / grow into a lab / split back out" 3-tier section, and a 4-way comparison (added Quartzy + an inventory row). Two audit bots drove an honesty pass (dropped an un-cited LabArchives price, killed a "hosted free too" over-promise, lab-tier "coming soon" pill). Depth in `[[project_welcome_page_redesign]]`.
  - **Fresh demo clips, captured autonomously.** All 8 `DemoLoop` slots are real clips now (7 freshly recorded; `own-your-data` keeps an existing Finder video). Files live on the Blob bucket `tkqei2x7bdmdvg7v` at exact pathnames. The recorder is a committed tool: `scripts/welcome-demo-capture/` (Playwright in-browser video of the `/demo` fixture, no OS screen-recording permission needed, forces light mode, hides dev chrome, auto-trims the loader; `node scripts/welcome-demo-capture/capture.mjs <clip>`). The PI-dashboard clip needed a demo-only "view as lab head" toggle (`components/DemoViewAsButton.tsx`) that flips the fixture identity via a new internal `?demoViewAs=` param (honored only on `/demo`, sticky-backed in `wiki-capture-mock.ts`). **Trap:** `vercel env pull` overwrites `.env.local` wholesale and clobbers hand-added local-only vars; pull into a separate file and copy what you need. Blob uploads need the store rw-token (not in repo); easiest is the Vercel dashboard drag-upload.

- **Marketing / launch slide deck (2026-06-10, on `main`).** A self-contained, recording-ready HTML deck at `docs/marketing/researchos-deck.html` (40 slides, vanilla-JS scaler + keyboard nav, no build step, just open it in Chrome). Assets are self-hosted under `docs/marketing/assets/` (Geist woff2, BeakerBot SVGs, wiki screenshots, the 5 welcome demo `.mp4`s, mobile emulator shots). Brand decisions to preserve: the pastel rainbow stays only as the signature top/bottom bands and the BeakerBot liquid, while card/accent color uses the three app colors (sky `#1AA0E6`, amber `#F59E0B`, coral `#FF6F61`), NOT rainbow-on-cards (Grant's call). Live CSS animations: an animated BeakerBot gallery ("Meet BeakerBot") and a hand-built recreation of the native Skia launch splash (the real one cannot be embedded; the splash slide auto-swaps to a real recording if one is dropped at `assets/shots/video/splash.mp4`). The two charts (codebase net-lines-over-time, features-over-time) are drawn from real git/tracker data baked into inline JS arrays. Demo buttons open the DEPLOYED site via `DEMO_BASE = "https://research-os.app"` (a const at the top of the inline `<script>`). Content arc: accidental-origin story, the tracker charts, local-first + optional cloud + the R2/DO/relay cloud architecture, solo-vs-accounts, the "point any AI at your own data" angle + the shipped AI Helper, a feature tour, the companion app, pricing/savings vs a LabArchives ($330/user/yr academic) + SnapGene stack, the LLC (why + the six formation steps) + operator dashboards (`/admin`, `/admin/business`) + the two standing AI workers (the Accountant and the Maintainer, see `docs/ops/INBOX_BOT.md` and `MAINTAINER_BOT.md`), and the open-source credits (from `credits.json`). All numbers were verified against git / the wiki / `credits.json`; sensitive LLC identifiers (EIN, bank, registered-agent address) were deliberately kept off every slide since it is recorded for YouTube. House voice throughout. Built by the marketing-agent role in one session.

- **Fuzzy search, spell-check, and OCR cleanup (2026-06-10, on origin/main).** Three proven-OSS data/lib additions, no homegrown algorithms (Grant's call, do the right thing not the fast thing). (1) Global search (`components/beaker-search/global-source.ts`) gained a MiniSearch (`minisearch` 7.2.0, MIT) additive fuzzy tier BELOW the strict subsequence matcher, so exact and prefix hits still rank first and fuzzy/typo hits fill in under them; `global-index.ts` added a `note` entry type with an `ocr` field so scanned-note text is searchable. (2) Spell-check (`lib/spellcheck/spellchecker.ts`) is a lazy `nspell` (2.1.5, MIT) + `dictionary-en` (4.0.0) loader (en.aff/en.dic fetched from `public/spellcheck/`) seeded with a curated biomedical wordlist plus the user's own inventory/methods/note-title terms (`SpellcheckAutoSeed.tsx`, mounted in providers); the CM6 editor underline is a `@codemirror/lint` linter (`lib/markdown/cm-spellcheck/spellcheck.ts`) that skips code and URLs and offers click-to-fix plus add-to-dictionary. The custom dictionary is folder-backed and SCOPED, lab-wide (`_spellcheck_dictionary.json`) for lab accounts and per-person (`users/<user>/spellcheck_dictionary.json`) for solo, keyed off `settings.lab_id` (`lib/spellcheck/custom-dictionary.ts`). (3) OCR auto-correct is deliberately CONSERVATIVE, `confidentCorrection` rewrites a token only when there is a single confident suggestion; the OcrImage "Clean up" button (`components/OcrImage.tsx`) runs `cleanOcrText`.

- **Mobile companion features + Companion wiki section (2026-06-10/11, on main).** Alongside the cosmetic convergence (see §5), three companion features landed plus the scan crash fix. (1) **View method on phone**, the laptop publishes the active experiment's method to a paired phone over a sealed relay snapshot (`mobile/lib/mobile-relay/method-snapshot.ts` publisher, `mobile/app/method.tsx` viewer); add-variation writes back to `variation_notes` via `tasksApi.saveVariationNote`. (2) **Today pull-down**, a collapsible Today glance on the Notebook tab (chevron, default-on pref `researchos.todayPrefs.v1` in `mobile/lib/today-prefs.ts`) plus a flagship scan card. (3) **Scan crash fix (launch-critical)**, `isScannerAvailable()` in `mobile/lib/ocr.ts` now probes the native module via `TurboModuleRegistry.get('DocumentScanner')` instead of the JS-wrapper check, so tapping scan on a build that lacks the native module hides cleanly instead of throwing and unmounting the Notebook tab. The **Companion wiki section** (`frontend/src/app/wiki/features/companion/`, 7 pages, index plus pairing, capture-and-route, scanning-notes, today-glance, view-method, inventory-scanning) is drafted in house voice; 3 of 7 screenshots are captured and committed (`companion-home/today/inventory.png`) via the adb-driven `scripts/capture-companion-screenshots.mjs` run in emulator demo mode. The other 4 (scan, pairing-qr, method, capture) plus the chooser footer-height check are DEVICE-ONLY (the emulator has no native scanner, and demo mode never opens NotebookChooser since it needs a real paired session with notebooks), so they ride Grant's iPhone pass. Dev-client install and run reference for both platforms is `docs/COMPANION_DEV_INSTALL.md`.

- **Fork an experiment's edited method into the library (2026-06-13, on main, browser-verified).** When a method is attached to an experiment, the researcher's per-task edits (`body_override` / the per-type structured overrides `pcr_gradient`+`pcr_ingredients`/`lc_gradient`/`plate_annotation`/`cell_culture_schedule` / `variation_notes`) live ONLY on that task's `method_attachment`, never the reusable library method. A **"Save as new method"** action in the experiment method-tab header (`components/methods/ForkToLibraryAction.tsx`, mounted in `MethodTabs.tsx` under `!readOnly && activeMethod`, kits included) promotes the attachment's CURRENT edited content into a brand-new independent library `Method` linked via `parent_method_id`. The core is `forkAttachmentToLibrary(source, attachment, name)` in `lib/methods/fork-method.ts` (extends the existing `forkMethod`, which is now a thin no-attachment wrapper -> byte-identical legacy clone). Per-type overrides are baked over the source exactly as the viewers render them (override-or-source); `variation_notes` append to the method's text sink (body/notes/description) under a labeled heading; plate `wells` -> `region_labels` via an inlined pure converter so the lib doesn't import a client component. DELIBERATELY NOT baked in (per-run measurement data, not reusable template): `qpcr_analysis` Cq readouts and compound `compound_snapshots` (a kit forks as one method = its child references; Grant's "a kit IS one method, don't track the diff parts"). Locked by 9 new unit tests in `fork-method.test.ts`.
- **Browser-verifying a local-main feature when every running dev server is a sibling lane's worktree (2026-06-13).** `preview_list`'s reported `cwd` is the repo root even when the `next dev` process actually runs from a `.claude/worktrees/<x>/frontend` worktree on a DIFFERENT branch (check `ps aux | grep "next dev"` for the real path) -- so driving it shows that lane's code, not yours. You also must NOT `preview_start` a 2nd dev against the MAIN checkout's `frontend/` while Grant's `:3000` runs (shared `.next` corrupts Turbopack). The clean pattern: `git worktree add --detach <path> <main-HEAD>` (includes your merged commit), `cp -c -R frontend/node_modules <path>/frontend/node_modules` (COW, not symlink), add a `.claude/launch.json` entry pointing `cd <path>/frontend && npx next dev -p <freeport>`, `preview_start` it, drive `/demo`. GOTCHA: demo data is in-memory per tab; a FULL navigation (`location.href=...`) RE-SEEDS the demo store and discards anything you just created -- use CLIENT-SIDE nav (click the in-app nav link) to move between pages after a write. Tear down after (preview_stop + `git worktree remove --force` + revert the launch.json entry).

For deeper design context, see `LAB_MODE_RETIREMENT_PROPOSAL.md`, `STREAK_AND_MILESTONES_PROPOSAL.md`, `PRE_ONBOARDING_PROPOSAL.md`, and the BeakerBot scene catalog under `frontend/src/components/animations/scenes/`.

---

## 8. Open backlog / things worth queuing

> **PARALLEL SESSIONS ACTIVE (2026-06-02).** Several manager sessions are working this repo at once: a minimalism/de-bloat arc, a sequence-editor ("Benchling-type") arc, and a photo-annotation arc. Before building, read **`docs/proposals/MINIMALISM_ARC_COORDINATION.md`** (what the de-bloat arc has landed and is tearing down) and **`docs/proposals/SEQUENCE_EDITOR_PROPOSAL.md`**. **The customizable widget-dashboard framework (`components/lab-overview/**`, the `/` home canvas, `/lab-overview`) is being DELETED. Do NOT build new features on it.** The editor stack (`LiveMarkdownEditor`/`InlineMarkdownEditor`/`TaskDetailPopup`/`ImageStrip`/`FileStrip`) and project-folder/Workbench territory are active collision zones. Integrate via per-commit `git cherry-pick`, never a stale-anchor tree-merge. The "Active workstreams" snapshot just below is stale (2026-05-23); trust the coordination docs for current state.

### Active workstreams in flight (as of 2026-05-23)

- **R3 widget catalog port** (Lab Mode retirement): sub-bot porting the widget set onto the R2 react-grid-layout canvas. Touches `frontend/src/components/widgets/` and `app/lab-overview/`.
- **Calendar color UX**: chip in flight (color picker + persistence on event categories, propagating into the calendar render path).
- **AGENTS.md refresh**: this pass (retire sub-manager tier + prune stale snapshot).

### Queued, not in flight

- **R1d `methodsApi` `shared_with` API**: drop the legacy `is_public` flag from methods; replace with the unified `shared_with: string[]` shape used elsewhere.
- **R4 PI walkthrough rip**: remove the Lab Mode-specific PI walkthrough now that V4 supersedes it.
- **R5 Lab Mode `/lab/*` pseudo-account deletion**: remove the `/lab` route directory + `users/lab/` data folder + all callers; the unified sharing primitive (R1) is the replacement.
- **R6 wiki rewrite**: paired with the #115 wiki audit sweep. Held pending Grant green-light.
- **5 Mira-as-PI persona verifiers**: fire after R3 lands; exercises the Lab Head capability set against the new widget canvas.
- **PI customizable sidebar**: R2 extension; per-user widget rail + reorderable sections.
- **Open-source acknowledgements (VITAL, Grant 2026-06-02).** A "Built on open source" credits surface (in-app `/open-source` page + `THIRD_PARTY_NOTICES` + `ACKNOWLEDGEMENTS.md`, auto-generated from actual installed licenses via `scripts/build-open-source-credits.mjs`). This is both gratitude and a license obligation (MIT/BSD attribution, Apache-2.0 NOTICE). **Drift durability (2026-06-08).** Adding a dependency without regenerating these artifacts used to break every Vercel deploy (the prebuild ran the generator with `--check` and HARD-failed on staleness; the `qrcode` dep landed via PR merge #9 and wedged all builds). Two-layer fix so this cannot recur: (1) the prebuild now REGENERATES instead of gating (`build-open-source-credits.mjs`, no `--check`), so a drift can never fail a build, and the served `/open-source` credits are always fresh; (2) a pre-commit self-heal (`scripts/credits-precommit.mjs`, wired by `scripts/install-git-hooks.sh`) regenerates + stages the artifacts whenever a commit touches a `package.json`, keeping the committed compliance files honest with no human step. Fails open. To verify in sync manually: `npm run credits:check` (in `frontend/`). See `[[project_open_source_credits]]`.
- **Lab-calculators modal is too tall (Grant 2026-06-02; de-bloat/minimize domain).** The Scientific tab's keypad overflows the modal's `max-h-[88vh]` and scrolls vertically while horizontal space goes unused. Grant wants it WIDER, not taller: widen the modal (`max-w-2xl` on `CalculatorsModal`) and/or lay the scientific keypad out in two columns (function keys beside the number pad in `ScientificCalcTab`) so it fits on one screen.
- **Notification delivery, the remaining halves (Grant 2026-06-12, queued to be DONE not just parked).** The notification-preferences feature shipped phases 1-3 (per-type routing to bell / laptop pop / email / phone, pushed 2026-06-12, see `[[project_notification_preferences]]`). (1) **Real phone push (a buzz, not the synced list).** Design `docs/proposals/2026-06-12-phone-push-notifications.md` (wake-and-fetch keeps E2E: a generic content-free push wakes the companion to fetch + locally decrypt the snapshot it already reads). **P1 (laptop-open buzz) is BUILT on LOCAL main 2026-06-13 (merge d233b6dd3, commit 7e5428949, NOT pushed, gate-verified tsc 0 web+mobile+relay + tests 22/22):** the `NotificationDesktopWatcher` fires one generic buzz per poll batch (publishes the fresh sealed snapshot, then POSTs the paired devices' Expo tokens to the new `frontend/src/app/api/send-push/route.ts`), gated by the existing quiet-hours + per-category phone toggle + account gate + the `autoPublishSnapshotsToPhones` kill switch; relay gained an additive `push_token` column + a device-signed `/capture/devices/push-token` route + returns the token from `/capture/devices`; mobile registers the Expo token (`mobile/lib/push-token.ts`) at pairing + notifications-screen mount, with tap-to-open in `_layout.tsx`. The push payload carries NO research content (generic body + coarse category only; the single most important check, covered by the send-push test). **INERT until Grant (a) redeploys the relay worker (`wrangler deploy`) for the column + route and (b) builds the companion as an EAS dev build (remote push fails in Expo Go), then a real-device end-to-end buzz.** **P2 (laptop-CLOSED cross-user buzz) is BUILT on LOCAL main 2026-06-13 (merge d2cec30c7, NOT pushed, gate-verified; design `docs/proposals/2026-06-13-phone-push-p2-and-sender-email.md`, decisions A1/B1/C1).** Model is SENDER-TRIGGERED (the relay sees no cross-user event stream, so the online sender drives it): new relay routes `POST /capture/notify-config` (recipient laptop mirrors its routing matrix + quiet hours + tz offset to its own DO so the relay gates server-side) + `POST /capture/notify-recipient` (sender-signed; relay runs the recipient's gate, seals a generic content-free snapshot to the recipient device X25519 keys into a `notifications-pending` R2 lane, sends a generic Expo push, per-DO cooldown). Worker `sealToRecipient` is a byte-for-byte port of the frontend seal, proven by a round-trip test through `openSealed`. ALL P2 triggers now wired (merge 7f026df03): `sendShare` + `sendRawShare` (cross-boundary, recipient Ed25519 from the directory lookup) + the four in-folder lab actions in `pi-actions.ts` (assign/flag/purchase/announce) via a `buzzRecipientPhone` helper that resolves the co-member's Ed25519 from their `_sharing_identity.json` sidecar (category derived from the notif type). INERT until the SAME relay redeploy + EAS dev build as P1, then a real-device offline-buzz test. (2) **Phase 2.5 sender-triggered email is BUILT** (same merge) on the SAME `notify-recipient` route: the handler evaluates BOTH channels (phone + email) on the recipient's own per-category toggle + quiet hours, and when email is routed + the recipient email is synced it sends a generic content-free mail to the recipient's OWN address via the existing Vercel `/api/notify-email` (Resend) using the existing `APP_BASE_URL` binding (no amplification, per-DO 30s cooldown). notify-config now carries the email channel flag + the recipient email. **(3) P3 (the reminders category) is SPLIT: P3a shift_alert is BUILT** (commit 64a16b1c5, merge 17026f1bc) — not scheduled, it has an online actor (the shifter), so `recordShiftAlerts` in local-api.ts now collects each shifted task's shared_with set (minus the shifter) + fires the P2 notify-recipient route per recipient with category `reminders`; no relay change. **P3b scheduled event_reminder buzz is BUILT** (commit 69ba28a66, merge af8c98487): the laptop's `use-event-reminders.ts` mirrors its upcoming due schedule (content-free opaque id + fire_at) to a new user-signed `POST /capture/register-reminders` (REPLACE each pass, account-only, gated by autoPublishSnapshotsToPhones); the CaptureInbox DO arms an alarm (CollabRoom setAlarm pattern) to the nearest fire time and `alarm()` delivers due ones via the shared `deliverToRecipient`, with a DEAD-MAN'S-SWITCH (delivers only when the laptop has gone silent >3min, so it stands down while the laptop is online + P1 handles it, no double-buzz). `expo-notifications` is already a dep; Expo Push Service is free. Every push/email runs through the existing quiet-hours + per-category gate. All account-tier only, consistent with the existing gating. **(4) TEST SYSTEM BUILT + relay core VERIFIED** (merge ceb1c64f9, guide `docs/testing/2026-06-13-notification-push-test-guide.md`): `relay/scripts/smoke-notify.mjs` (`npm run smoke:notify`) drives a local `wrangler dev` DO as sender+laptop+phone with real crypto and PASSES 10/10 — notify-config, the worker seal opening with the real `openSealed`, the gate (muted + quiet hours), the cooldown, register-reminders, AND the DO alarm firing a due reminder. The cooldown + dead-man's-switch are env-tunable for the harness (`NOTIFY_COOLDOWN_MS`/`REMINDER_STALE_MS`). Layer 2 = a Claude-in-Chrome prompt for the laptop pop-up + email; Layer 4 = the only un-fakeable part (real OS buzz, needs the EAS build). **NET: the entire notification-delivery lane is COMPLETE IN CODE on local main and the relay core is END-TO-END PROVEN (not just tsc); only the literal OS-push delivery awaits Grant's relay `wrangler deploy` + EAS companion dev build + a real-device test.**

- **Data Hub v2 stats, the SHORT remaining tail (audited 2026-06-14).** The "do it all" 18-card v2 stats expansion (`[[project_datahub_v2_stats]]`, 4 themes T1 estimation / T2 dose-response+regression / T3 survival+repeated-measures / T4 diagnostics+public-validation) is ~95% SHIPPED, not a greenfield build. Confirmed BUILT by an engine audit: the validation gate (`frontend/scripts/gen-datahub-stats-golden.py` + the transparency `datahub-stats` domain), the LLM-FREE assumption-aware finder (`GuidedAnalysisWizard.tsx` + `planner.ts`, auto-checks Shapiro/equal-variance + Report Card), and the stats themselves — t-tests/Welch + Cohen's d + Hedges' g + power/sample-size (`ttests.ts`), one/two-way + nested ANOVA + Tukey/Dunnett + eta-squared/omega-squared (`anova.ts`), Mann-Whitney/Wilcoxon/Friedman, Pearson/Spearman, linear/multiple(+VIF)/logistic(+Firth) regression, 4PL/5PL dose-response + EC50 + model comparison (AICc) + global fit (`fit/`), Kaplan-Meier + log-rank + **Cox PH** (`survival.ts`), mixed-model (`mixed-model.ts`), Grubbs outliers, ROC/AUC, QQ/residual diagnostics. REMAINING TAIL (1 stat, PARKED 2026-06-14): **T3 Gehan-Breslow-Wilcoxon is now BUILT** (`c7876e39e`, weighted log-rank in `survival.ts`, wired + gate-pinned; it shipped ~15 min after the audit flagged it missing — the fleet clears this tail in real-time, so re-verify any "missing" claim before building). The SOLE remaining gap is **T4 ROUT** robust-outlier (Prism's FDR/Q method; `grubbs.ts` is Grubbs/ESD only; uncontended at park time). PARKED + DE-RISKED, build it as its own focused session: ALGORITHM (verbatim, Motulsky-Brown 2006 / PMC1472692) = robust-fit (Lorentzian/Cauchy iterative, K params) -> RSDR = P68 * N/(N-K) where P68 = 68.27th pctile of |residuals|; t_i = |resid_i|/RSDR; two-tailed P from t with N-K df; FDR rank residuals low->high, test the 30% furthest, threshold alpha_i = Q*(N-(i-1))/N, flag the point + all more-extreme once P < alpha_i; default Q = 1%. VALIDATION PIN (published-validation framework, no Prism needed) = the paper's worked example, a 13-pt one-phase exponential decay (3 params): Q=1% flags Time=3 (resid -395.21, t=5.05, P=0.0005), Q=5% adds Time=2 (resid -108.51). BUILD NOTE: ROUT is a robust-REGRESSION method, so wire it into the curve-fit/dose-response path (`engine/fit`), NOT as a Grubbs-style column test; the underdetermined piece is the Lorentzian iterative robust fit (secure the full primary source or a Prism run so the result numbers-matches before shipping; do NOT ship it half-validated, Principle 2). COMPLETENESS AUDITS (verify, likely fine): every shipped test is surfaced in the chooser catalog (`NewAnalysisDialog`) AND has a gate pin in `datasets/datahub-stats.ts`; confirm the equal-variance test (Levene/Brown-Forsythe) name in `assumptions.ts`. STANDING RULE: each new stat ships WITH its scipy/R/Prism validation case through the gate (Principle 2, the most important one). FINDER DESIGN DECISION (Grant + me, 2026-06-14): the analysis finder is and STAYS LLM-FREE (deterministic, assumption-aware, reads the real data, explainable, never hallucinates a test). The optional LLM-assisted entry is ALREADY BUILT, not deferred: the "Help me choose an analysis" button in `NewAnalysisDialog.tsx` (`handleHelpMeChoose`, account-gated by `canUseAI` so it HIDES when AI is off) opens BeakerBot seeded "help me choose an analysis for this table", and the `run_datahub_analysis` BeakerBot tool (`lib/ai/tools/datahub-analysis.ts`, built 2026-06-11) maps the NL request onto table+columns+paired-intent, routes through the SAME `planAnalysis` deterministic planner + reference-validated engine the wizard uses (engine computes, model only narrates), confirms groups via ask_user, and navigates to the stored result. `plan-study.ts` is the power/sample-size NL twin. So the AI-assisted finder is COMPLETE but DARK until AI billing ships (`[[project_ai_assistant]]` / `[[project_ai_billing_build]]`; no free AI in beta). The only UNBUILT variant is a wizard-PREFILL flavor (NL -> fill the deterministic stepper instead of running in chat); likely unnecessary since the chat flow already delivers the value.

### Held pending Grant green-light

- **#115 wiki audit sweep**: 33 per-page sub-bots running a structured audit pass (voice, screenshots, callout variants, dead links, callout-vs-blockquote drift).
- **R6 wiki rewrite**: paired with the audit above; held until the audit's findings shape the rewrite scope.

---

## 9. Quick-start playbook for the master bot

1. Read this file end-to-end.
2. Run `git log --oneline -20` and `git status` to see where main is.
3. For any new request: decide inline vs spawn. Inline if < 1 hour and self-contained; spawn if larger or parallelizable.
4. For spawned tasks, write a prompt that's a complete briefing — see Section 4 above for the recipe.
5. Default to a 2–3 sentence recommendation when the user asks an exploratory question. Don't dive into code until they greenlight.
6. After landing a fix, run typecheck. Then ask the user to test before pushing.
7. Push only when the user says push.

---

## 10. Useful commands cheat-sheet

```bash
# Dev
cd frontend
./start.sh                     # or npm run dev — Next on :3000

# Typecheck / lint
npx tsc --noEmit
npx eslint src/

# Where the data lives during dev (example only — this is Grant's local path;
# other users / dev environments will have different roots, and the app
# accepts any directory the user picks via showDirectoryPicker)
/Users/gnickles/Library/CloudStorage/OneDrive-UW-Madison/ResearchOS_FungalInteractionsLab

# Cleanup script (zips originals to Desktop, then deletes migrated copies)
node scripts/cleanup-migrated-images.mjs "<data-folder>" --dry-run
node scripts/cleanup-migrated-images.mjs "<data-folder>" --prune-empty-dirs

# Hosted URL
https://research-os-xi.vercel.app/
```

Good luck.
