# Parity screenshots

Drop live-app screenshots here named by the parity-board screen id, so the
per-page polish agents can read them next to the ideal mockup:

  notebook.png  inventory.png  calc.png  timer.png  wiki.png
  note.png  scan.png  deduct.png  bulk.png  addpurchase.png  pair.png

Source the names from "Download shots" in docs/mockups/2026-06-08-parity-board.html.
Each polish agent reads docs/parity/<id>.png (live) + the ideal mockup + the RN
screen, then closes the gap. These PNGs are working artifacts, not shipped.
