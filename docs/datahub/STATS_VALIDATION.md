# Data Hub statistics validation (the standing requirement)

The Data Hub analysis engine is a free, open, local-first alternative to GraphPad
Prism. Researchers will only trust it if they can see that every statistic it
reports matches the tools they already trust. This document is the operating rule
for that trust, and it is mandatory for all v2 stats work.

## The rule

Every new Data Hub statistical test must ship with BOTH of the following, before
it is merged:

1. **An independent reference value**, generated by
   `frontend/scripts/gen-datahub-stats-golden.py` from scipy, statsmodels,
   lifelines, or R. The reference must come from a tool that was actually run.
   Never hand-type a number from memory, a textbook, or a chat answer.

2. **A pinned comparison** in the "Data Hub statistics" transparency domain
   (`frontend/src/lib/transparency/datasets/datahub-stats.ts`), wired in
   `frontend/src/lib/transparency/run.ts` so our engine is run on the SAME fixed
   dataset and the agreement is classified. The comparison is then enforced on
   every commit by the gate tests
   (`frontend/src/lib/transparency/report.test.ts` and
   `frontend/src/lib/transparency/datahub-stats.gate.test.ts`).

A statistic with no reference value and no pinned comparison is not done, no
matter how clean its unit tests look. A unit test that checks "our engine equals
what our engine produced" proves nothing on its own.

## How the pieces fit

- `gen-datahub-stats-golden.py` holds ONE small, fully hardcoded dataset and runs
  scipy / statsmodels / lifelines over it, printing every reference value with its
  exact provenance (the call that produced it) as JSON. Re-running it must
  reproduce the constants pinned in the TypeScript dataset.
- `datasets/datahub-stats.ts` mirrors that same dataset verbatim and pins each
  reference value as a `StatPin` (value, oracle, tolerance, and an optional
  documented-difference note).
- `run.ts` runs our engine on the dataset and classifies each comparison as
  match, within-tolerance, or documented difference, exactly like the
  bioinformatics domains. It renders on `/transparency` automatically.
- The gate tests fail the build if any pinned statistic drifts beyond its
  tolerance, so the page can never advertise a match the engine no longer makes.

## Adding a new statistic (the checklist)

1. Add the inputs to the fixed dataset in `gen-datahub-stats-golden.py` (reuse an
   existing array if the test fits one; only add data when a test genuinely needs
   a new shape, such as a contingency table or a survival arm).
2. Add the reference computation in the generator, recording the exact
   scipy/statsmodels/lifelines/R call in the `PROVENANCE` block.
3. Run the generator in a throwaway venv and copy the printed reference value:

   ```
   python3 -m venv /tmp/dh-venv
   /tmp/dh-venv/bin/pip install scipy statsmodels lifelines numpy
   /tmp/dh-venv/bin/python frontend/scripts/gen-datahub-stats-golden.py
   ```

4. Mirror any new dataset array in `datasets/datahub-stats.ts` and add a `StatPin`
   for the new statistic.
5. Read the engine's value out in `runDatahubEngine()` in `run.ts` and map it to
   the pin id.
6. Run the gates from `frontend/`:

   ```
   node_modules/.bin/vitest run src/lib/transparency/ src/lib/datahub/
   ```

## Tolerances and documented differences

Most statistics are pinned tight: our engine and the reference agree to several
decimals, so any drift is a bug. A few legitimately differ for a documented
method reason, and the framework supports that ("documented difference"). When you
hit one, do NOT force a match. Widen the tolerance into the `warn` band and write
a `difference` note on the pin explaining why. The note renders on the page so a
reader sees the difference is expected, not hidden.

Current documented differences in the pinned set:

- **Wilcoxon signed-rank p-value.** scipy uses the EXACT signed-rank distribution
  at small n; our engine uses the normal approximation with a continuity
  correction (via @stdlib). At n = 6 these give p = 0.03125 vs ~0.034. Same
  decision at alpha = 0.05; the roughly 0.003 gap is exact-vs-asymptotic, not a
  bug.
- **Levene centering.** Our `levene()` centers on the group MEAN; scipy's
  `levene` defaults to the MEDIAN (which is Brown-Forsythe). So `levene()` is
  pinned against `scipy.stats.levene(center='mean')` and `brownForsythe()`
  against the scipy default `center='median'`. Both match to floating point.
- **Tukey adjusted p.** Both compute the studentized-range adjusted p, but
  statsmodels evaluates the range distribution through `psturng` (a tabulated
  approximation) while ours uses a numeric integral, so adjusted p can differ in
  the third decimal. Two of the three pairwise comparisons are pinned only on the
  mean difference because statsmodels clamps their adjusted p to exactly 0.

## Pending references (engine work not yet done)

These reference values are generated and recorded so the standing rule is already
satisfied when the corresponding engine feature ships. They are NOT gated against
the engine yet because the engine does not compute them:

- **Chi-square test of a contingency table.** The engine has the chi-square
  distribution backend (`chiSquarePValue`) but no contingency-table test. The
  scipy reference for the pinned 2 x 3 table
  (`scipy.stats.chi2_contingency([[10,20,30],[25,15,20]], correction=False)` ->
  chi-square 9.142857, df 2, p 0.010343) lives in `datasets/datahub-stats.ts` as
  `CONTINGENCY` / `CONTINGENCY_REFERENCE`. When the contingency chi-square ships,
  move its pin into `STAT_PINS`, read it in `run.ts`, and delete this note.

## Needs Grant to run Prism (not invented)

Where a comparison would require a GraphPad Prism license to produce, the
reference is NOT fabricated. Prism's published numbers are the most persuasive
cross-check for a Prism alternative, so these are worth capturing, but only Grant
can produce them. Flag them here as they come up:

- A Prism worked-example run of one-way and two-way ANOVA on this fixed dataset
  (Prism's exact Tukey adjusted p-values and the two-way ANOVA table), to pin
  alongside the statsmodels references and confirm we match the specific tool the
  customer is switching from.
- A Prism survival run (Kaplan-Meier curve values and the log-rank p) on the
  survival arms, to pin alongside the lifelines references.

Until Grant runs Prism on the fixed dataset, these stay as scipy / statsmodels /
lifelines references. Do not substitute a guessed Prism number.
