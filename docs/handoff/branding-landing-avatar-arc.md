# Handoff: Branding, Landing, and BeakerBot Avatar arc

You are inheriting the chat that ran the master-orchestrator **branding / landing / avatar** arc on ResearchOS (the same chat also did wiki-refresh round 1 and parked the sharing + sequences wiki content plans). Read `AGENTS.md` first for the master-bot conventions, then this file. Everything below is real and current as of the handoff.

## How work lands here (read this before touching git)
- All work is committed to **LOCAL main**, NOT pushed to origin. Grant's workflow is local-main; push only on his explicit say-so.
- `main` is churned live by the other two active chats (a sharing worker and a wiki worker). The shared checkout's branch can flip under you and `main`'s HEAD moves between reads. After landing anything, verify with `git merge-base --is-ancestor <sha> main`. Do NOT tree-merge a stale sub-bot branch; **cherry-pick the single commit** onto main with path-scoped staging, then run tsc.
- To land a sub-bot's work: `git cherry-pick <sha>` onto main (3-way, preserves parallel sessions' changes), resolve any conflict on shared files, `cd frontend && node_modules/.bin/tsc --noEmit` must be 0, done. The shared checkout has node_modules, so tsc + read-only screenshots of Grant's running :3000 are your verification tools.

## What is DONE (commits on local main)
- **Brand kit** `brand/` (commit `3e028114`): SVG masters (logo mark, four profile avatars peach/sky/white/purple, two banners lockup/wordmark, favicon), `brand/png/` upload-ready PNGs (avatars 1600px, banners 2048px), `brand/src/` generators, `README.md` with the color + type spec. All generated from the real BeakerBot geometry in `frontend/src/components/BeakerBot.tsx`.
- **Favicon** (in `3e028114`): `frontend/src/app/icon.svg` + `favicon.ico` are BeakerBot now (replaced the default triangle). The .ico MUST be an RGBA PNG-in-ICO (built via PIL); a Chrome-screenshot RGB PNG fails Next's build with "PNG is not in RGBA format."
- **BeakerBot `alive` idle** (commit `bf8ae839`): opt-in `alive?: boolean` prop, subtle sway + blink + gaze drift, NO mouth/face-tracking. CSS keyframes in `BeakerBot.tsx`/`.module.css`, gated to neutral poses (idle/pointing*), reduced-motion-aware, per-instance de-synced via post-mount CSS vars (no hydration mismatch). Preview at `/dev/beakerbot-gallery`. The click-to-heart easter egg is preserved (the click-wobble briefly overrides the sway, then it resumes).
- **Landing redesign** (commit `1b98be44`): `frontend/src/components/landing/LandingPage.tsx`, route `/welcome`. Light + rainbow-brand BALANCED hero ("Your whole lab, in a notebook you actually own"), wordmark nav, headline Google/GitHub sign-in row, new Sequence Editor + cross-boundary-sharing bands (inline-SVG illustrations as placeholders), 3-way comparison (ResearchOS vs LabArchives vs SnapGene, honest), stale fixes (ICS-only calendar, version-history-is-shipped).
- **`alive` sweep** (commits `d8ca5a17` + `74d61cf0`): living idle applied to the landing hero, the folder-setup welcome greeting (`ResearchFolderSetupNew`), the cleanup-dialog guide (`Phase4CleanupStep`), and the invite-accept mascot (`/accept/[inviteId]`). One-off greeting waves, dynamic per-step poses, and tiny brand-mark logos were deliberately left static.
- **Avatar tool** `tools/beakerbot-avatar/index.html` (in `3e028114`): standalone talking-head for recording tutorials. Mic-driven mouth + MediaPipe face tracking (head turns, gaze, blinks, raise/furrow brows, o + wide-smile mouth shapes), transparent bg for OBS. Serve over localhost (mic/camera need a secure context): `python3 -m http.server 8765 --directory tools/beakerbot-avatar`. Mouth uses a fast voice-activity GATE for the smile<->talk switch (snaps on a whisper); volume only sets how wide. Brows have a deadzone. NO arms (Grant cut them).

## Open threads (what to pick up)
1. **Login-screen redesign** (Grant flagged, NOT started). He wants first-time visitors to sign in with Google/GitHub on FIRST entry. This is a FUNCTIONAL auth-flow change (touches `UserLoginScreen`, the connect-folder screen, and `lib/sharing/auth.ts` OAuth). Scope it WITH Grant first: investigate the current connect/login flow, come back with a proposal + direction questions before building. The landing's headline sign-in buttons currently route to `/?connect=1` (handleSignIn); the real OAuth (`signIn` via SharingSetupWizard / UserLoginScreen) lives one screen in. Frame everywhere: the notebook needs NO account; sign-in is only for sharing.
2. **Screenshot capture pass** (blocked on UI settling). The two new landing bands use inline-SVG placeholders. Capture in FIXTURE mode only (`?wikiCapture=1`, never real data) once final: `sequence-editor-circular-map.png` (circular plasmid map with annotated features + restriction sites) and `cross-boundary-send-dialog.png` (SendOutsideDialog addressing a collaborator by email). Also round-1 wiki captures are pending: the trash bulk-action-bar needs the fixture seeded with trashed records, and the PI-password login screen needs an `_auth.json` fixture user.
3. **Wiki content plans** (parked; Grant's decisions LOCKED): ONE consolidated cross-boundary-sharing page (fold into `features/sharing-and-permissions`, NO new hub), "encrypted transfer" terminology in user copy, ONE conceptual page named "Sequences" (match `lib/nav.ts`), inline lab-calculators callout, OAuth setup stays a deployer doc. Draft plans live at `docs/proposals/wiki-content-plan-{sharing,sequences}.md` (committed on sub-bot branches `worktree-agent-a232e2e1...` / `...af8a0a4f...`; re-land from those if needed). Held until the sequence + sharing UIs settle. Coordinate with the Wiki Update worker chat to avoid overlap.

## Conventions + gotchas (so you do not relearn them)
- House voice: concept-first, warm, NO em-dashes, NO emojis (every icon is an inline SVG), NO mid-sentence colons, contractions, BeakerBot is the only mascot. Put this in EVERY sub-bot brief.
- Sub-bots: isolated worktree, `git merge main --no-edit` first, SYMLINK node_modules (NEVER install, a real install revokes macOS filesystem permissions and kills every running agent), never `git stash`, never pipe a gate through `tail`, `git add` exact paths only, commit-don't-merge, run `node_modules/.bin/tsc` directly (NOT `pnpm exec`).
- Asset render pipeline: Chrome headless `--screenshot` of an HTML/SVG served on localhost; Geist font via Google Fonts with `--virtual-time-budget` so it loads; `--force-device-scale-factor=2` for hi-DPI PNGs.
- MediaPipe (avatar tool): "dbg is not a function" is a Chrome-EXTENSION environment issue (test in Incognito / a clean profile), not a version or code bug; all tasks-vision versions init cleanly in a clean browser. Use a FRESH FilesetResolver per delegate (GPU then CPU fallback).
- Verification: screenshot Grant's running `:3000` (read-only GET); NEVER start a second `next dev` against master (corrupts Turbopack's persistent cache, fix is `rm -rf frontend/.next`).
- Decisions to Grant use AskUserQuestion (clickable options), not prose. He wants to SEE rendered options before deciding visual/copy direction (that is how the hero positioning was chosen, mockups in `tools/beakerbot-avatar/hero-mock.html`, untracked).

## Status
Nothing is mid-flight; all sub-bots completed and landed. Branding arc is wrapped. Not pushed to origin (awaits Grant). Pick up from the three open threads above, login-screen redesign is the most likely next ask.

Signed, outgoing master / branding orchestrator.
