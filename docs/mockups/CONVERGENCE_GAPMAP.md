# Mobile convergence gap-map (ideal vs live)

Auto-generated by the analysis swarm (10 Sonnet agents, 2026-06-10). Notebook already locked by Grant (not audited). Triage notes by orchestrator.

**10 screens, 58 fixes** (52 live-app, 6 ideal-regenerations, 9 high-priority). 0 auto-locks (all audited screens have at least minor gaps).

Verification lane: **stable** = I can build + verify in Expo Go (Calc/Inventory/Timer/Wiki). **hot** = Notebook-family, needs the dev-client.

## Connect / pairing (pre-pairing state)  `[live-missing]`  `(hot)`
The live screen is structurally close but missing two high-visibility design-system elements: the rainbow bar at the top and the correct screen title ('Connect' vs 'Pair your phone'). Button copy and subtitle copy also diverge from the ideal's benefit-focused wording. The expanded inline code-entry form and separate demo card add real utility but make the layout busier than the clean two-button ideal.

- **[HIGH/layout]** No rainbow gradient bar at the very top of the screen. The ideal has a 4-pixel rainbow stripe (coral -> amber -> green -> sky -> purple) immediately below the status bar, matching every other screen in the design system.
  - fix: Add the rainbow bar component at the top of this screen, the same stripe used on Notebook and all other tabs.
- **[MED/header]** Screen title reads 'Pair your phone' but the ideal calls this screen 'Connect'. The ideal also uses the largetitle style (29px, weight 800, tight letter-spacing) while the live title appears similar in weight but the copy is wrong.
  - fix: Change the title copy to 'Connect' to match the ideal screen name and the tab label.
- **[MED/buttons]** Primary button reads 'Allow camera access' which is a platform-permission request label rather than a user-intent action. The ideal primary button reads 'Connect to your laptop'.
  - fix: Rename the primary button to 'Connect to your laptop' (or 'Scan a code' if camera-intent must be explicit). The sky solid style is correct; only the copy needs updating.
- **[MED/copy]** Subtitle body copy ('ResearchOS needs camera access to scan the pairing code shown on your desktop. You can also type the code in by hand.') is implementation-focused. The ideal uses 'Pair with ResearchOS on your laptop to capture, glance, and sync at the bench.' which is benefit-focused.
  - fix: Replace the subtitle with the ideal wording: 'Pair with ResearchOS on your laptop to capture, glance, and sync at the bench.'
- **[MED/layout]** The live screen shows an inline 'Enter a code manually' grouped card with a text input field and 'Pair with code' button embedded inside a white card, plus a separate 'Try the demo' section below a divider. The ideal shows only two clean buttons (primary + secondary) with no expanded form card or demo path on this screen.
  - fix: Collapse the manual-code entry into the secondary button ('Enter a code') which can open a sheet or expand inline on tap, matching the ideal's two-button layout. The demo path can remain but consider moving it to a tertiary text link rather than a full-width white card button to reduce visual weight.
- **[LOW/header]** A back-arrow chevron appears in the top-left. The ideal frame has no back navigation on this screen -- it is a root-level onboarding destination.
  - fix: Verify whether this screen is reachable from a parent nav stack. If it is the entry point, suppress the back button. If it can be reached from settings, the back arrow is acceptable and the ideal should be updated to reflect that.
- **[IDEAL-REGEN/other]** The PAIRS note flags that the ideal lacks the 'Try the demo' path. The live app offers this as a fallback for users without a desktop, which is a genuine UX improvement the ideal does not capture.
  - fix: Update the ideal frame to include a tertiary 'No desktop? Try the demo' text link below the two main buttons so the target reflects the real shipped experience.

## Inventory  `[live-missing]`  `(stable)`
The live Inventory screen gets the core grouped-card-on-grey rhythm right and the tracked items list is well-structured, but several key design-system details diverge: the scan CTA is an oversized banner instead of a bottom btn.primary, low-stock pills are amber instead of danger red, and the ideal's amber 'Reorder low' section shortcut is absent. These are real usability and visual-language gaps.

- **[HIGH/color]** The 'low' stock pills in the live app render in amber/orange-yellow. The ideal specifies 'Low' as a danger pill (red background, red text via --danger / --danger-bg). Using amber for 'low stock' undercuts the urgency signal.
  - fix: Change low-stock pills to use the danger variant (red) so users immediately recognise a critical reorder need. Reserve amber for creative or neutral actions per the design system.
- **[MED/buttons]** The scan action is a large banner-style card at the top (with subtitle 'Receive, track, and reorder' and a trailing chevron) rather than a compact solid-sky primary button. The ideal places the scan CTA as a simple bottom-of-scroll btn.primary. The live approach dominates above the fold and crowds the inventory list.
  - fix: Demote the scan banner to a standard solid-sky btn.primary ('Scan a barcode') anchored below the scroll content, matching the ideal layout. The banner format is heavier than the design system's btn.primary pattern.
- **[MED/header]** The section header row for 'Tracked items' in the live app is plain text with no action on the right. The ideal shows an amber 'Reorder low' shortcut in that row, providing a one-tap filter for low-stock items.
  - fix: Add an amber-colored 'Reorder low' tappable label to the right side of the 'Tracked items' sec-h row, matching the ideal's flex justify-content:space-between layout.
- **[LOW/copy]** Live subtitle reads 'Scan a package to receive it, track barcodes, and reorder.' The ideal reads 'Track stock and reorder from the bench.' The live version is longer and scan-centric; the ideal is shorter and inventory-centric.
  - fix: Shorten the subtitle to match the ideal: 'Track stock and reorder from the bench.' It better reflects the screen's primary purpose (stock tracking) without front-loading the scan action in copy.
- **[LOW/buttons]** The live app has a separate '+ Add a purchase item' secondary button beneath the scan banner. The ideal has no such standalone add-item button visible, keeping the UI focused on scanning and viewing.
  - fix: Evaluate whether '+ Add a purchase item' is needed at this level of the hierarchy. If kept, style it as a btn.secondary (white card, sky label) rather than a standalone outline-look link. If it duplicates functionality accessible via the scan flow, remove it.

## Quick note compose  `[small-fixes]`  `(hot)`
The compose panel is well-structured and the primary/discard button pattern is mostly right, but several design-system gaps exist: the "Quick note" trigger button uses coral (correct for a creative action), but "Send to lab" is shown in a washed-out pale sky that does not meet the solid-sky primary spec; the floating keyboard toolbar uses an emoji/smiley icon which violates the no-emoji custom-icon rule; and the title/subtitle typography sits slightly under the 800-weight large-title spec. No ideal frame exists for this screen and one should be authored.

- **[HIGH/buttons]** The 'Send to lab' primary action button renders in a pale, desaturated sky blue rather than the solid sky #1AA0E6 required for primary actions. It reads as disabled even when it should be active (or intentionally disabled but still needs the correct disabled-state treatment).
  - fix: When the note body is non-empty, render 'Send to lab' as a solid sky #1AA0E6 filled button with white label. For the empty/disabled state, use opacity-50 on the solid sky rather than a completely different washed-out fill so the affordance remains consistent.
- **[HIGH/iconography]** The floating keyboard accessory toolbar contains a smiley-face emoji/glyph icon (third button from top). The house style prohibits emojis and requires custom inline SVG icons for every user-facing icon.
  - fix: Replace the smiley-face glyph with a custom SVG icon (e.g., a simple smile outline matching the BeakerBot/lab icon family) using the <Icon name=...> component, consistent with StreakBadge / AppShell patterns.
- **[MED/typography]** The screen title 'Notebook' appears at roughly the correct large size but the weight looks closer to 700 (bold) than the specified 800 (extrabold). The subtitle beneath it ('Capture the bench into your lab notebook...') spans two lines and uses a relatively small grey text; the line-break at a narrow width creates an awkward widow.
  - fix: Set the title to font-weight 800 (Tailwind: font-extrabold) and add tracking-tight. Tighten or reword the subtitle to a single line at the available width, e.g. 'Capture the bench. See what is on today.'
- **[MED/layout]** The 'Title, optional' and 'Write your note' input fields are rendered as two separate bordered card-like boxes. The design system specifies a single grouped container with hairline separators between rows, not separate bordered cards per row.
  - fix: Wrap both inputs in one rounded white card. Separate them with a single 1px hairline divider (#e5e7eb or similar). Remove individual card borders/shadows from each field.
- **[MED/buttons]** The 'Discard' button is a white card with a sky-colored label, but per the design system, a discard/destructive secondary action should use coral (#FF6F61) as the accent tint, not sky.
  - fix: Change the 'Discard' button label color to coral #FF6F61 to signal its destructive nature, keeping the white card background.
- **[IDEAL-REGEN/other]** No ideal frame exists for this screen. The Quick note compose flow is a first-class mobile interaction that deserves a design-system-compliant reference frame for future audits.
  - fix: Author an ideal mockup for this screen covering: the grouped input card, solid-sky 'Send to lab' button, coral 'Discard' label, custom toolbar icons, and 800-weight title.

## Settings  `[small-fixes]`  `(hot)`
The Settings screen is structurally sound with correct grouped-container usage for Interaction and reasonable copy, but the canvas background reads as white rather than #f2f3f7 so cards do not pop, the page title is a small nav-bar string rather than the 34px/800-weight large title, and the Alerts section rows are not unified inside a single grouped container. An ideal frame should be authored for this screen so future audits have a concrete target.

- **[HIGH/layout]** The Alerts section presents Alarm, Sound, Chime/Digital segmented control, Play sample, and Vibration as visually loose rows without a single unified grouped container with hairline separators between them.
  - fix: Wrap all Alerts rows inside one grouped white card with hairline dividers between each row, matching the Interaction section's treatment.
- **[MED/color]** The page background appears white (#ffffff) rather than the canvas near-white grey #f2f3f7, so the white cards do not visually pop off the surface.
  - fix: Set the ScrollView/page background to #f2f3f7 to match the design system canvas color.
- **[MED/typography]** The screen title 'Settings' is rendered as a small navigation-bar header (standard iOS nav title weight/size, ~17px) rather than the 34px weight-800 large title specified by the design system.
  - fix: Use the large-title pattern (34px, weight 800, tight letter-spacing ~-0.8) for the Settings heading, consistent with other primary screens. If this is a pushed sub-screen where a large title is inappropriate, document the exception explicitly in the design system.
- **[MED/iconography]** The 'Play sample' row uses a circle-play icon and the Alarm row uses a bell icon -- cannot confirm from the screenshot whether these are the house <Icon name=...> component or inline lucide/SVG imports, which would violate the icon guard rule.
  - fix: Audit the Settings screen component to confirm both icons reference the house Icon component; replace any lucide or inline SVG usage.
- **[LOW/buttons]** The Chime/Digital segmented control has a visible rectangular border around the entire control giving it a web-form appearance rather than a native pill-style segment picker.
  - fix: Use a pill-shaped background container (rounded-full or rounded-xl) without an explicit stroke border; the selected segment gets a solid sky fill with white label, unselected gets transparent with a muted label.
- **[IDEAL-REGEN/other]** No ideal/design frame exists for the Settings screen, making future convergence audits purely system-spec-based with no visual reference.
  - fix: Author an ideal mockup frame for Settings (all sections visible: Appearance, Interaction, Alerts, Device and lab) so the audit target is concrete.

## Timer  `[live-missing]`  `(stable)`
The live Timer screen has the right typography, color language, and keypad shape, but deviates meaningfully from the ideal in structure: all content is buried inside a single 'New timer' white card instead of sitting on the canvas, the running-timer row (amber icon + coral stop) is absent, the display digits are washed out, the hint label is lowercase, and two mid-card section labels ('OR SET A CUSTOM TIME') contradict the house sec-h pattern.

- **[HIGH/layout]** All timer content (quick-start pills, keypad, display, CTA) is wrapped inside a single white 'New timer' card. The ideal places the quick-start pills and keypad directly on the canvas (no enclosing card), with only the running-timer entry in a grouped card.
  - fix: Remove the outer 'New timer' white card wrapper. Let the quick-start section header, pills, display, and keypad sit directly on the #f2f3f7 canvas. Reserve the white grouped card for an active running-timer row only.
- **[HIGH/layout]** The ideal shows a running-timer card (amber icon + countdown + coral Stop action) above the quick-start section, demonstrating the active-timer state. The live screen does not implement this running-timer row in the list view.
  - fix: When a timer is running, render it as a grouped-list row above the quick-start section: amber timer icon, HH:MM countdown, description label, and coral 'Stop' text action -- matching the ideal's running-timer card.
- **[MED/copy]** Live shows 'QUICK START' and 'OR SET A CUSTOM TIME' as section labels inside the card. The ideal uses the house sec-h style 'Quick start' (mixed case, grey, uppercase letter-spacing) as a standalone section header on the canvas, with no 'OR SET A CUSTOM TIME' divider.
  - fix: Replace 'QUICK START' and 'OR SET A CUSTOM TIME' with a single 'Quick start' sec-h label and remove the divider label. Let the display naturally follow the pills without a second header.
- **[MED/color]** The 00:00:00 display digits appear very washed-out/pale (low-opacity sky), making the display feel unconfident. The ideal specifies solid sky (#1AA0E6) at full opacity for the display.
  - fix: Set the timer display color to full-opacity sky #1AA0E6 with no transparency reduction.
- **[MED/buttons]** The 'Enter a time' CTA button at the bottom appears disabled/pale sky (light background, muted text) rather than the solid primary sky button the design system specifies for primary actions.
  - fix: When a time has not yet been entered the button can stay disabled, but the disabled state should still use the sky color family with reduced opacity rather than a generic grey wash. Alternatively, activate the button style once any digit is entered.
- **[LOW/copy]** Live subtitle reads 'Start a countdown at the bench and get an alert when it finishes.' The ideal subtitle reads 'Start a countdown and get an alert when it finishes.' (no 'at the bench').
  - fix: Trim the subtitle to match the ideal: 'Start a countdown and get an alert when it finishes.'
- **[LOW/typography]** The 'hours : min : sec' hint label is lowercase. The ideal renders it as 'HOURS : MIN : SEC' in uppercase with a letter-spacing hint class (font-size 10px, weight 600, letter-spacing 0.08em).
  - fix: Uppercase the hint label and apply tight tracking: 'HOURS : MIN : SEC' in 10-11px, weight 600, letter-spacing ~0.08em, color ink-3.

## Wiki base page  `[live-missing]`  `(stable)`
The live Wiki base page is close in structure but has three meaningful gaps versus the ideal: the title is 'Help & Wiki' instead of the single-word 'Wiki', the AI 'Ask:' callout chip is absent, and a QUICKSTART section appears that the ideal does not call for. Secondary issues are a sync-status timestamp line that breaks the flow and a featured sky-tint on the Overview row that should be plain.

- **[HIGH/header]** Page title reads 'Help & Wiki' instead of the single-word 'Wiki' the design system specifies. The large title should be the tab label only, not a compound phrase.
  - fix: Change the page heading to just 'Wiki' (34px, weight 800) to match every other tab title in the app (Notebook, Calc, Timer).
- **[HIGH/copy]** The 'Ask: how do I pair my phone?' AI-prompt callout chip (the `.ask` row with a spark icon) is present in the ideal but entirely absent from the live screen.
  - fix: Add the Ask callout row between the search field and the 'Start here' section. Use the spark icon (sky accent) and placeholder copy 'Ask: how do I pair my phone?' or equivalent, styled as a secondary tappable chip that opens an AI-driven help flow.
- **[MED/layout]** Live screen has an extra 'QUICKSTART' section ('ResearchOS Wiki -- 60-second tour') that is not in the ideal and adds a section the design does not call for.
  - fix: Evaluate whether QUICKSTART belongs; if kept, fold its single entry into 'Start here' as a second row alongside 'Read this first'. If it duplicates that row's intent, remove it.
- **[MED/layout]** The 'Overview' row in Getting Started is rendered with a sky-tinted highlight (featured/active treatment) in the live app, making it visually distinct from its sibling rows. The ideal treats all Getting Started rows identically with no accent.
  - fix: Remove the featured sky-tint from the Overview row. All Getting Started rows should use the same plain grouped-list style with a chevron only.
- **[LOW/copy]** A 'Last pulled from the web wiki on Jun 9, 2026' sync-status line sits between the search bar and the first section. The ideal has no such line.
  - fix: Move the sync-status timestamp to a less prominent location (e.g., footer of the scroll area or behind a small info icon) so it does not interrupt the visual flow between search and content.

## Calc  `[small-fixes]`  `(stable)`
The live Calc screen is structurally very close to the ideal: coral AC, sky del/Ans, amber equals, sky-tinted operators, dark display panel, horizontal chip selector, and the canvas color all match. Two targeted fixes would fully align it: the active function strip chip should be amber (not sky-tinted), and the Export to notebook secondary button label is rendered in muted grey instead of sky, breaking the design system secondary button rule.

- **[MED/color]** In the function strip (sin, cos, tan, asin, acos), the first chip 'sin' is sky-tinted background with sky text, matching the inactive chips. The ideal specifies the active/first function chip as solid amber background with white text, using the amber accent for a creative/active-mode indicator.
  - fix: Set the active function chip (whichever is currently selected, defaulting to 'sin') to background var(--amber) / #F59E0B with white text. Inactive function chips stay at sky-tinted bg with sky label.
- **[MED/buttons]** The 'Export to notebook' secondary button at the bottom has a muted grey label color, making it appear disabled or inactive. The design system secondary button spec requires a white card with a sky-tinted label for a standard action.
  - fix: Change the Export to notebook button label color to sky (#1AA0E6) so it reads as an active secondary button, not a disabled one. Keep the white card background.
- **[LOW/copy]** The fifth calculator mode tab in the live app appears to be cut off and reads as 'Buffer', while the ideal specifies 'Primer Tm' as the fifth tab. These may be different modes, but if Primer Tm is available it should appear in the scrollable tab strip.
  - fix: Verify that 'Primer Tm' mode exists in the live tab list and is included. If 'Buffer' is a different mode that coexists, ensure the tab order matches the ideal (Scientific, Molarity, Dilution, Serial, Primer Tm) or reflects intentional product decisions.
- **[LOW/other]** Inactive calculator mode chips (Molarity, Dilution, Serial, Buffer) in the live app appear as plain white outlined pills without any drop shadow. The ideal specifies card-shadow on inactive chips so they visually pop off the canvas.
  - fix: Add a subtle card-shadow (0 1px 2px rgba(16,24,40,.05), 0 8px 24px rgba(16,24,40,.06)) to inactive mode chips to match the ideal chip treatment.

## Notebook chooser sheet  `[small-fixes]`  `(hot)`
The sheet is functionally clear and well-structured but has several design system deviations worth tightening: individual bordered cards per row instead of a single grouped container with hairline separators, icon tints that stray outside the sky/coral/amber trio (green, purple), and an ambiguous 'Unsorted note' affordance that should be a styled secondary button. No ideal frame exists for this screen, which should be authored to lock the spec.

- **[MED/layout]** The sheet uses three separate labelled section headers (YOUR NOTEBOOKS, SHARED WITH YOU, 1:1 NOTEBOOKS) with individually bordered card rows rather than a single grouped container with hairline separators between rows. The design system calls for ONE grouped container per section with hairline dividers, not a separate bordered card per row.
  - fix: Collapse each section into a single rounded grouped container where rows are separated by a hairline (1px, ~10% opacity black), matching iOS grouped list convention. The section label sits above the container as a small caps uppercase label, not between cards.
- **[MED/color]** The notebook row icons use an amber/yellow tint for 'Running log' and a green tint for 'CRISPR knockout screen' and a purple tint for '1:1 with morgan'. The design system trio is sky #1AA0E6, coral #FF6F61, amber #F59E0B. Green and purple are outside the trio palette.
  - fix: Map notebook-type icons to the trio: owned notebooks use sky, shared notebooks use amber (collaborative/creative connotation), 1:1 notebooks use coral. Remove green and purple from the icon tint set.
- **[MED/buttons]** 'Unsorted note (inbox)' at the bottom is rendered as a lightly bordered secondary row with an inbox icon but no clear button variant. It reads as a list row, not as a distinct secondary action button, making its affordance ambiguous.
  - fix: Render 'Unsorted note (inbox)' as a white card secondary button with a sky-tinted label and the inbox icon to the left, matching the secondary button spec (white card, accent-tinted label). Separate it visually from the notebook list with extra top margin.
- **[IDEAL-REGEN/other]** No ideal frame has been authored for this screen (the Notebook chooser bottom sheet). It is a non-trivial interactive surface with section grouping, type-based icon tinting, and a secondary action button that all deviate from the design system. An ideal frame would help lock the spec before future iterations.
  - fix: Author an ideal frame for the Notebook chooser sheet that shows the single-grouped-container layout per section, trio-only icon tints, and the correctly styled 'Unsorted' secondary button.
- **[LOW/typography]** The sheet title 'Where should this go?' appears to be roughly 20-22px weight 600-700. The subtitle 'Pick a notebook, or leave it unsorted.' uses a muted body size. Neither is wrong for a bottom sheet, but the title weight feels lighter than the 800-weight tight-tracking house style for prominent titles.
  - fix: Bump the sheet title to weight 800 with letter-spacing ~-0.5 to match the house large-title feel, even at ~22px (sheets are smaller than full screens so the size is appropriate, but the weight should stay 800).
- **[LOW/iconography]** The camera and quick-note buttons in the main content above the sheet use emoji-style SF Symbol glyphs rendered at low contrast inside large colored tiles. The design system mandates custom inline SVG icons, not system glyphs.
  - fix: Replace the camera and edit-square glyphs with custom SVG icons from the house icon set (matching the <Icon name=...> pattern). Verify these are not already using the Icon component -- if they are, this is fine.

## Send / sent state  `[small-fixes]`  `(hot)`
The live screen is structurally solid -- large title, trio-accented primary action tiles, and a clear Outbox section with sent-state feedback are all present. However, the amber tint on the secondary 'Upload from camera roll' button is wrong for a non-creative action, the 'Send all' button reads as secondary when it should be primary, and the green 'Sent' badge falls outside the trio palette. No authored ideal frame exists for this screen, which is itself a gap.

- **[MED/color]** The 'Upload from camera roll' button uses amber text (#F59E0B) on a white card, but this action is not a creative/annotate action -- it is a standard secondary action and should use the sky accent per the design system's button rules.
  - fix: Change the 'Upload from camera roll' label color from amber to sky (#1AA0E6) to match the secondary button spec (white card, sky-tinted label).
- **[MED/buttons]** The 'Send all' button inside the Outbox section is styled as a white card with sky text, which reads as a secondary button. Sending all queued items is the primary action on this screen and should visually read as primary.
  - fix: Upgrade 'Send all' to a solid sky (#1AA0E6) filled button to signal it is the primary call-to-action in the Outbox section.
- **[IDEAL-REGEN/other]** No authored ideal frame exists for this screen. The 'sent state' (item in outbox with a Sent badge + 'Appears on your laptop in a few seconds.' helper text) is a meaningful UX state worth capturing in a reference frame so future audits have a target.
  - fix: Author an ideal frame for the Send / sent state screen covering the Outbox section with a Sent item, correct badge color, helper text, and 'Send all' button treatment.
- **[LOW/color]** The 'Sent' badge on the outbox item uses a light green pill, which is a fourth color not in the trio palette (sky / coral / amber). The design system specifies trio accents only.
  - fix: Replace the green 'Sent' badge with a sky-tinted pill (sky background at low opacity with sky text) to stay within the trio palette and signal a completed/positive state.
- **[LOW/typography]** The screen title 'Notebook' appears at approximately the correct large-title size but the subtitle 'Capture the bench into your lab notebook, and see what is on today.' is long, awkward prose, and the phrase 'what is on today' reads as unnatural copy.
  - fix: Tighten the subtitle to something like 'Capture the bench into your lab notebook and see what is happening today.' -- remove the comma before 'and' and fix the awkward closing phrase.
- **[LOW/layout]** The 'Take a photo' and 'Quick note' primary action buttons each appear as large rectangular tiles with an icon above the label. This is a valid large-action pattern, but they sit directly on the canvas with no grouped-container wrapper, which is inconsistent with the design system's grouped-container rule for related controls.
  - fix: Wrap the two primary action tiles in a single grouped card container so they share the white-card-on-grey-canvas treatment that the design system prescribes.

## Wiki rendered page  `[small-fixes]`  `(hot)`
The live wiki rendered page is readable and functional, but drifts from the design system in three meaningful ways: the canvas is pure white instead of #f2f3f7, the h1 title falls short of the 34px/800 large-title spec, and the back chevron uses iOS default blue rather than the house sky accent. An ideal frame for this screen does not exist and should be authored to standardize the rendered markdown typography and callout block variants.

- **[MED/typography]** The large page title 'Oh god, another massive docs site' renders at what appears to be roughly 28-30px weight 700, not the design system spec of 34px weight 800 with tight letter-spacing (~-0.8). It lacks the modern iOS large-title feel.
  - fix: Apply font-size: 34px, font-weight: 800, letter-spacing: -0.8px to the wiki article title. If this is prose content from MDX/markdown, apply the style to the h1 element in the wiki renderer.
- **[MED/color]** The canvas background is pure white, not the near-white grey #f2f3f7 specified by the design system. The article content sits flat on white with no card-pop contrast.
  - fix: Set the page background to #f2f3f7 and wrap the article body in a white card with a subtle shadow so content pops off the canvas as the design system intends.
- **[IDEAL-REGEN/other]** There is no ideal frame authored for this screen. Given that this is a content-heavy wiki rendered page with typography, callout blocks, and back navigation, an ideal frame would help standardize heading hierarchy (h1/h2/h3 sizes and weights), callout/blockquote variants, and link styles within rendered wiki content.
  - fix: Author an ideal mockup for the wiki rendered page covering: large-title h1, section h2 sizing, body paragraph line-height, callout block variants (info/warning), and inline link color using sky #1AA0E6.
- **[LOW/header]** The back chevron is plain system blue (iOS default tint), not the design system sky #1AA0E6. It appears as a generic iOS back arrow with no label.
  - fix: Tint the back button with sky #1AA0E6 to match the trio accent language, or replace with a custom <Icon name=...> back arrow component so it matches the house style.
- **[LOW/layout]** The numbered list item at the bottom ('1. Your data is yours, on your machine') appears in a light blue/tinted callout box, but the styling is inconsistent -- it looks like a one-off inline style rather than a proper grouped-container with hairline separators for list items.
  - fix: If this is meant to be a grouped list, render all numbered items in one grouped container with hairline separators between rows, matching the house list pattern. If it is a callout/highlight block, use a defined callout variant with sky #1AA0E6 left border rather than a full background tint.
- **[LOW/spacing]** The article has no visible horizontal padding margin on the left or right edges -- text appears to run very close to the screen edges, making it feel cramped on mobile.
  - fix: Apply consistent horizontal padding (16-20px) to the article container so text has breathing room from the screen edges.
