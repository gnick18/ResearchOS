# Handoff: phone-compatible web pages + UW citation scrub + IP disclosure (2026-06-15)

Session picked up the throttled "Billing" lane and then did several side-quests. All on **local main, UNPUSHED**. tsc 0 throughout.

> **UPDATE 2026-06-15 (takeover session):** Welcome V1 is **DONE + merged to local main** (still UNPUSHED). Rebased the parked worktree onto current main (clean, citation wording matched), finished the build, and re-themed the demo bento to the R2 `welcome/` set per Grant's call (re-theme, not drop): `replaces-5-tools`→cost lead, `own-your-data`→how-it-works, `pi-lab-overview`→check-ins, `methods-library`→companion, `sequence-editor-a`→sequence; chemistry/data-hub/purchases/nih + the 2nd sequence clip went text-only (made `FeatureRow.visual` optional). All 7 stale Vercel Blob clips removed. Verified at 390px (tri-CTA + Coming-soon store badges + phone cost cards render) and no horizontal overflow at desktop width; tsc 0; welcome+marketing tests green. **Icon baseline bumped 3→5 for the 2 Apple/Google store badges (brand marks) — pending Grant sign-off.** Merge commit on local main. **Phase 2 (per-page reflows) is now ALSO DONE + merged** — Pricing, AI (`/ai`), Transparency, and Dept+Institution admin pages all reflowed for phones by 4 parallel sub-bots in isolated worktrees (responsive-hybrid; desktop ≥sm untouched; stacked cards / single-col / clamp / 44px tap / overflow-x-auto below 640px), each 390px no-overflow + tsc 0, merged sequentially, final combined tsc 0. **All phone-pages work is now done + merged to local main, UNPUSHED.** Remaining: Grant's live browser pass (real phone / real Chrome at 390px across all pages) and the R2 `welcome/` clips loading on prod (CSP confirmed shared with the live icon library by the Figure Composer lane). **Store-badge decision RESOLVED (Grant): brand-free.** The tri-CTA store badges are replaced by an "iOS & Android · Coming soon" pill with no Apple/Google marks (official badges only apply once real store listings exist + require their own artwork); removed the StoreBadge component + 2 inline brand SVGs + placeholder APP_STORE_URL/PLAY_STORE_URL; icon baseline reverted 5→3 (no sign-off needed). At launch, MobileUI swaps in the official Apple/Google badge artwork + flips `NEXT_PUBLIC_COMPANION_APP_LIVE`. Also added an open icon-library resource block on Welcome (links `/library`, per the INJEST lane directive).

## 1. Phone-compatible web pages (IN FLIGHT, the main open build)
Grant wants the public/text web pages phone-compatible. Phones already hit a distinct path: the app needs the File System Access API (Chrome/Edge desktop only), so `providers.tsx` renders `<WelcomePage unsupported />` on phones (read-only marketing). That marketing surface IS the phone surface.

- **Approved direction (mockup):** `docs/mockups/2026-06-15-phone-welcome-redesign.html`. Decisions LOCKED by Grant: **hybrid** approach (responsive base + swap heavy desktop chunks like the comparison TABLE to stacked cards below `sm`); **tri-CTA** on the phone/`unsupported` path (capture email / push the companion app / send to desktop for full app + demo); keep the **branded `MarketingBackdrop` + accents** on phone (no flattening to white); keep the **living BeakerBot** at parity with desktop (big `alive` hero Beaker + `BeakerBotPeek` over the same sections desktop uses, always pastel). Quality bar: desktop is "9/10", phone must match.
- **Welcome V1 build = PARKED, NEEDS FINISHING.** A background sub-agent built it but got server-throttled before committing/verifying. Its work is **uncommitted in worktree** `.claude/worktrees/agent-aa4825134ffbb2215` (only `WelcomePage.tsx` + `WelcomePage.test.tsx`). The worktree branch is behind main. TO RESUME: rebase onto current main, finish per the mockup, run `vitest run src/components/welcome` + Preview at ~390px (and desktop, confirm no regression), then merge. NOTE the bot's brief is in this session's transcript; key reqs = reuse `BeakerBotPeek`/`MarketingBackdrop`/`BeakerBot alive`, companion-app badges fall back to "Coming soon" if no real store links, email capture wires to an existing endpoint or a safe placeholder (no silently-broken form). The citation line in that worktree's WelcomePage was already updated to the origin-credit wording (line ~1654), so no conflict there.
- **Phase 2 (NOT STARTED):** parallel per-page responsive reflows for Pricing, AI (`/ai`), Transparency, and the Dept + Institution admin/login surfaces. Wiki + Institution-join are already mobile-good (reference patterns). Grant chose **parallel sub-bots in worktrees** for this, to run AFTER the welcome flagship merges (welcome touches shared chrome `MarketingBackdrop`/`MarketingNav`). No shared viewport hook exists; use Tailwind `sm:` toggles (SSR-safe), v4 default breakpoints (phone = below 640px). Full per-page inventory is in this session's transcript.

## 2. Website-wide UW citation scrub (DONE, committed `762aa13e5`)
UW OVCR (Petra Schroeder) asked that nothing state or imply UW-Madison/WARF funding supports the company or keeps it free. Replaced every such claim with **origin-only credit** ("ResearchOS grew out of work begun during a UW-Madison Distinguished Research Fellowship") and re-anchored the "why it's free" story on **open source + local-first + voluntary sponsors**. Covered the live site + README + canonical/public docs (deck, brand rulebook, MESSAGING, POSITIONING, release notes, both Vercel OSS apps, PRICING_TRANSPARENCY, ROADMAP) + regenerated wiki JSON + mobile bundle. Left as-is: dated `docs/mockups/*` snapshots (historical), audience "grant-funded labs" mentions, the personal "UW Fellow" badge. Final grep confirmed clean.

## 3. Business-page grant number (DONE, committed `09e0d4f6a`)
Added a `fundingGrantNo` entity-fact field to the operator-only business console (`/admin#finances`, gated): `EntityConfig` + `funding_grant_no` column (idempotent) + upsert + the BusinessTracker input. **Defaulted null in source on purpose** (calc.ts is public). The real value (UW project number **PJ000007271**, non-federal, via OVCR/WARF) must be entered ONCE in the operator console so it persists to the private DB, never the public repo. Grant has the number.

## 4. Petra email + IP disclosure (Grant's actions, DONE on his side)
- Reply to Petra is drafted (in transcript), humanized, no em dashes, covers the IP disclosure + the wording fix + a call offer. Grant sends it himself.
- Grant **submitted** the UW IP disclosure (non-official, informational). Funding recorded as the fellowship via OVCR/WARF + grant PJ000007271 under Non-Federal Contracts.

## 5. Earlier this session: onboarding wizard go-live
P1+P2+P3 all built + merged behind `NEXT_PUBLIC_ONBOARDING_WIZARD` (off/inert), awaiting Grant's live browser-verify of P2. See memory `[[project_onboarding_wizard_golive]]` and the AGENTS.md Billing-lane block.

## Coordination note
The CCD TaskList is SESSION-LOCAL (not shared); cross-lane claims go via the CDD `send_message` tool (sessions only, not sub-agents). Sub-agents have no live message channel, so to push a change into a sub-agent's work, edit its worktree branch directly.

## Welcome-page videos (DECISION 2026-06-15, do during the redesign)
The 7 `DemoLoop` slots in `WelcomePage.tsx` currently point at OLD videos on **Vercel Blob** (`tkqei2x7bdmdvg7v.public.blob.vercel-storage.com`: chemistry-gliotoxin, data-hub, sequence-editor-a, sequence-ncbi, purchases, check-ins, nih-zenodo). **Grant's call: remove all of these, they're stale.** Do it as part of the phone-Welcome redesign pass (same file, avoids a layout-break + double edit). Grant uploaded a NEW lineup to **R2 `welcome/`** served at `https://assets.research-os.com/welcome/<name>.mp4` (+ `.poster.jpg`): own-your-data, replaces-5-tools, pi-lab-overview, methods-library, sequence-editor-a. DECIDE during the redesign whether the new sections use that R2 set (re-theme) or drop the video bento entirely. `DemoLoop` needs a required `src`; removing a slot means removing its wrapping cell too (watch the grid). `ASSET_BASE_URL` (`lib/figure/asset-library.ts`, defaults `https://assets.research-os.com`) is the constant to reuse for the R2 base. Also pending uncommitted on main working tree: the OAuthFirstLanding top-left trim ("ResearchOS LLC, Madison, Wisconsin" → "ResearchOS LLC", de-duped the Madison mentions).

## NEXT (in order)
1. Finish + merge the phone Welcome V1 (rebase the parked worktree) — INCLUDING removing the 7 old Vercel Blob DemoLoop videos per the decision above.
2. Dispatch Phase 2 phone reflows (Pricing / AI / Transparency / Dept+Inst).
3. Grant: enter PJ000007271 in the operator console; send the Petra email; push local main when ready.
